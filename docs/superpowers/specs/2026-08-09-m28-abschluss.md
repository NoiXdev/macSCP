# M28 — Abschlussbericht (Die zwei löschenden Binder)

**Status:** abgeschlossen 2026-08-09. HEAD vor diesem Bericht: `360dc2d`.

> **Dieser Bericht wurde nach der Whole-Branch-Review neu gefasst.** Seine
> erste Fassung (`071776b`) beschrieb einen Meilenstein mit vier Tasks und
> nannte Abschnitt 6 als offenen Befund. Die Review hat daraus drei weitere
> Tasks gemacht — darunter einen **Critical**, den keine der vier
> Task-Reviews sehen konnte. Alle Zahlen unten sind nachgemessen, keine
> fortgeschriebene.

Doc-Commits: `3df3146` (Zielverschiebung), `d657995` (Plan), `3ce47f7`
(Korrektur der Plan-Aussage zur Managed-Key-Probe), `071776b` (erste Fassung
dieses Berichts), `ffad380` + `603c3dc` (Kommentar- und Zitat-Korrekturen).
Task-Commits: `d92a520` (T1, die Deckungsfrage), `a6d71b5` + `96986e7` +
`0908628` + `b4d428c` + `2dab226` (T2, `applyMerge` und vier Fix-Runden),
`325d571` (T3, die Jump-Bindung), `1b8802c` (T4, der Import-Hinweis),
`28002dd` + `9cc5a78` (T5/T6, das artfremde Set — Abschnitt 6 der ersten
Fassung), `b42ba58` + `e78ae99` + `f9c9979` (T7, der fünfte Credential-Pfad),
`9a68093` (T7, `deleteLoginSet`), `360dc2d` (die Kopplung Predicate↔Resolver).

**Unversendet:** `git rev-list --count origin/develop..develop` → **10** vor
diesem Bericht, **11** danach. Nur die Commits bis `1b8802c` sind auf
`origin/develop` (Ledger-Eintrag „PUSHED 2026-08-09: 61 commits,
9f99624..1b8802c, CI grün in 4m56s"); alles danach ist lokal. **Release-Stau
unverändert offen:** `git rev-list --count origin/main..develop` → **385**
(386 nach diesem Commit); M27 nannte 348.

Spec: `2026-08-09-m28-loeschende-binder-design.md`.
Plan: `../plans/2026-08-09-m28-loeschende-binder.md`.

## Die Vorgeschichte in einem Absatz

Am selben Tag wurde ein Login-Set-Slot-Fix **zurückgenommen** (`479d018`).
Sein Ziel war der **veraltete Slot**: eine an ein Login-Set gebundene Sitzung
behält ihr altes Passwort im Schlüsselbund. Vier adversariale Reviews fanden
vier Wege, auf denen der Fix dabei die **einzige** Kopie eines Secrets
vernichtete. Die Aufklärung danach hat die Zielsetzung widerlegt: der
veraltete Slot ist der **milde** Zustand — es geht nichts verloren, und der
Connect verweigert ehrlich. Gefährlich sind zwei Stellen, die der
zurückgenommene Fix **nie berührt** hat und die schon vorher da waren: von
fünf Bindern löschen genau zwei einen Schlüsselbund-Slot (`applyMerge` und die
Jump-Bindung über `cleanOrphanedJumpSlot`), und keiner von beiden fragte
dabei irgendeine Bedingung ab. M28 lässt sie fragen — und fasst die drei
nicht löschenden Binder und den veralteten Slot ausdrücklich **nicht** an.

## Verifikation zum Abschluss

Alles Folgende wurde in dieser Sitzung ausgeführt, nichts aus den
Task-Berichten übernommen.

| Lauf | Ergebnis |
|---|---|
| `swift build` | Exit 0, keine Warnungen (7,83 s), **einschließlich App-Target** |
| `swift test` | **1679 Tests in 131 Suiten, grün** (4,08 s) |
| `docker compose -f docker/test-server/compose.yml up -d` | Rig oben (sshd, sshd2, minio, minio-init, webdav), gestartet aus dem **Haupt-Checkout** |
| `MACSCP_ITEST=1 swift test` | **1679 Tests in 131 Suiten, grün** (14,66 s) |
| `MACSCP_KEYCHAIN=1 swift test --filter Keychain` | **28 Tests in 10 Suiten, grün** (0,073 s) |
| `plutil -lint` über alle acht Kataloge (4× App, 4× Core) | jede Datei `OK` |
| `pgrep -fl swiftpm-testing-helper` | keine Treffer, keine Waisen |
| `git status --porcelain` | leer (vor und nach jeder Gegenprobe) |

**Dass die gegatete Suite wirklich lief**, ist an zwei Dingen ablesbar: an der
Laufzeit (4,08 s ungegatet gegen 14,66 s gegatet) und daran, dass die Ausgabe
namentlich die Suite `CitadelFileSystem against Docker SSH server` führt. Die
Testzahl ist in beiden Läufen identisch, weil die Integrationstests intern
über die Umgebungsvariable früh zurückkehren, statt zusätzliche Testfunktionen
beizusteuern — dieselbe Erklärung wie in M24–M27.

Testzahl **1640 (Planbasis) → 1679**, also **+39**: 11 aus T1 (3 in
`BackendDescriptorTests`, 8 in `SessionListViewModelTests`), 8 aus T2 über
vier Fix-Runden, 7 aus T3, 1 aus T4, 8 aus T5/T6 (`LoginResolverTests` und
`JumpLoginSetEligibilityTests`), 3 aus T7, 1 aus dem Kopplungstest.

**Der seit M20 bekannte 0-%-CPU-Hänger trat in keinem der Läufe auf.** Das ist
eine Beobachtung, kein Beweis seiner Abwesenheit.

Milestone-Diff (`git diff --stat 479d018..HEAD -- Sources Tests`): **22
Dateien, +1674 / −35**. Vier Core-Quelldateien (`SessionListViewModel.swift`,
`ConnectionViewModel.swift`, `BackendDescriptor.swift`, `LoginResolver.swift`)
plus die neue `JumpLoginSetEligibility.swift`, drei App-Dateien
(`ContentView.swift`, `ConnectionFormView.swift`, `LoginSetsSheet.swift`),
acht Kataloge, fünf Testdateien. Die 35 Löschungen sind ausschließlich
ersetzte Zeilen — M28 ist fast reine Ergänzung.

## Die Gegenprobe zum Verlustweg (Kriterium 5)

Die Deckungsfrage wurde probeweise auf die Formulierung des **zurückgenommenen
Anlaufs** umgestellt. **Diese Probe lief bei HEAD `1b8802c`**, also vor T5–T7;
die Testzahlen darin sind die von damals (1667/130), das Ergebnis gilt
unverändert:

```swift
let descriptor = BackendDescriptor.descriptor(for: set.kind)
_ = descriptor
guard set.authKind != .agent else { return true }
```

`swift test --filter Covered`, wörtlich:

```
✘ Test anS3SetDeclaringAgentAuthIsStillNotCovered() recorded an issue at SessionListViewModelTests.swift:3217:9: Expectation failed: try vm.setCoversItsLogin(set) == false
✘ Test anS3SetDeclaringAgentAuthIsStillNotCovered() failed after 0.004 seconds with 1 issue.
✘ Test aPrivateKeySetIsCoveredWithoutASetSecret() recorded an issue at SessionListViewModelTests.swift:3182:9: Expectation failed: try vm.setCoversItsLogin(set) == true
✘ Test aWebDAVSetIsCoveredWithoutASecret() recorded an issue at SessionListViewModelTests.swift:3201:9: Expectation failed: try vm.setCoversItsLogin(set) == true
✘ Test aWebDAVSetIsCoveredWithoutASecret() failed after 0.005 seconds with 1 issue.
✘ Test aPrivateKeySetIsCoveredWithoutASetSecret() failed after 0.005 seconds with 1 issue.
✘ Suite "SessionListViewModel" failed after 0.005 seconds with 3 issues.
✘ Test run with 7 tests in 1 suite failed after 0.005 seconds with 3 issues.
```

**Das ist der Fehler des Vorgängers, festgenagelt:** ein `.s3`-Set mit
`authKind: .agent` — die Form, die der Login-Set-Import wörtlich aus der Datei
durchreicht — gilt unter der `authKind`-Frage als *gedeckt*, und aus dieser
Antwort entfernt ein löschender Binder die einzige Kopie des Secret Access
Key. Zwei weitere Zeilen der Spec-Tabelle (privater Schlüssel, WebDAV) fallen
mit.

**Zusätzlich, nicht verlangt, aber lehrreich:** dieselbe Mutation gegen die
**volle** Suite gefahren:

```
✘ Test run with 1667 tests in 130 suites failed after 3.956 seconds with 3 issues.
```

Es sind **genau diese drei** Tests — **kein einziger Jump-Test wird rot.**
Nachgerechnet stimmt das auch: für die vier Set-Gestalten, die ein Jump
praktisch erreicht (Passwort mit/ohne Secret, Agent, privater Schlüssel),
liefern beide Formulierungen dieselbe Antwort; nur der Widerspruch zwischen
`kind` und `authKind` trennt sie, und den kann heute nur ein S3- oder
WebDAV-Set tragen. **Die Deckungsfrage ist damit an genau einer Stelle
abgesichert — im Unit-Test, nicht am löschenden Aufrufer.** Das ist kein
Mangel des Tests, sondern der Grund, warum es ihn gibt: er hält die Antwort
korrekt für jeden künftigen Löschaufrufer, nicht nur für den heutigen.

Rücknahme:

```
$ git checkout -- Sources/macSCPCore/Presentation/SessionListViewModel.swift
$ git status --porcelain
porcelain: []
```

Danach die volle Suite wieder grün (1667, 3,69 s).

### Die zwei Rot-Zustände aus den Task-Berichten, im Wortlaut

Diese beiden wurden in dieser Sitzung **nicht** reproduziert; sie stehen hier
zum Vergleich, gekennzeichnet als übernommen.

**T2 — der verschluckte Read (`try?`) statt des werfenden**, Mutation gegen
`anUnreadableMemberSecretRollsTheMergeBackAndDeletesNothing`: der Test geht
rot mit **verschwundenem Credential** (`storedIDs` leer), nicht mit einem
abweichenden Flag — genau die Forderung der Spec-Testhinweise.

**T3 — den Wächter aus `cleanOrphanedJumpSlot` entfernt:**

```
✘ Test switchingAJumpToASetWithoutASecretKeepsTheBastionSlot() recorded an issue at SessionListViewModelTests.swift:1425:9: Expectation failed: (secrets.peek(jump.secretID) → nil) != nil
✘ Test switchingAJumpToASetWithoutASecretKeepsTheBastionSlot() failed after 0.006 seconds with 1 issue.
✘ Suite "SessionListViewModel" failed after 0.006 seconds with 1 issue.
✘ Test run with 1 test in 1 suite failed after 0.007 seconds with 1 issue.
```

Die Zusicherung **vor** dem Umschalten bleibt grün, die **danach** fällt: der
Slot war da und ist weg — der Verlust selbst.

## Die zehn Erfolgskriterien

Belege zitieren Testnamen, keine Zeilennummern (Lektion aus M27). Jeder
genannte Test wurde für diesen Bericht **gelesen**, nicht nur gezählt.

| # | Kriterium | Ergebnis | Beleg |
|---|---|---|---|
| 1 | `applyMerge` löscht keinen Slot, wenn das Set das Secret nicht hält | **dem Zweck nach erfüllt, dem Wortlaut nach NICHT** — siehe eigener Abschnitt | `memberSlotsHoldingOnlyTheEmptyStringLetTheMergeCarryNothing` und `genuinelyEmptyMemberSlotsStillMerge` zeigen: das Set bekommt keinen Slot, **und die Mitglieds-Slots gehen trotzdem**. Der von der Spec verlangte Nachweis („`storedIDs` aller Mitglieder unverändert") existiert für diesen Fall nicht und soll es nicht |
| 2 | `applyMerge` bricht ab, statt bei nicht lesbarer Keychain zu löschen | **erfüllt** | `anUnreadableMemberSecretRollsTheMergeBackAndDeletesNothing`: Set aus dem Store weg, keine Sitzung umgehängt, `#expect(secrets.storedIDs == Set([a.id, b.id]))` — **Mengengleichheit**, also fiele auch ein Slot unter irgendeiner dritten ID auf. Verstärkt durch `oneUnreadableMemberAbortsTheMergeEvenWhenAnotherMemberAnswers` (nur **ein** Mitglied unlesbar — der Fall, den ein Store, der jeden Read scheitern lässt, nicht zeigen kann) |
| 3 | Die Jump-Bindung löscht den alten Jump-Slot nur bei gedecktem Set | **erfüllt, breiter als verlangt** | **Neun** Tests, je ein Deckungsfall: `switchingAJumpToASetWithoutASecretKeepsTheBastionSlot`, `…ToASetThatHoldsItsSecretDropsTheOldSlot`, `…ToAnAgentSetDropsTheOldSlot`, `…WhileTheKeychainIsSilentKeepsTheBastionSlot`, `…ToAPrivateKeySetWithoutAPassphraseKeepsTheBastionSlot`, `…ToAPrivateKeySetHoldingItsPassphraseDropsTheOldSlot`, `switchingAJumpToSessionModeDropsTheOldSlotDespiteAStaleSetID` und die **zwei aus T5** für das artfremde Set (`…ToAWebDAVSetKeepsTheBastionSlot`, `…ToAnS3SetKeepsTheBastionSlot`). Die zwei bestehenden M10c-Tests (`saveCleansOrphanedJumpSlotWhenJumpRemoved`, `…WhenSwitchingToSetMode`) blieben **unverändert grün** |
| 4 | Alle fünf Konfigurationen werden korrekt unterschieden | **erfüllt** | Sechs Tests für fünf Tabellenzeilen (Passwort doppelt, mit und ohne Secret): `aPasswordSetWithoutASecretIsNotCovered`, `aPasswordSetHoldingASecretIsCovered`, `anAgentSetIsCoveredWithoutReadingTheKeychain`, `aPrivateKeySetIsCoveredWithoutASetSecret`, `anS3SetWithoutASecretIsNotCovered`, `aWebDAVSetIsCoveredWithoutASecret`. Der Agent-Test pinnt zusätzlich die **Reihenfolge** der beiden Zweige: `NoReadAllowedSecretStore` meldet jeden Read als Issue |
| 5 | Ein `.s3`-Set mit `authKind: agent` gilt **nicht** als gedeckt | **erfüllt, in dieser Sitzung durch Mutation nachgeprüft** | `anS3SetDeclaringAgentAuthIsStillNotCovered`; Rot-Ausgabe oben wörtlich |
| 6 | Die Deckungsfrage stellt nie `LoginSet.authKind` | **erfüllt, mit einer Präzisierung** | `setCoversItsLogin` besteht aus zwei Zeilen und nennt `authKind` nicht; die Frage geht über `BackendDescriptor.visibleSecretField(for set:)` ans Schema, und der Doc-Kommentar dort sagt warum. **Präzisierung:** `jumpSetDemonstrablyCoversItsLogin` fragt sehr wohl `set.authKind == .privateKey` — das ist aber nicht die Deckungsfrage, sondern der **zusätzliche** Arm der Löschentscheidung, und er kann nur **verschärfen**, nie freigeben |
| 7 | Ein unbeantwortbarer Deckungstest löscht nicht | **erfüllt** | `anUnreadableKeychainMakesCoverageThrowRatherThanFalse` (wirft statt `false`, und der Bestandsbeweis läuft über `secrets.peek(set.id) != nil`, nie über den Wert) plus `switchingAJumpWhileTheKeychainIsSilentKeepsTheBastionSlot` — dort **hält** das Set sein Secret, die Löschung wäre also sogar richtig, und der Wächter verweigert trotzdem, weil er es nicht feststellen konnte |
| 8 | Der Import nennt die Zahl der Sets ohne Passwort | **im Kern erfüllt, im verlangten Nachweis NICHT** | `importingSetsWithoutSecretsReportsTheirNumber` prüft `result.secretsMissing == 2` über `applyLoginSetImport` — vier Planeinträge, darunter ein Schlüssel-Set ohne Passphrase, das **nicht** zählen darf. Die Spec verlangte „Test über den erzeugten Text"; den gibt es nicht und er kann in dieser Struktur nicht existieren (siehe unten) |
| 9 | Kein Secret-Wert in Meldung, Log oder Testfehlertext | **erfüllt, durch Inspektion** | `LoginMergeSecretConflict.description` ist ein lokalisierter Satz ohne Interpolation („they store different credentials"); der Rollback-Zweig interpoliert ihn in `core.login.mergeFailed %@`. `membersWithDifferentPassphrasesAbortTheMergeAndKeepBothSecrets` prüft eigens, dass die Meldung keinen der beiden Werte enthält — und reduziert vorher auf einen `Bool`, damit die Meldung nicht selbst in einen Fehlerbericht gerät. Die Bestandsbeweise laufen durchgehend über **ID-Mengen** statt über Werte. `git diff … -- Sources` enthält **keine** hinzugefügte `print`/`NSLog`/`logger`-Zeile |
| 10 | Alle vier Kataloge tragen die neuen Schlüssel | **erfüllt, mit ausgewiesener Reichweite** | **Drei** neue Schlüssel, nicht zwei: `logins.import.withoutPassword %lld` und `form.jump.set.notSSH` (T7) in allen vier App-Katalogen, `core.login.mergeConflictingSecrets` in allen vier Core-Katalogen — je Datei per grep ausgezählt. `LocalizableStringsTests` grün, `plutil -lint` auf alle acht `OK`. **Was der Wächter kann und was nicht:** er prüft Parse-Barkeit und **Mengengleichheit** zwischen Englisch und jeder anderen Sprache pro Schicht. Fehlte ein Schlüssel in **allen vier** Katalogen, sähe er nichts. Die Anwesenheit ist hier durch Auszählen belegt, nicht durch den Test |

### Kriterium 1 im Einzelnen: warum der Wortlaut nicht gilt und nicht gelten soll

Die Spec verlangt „Set ohne Secret, Merge läuft, `storedIDs` aller Mitglieder
unverändert". So verhält sich `applyMerge` **nicht**: hält die Gruppe nichts
Übertragbares — kein Slot, oder nur Slots mit `""` —, läuft der Merge durch
**und löscht die Mitglieds-Slots**. `memberSlotsHoldingOnlyTheEmptyStringLet
TheMergeCarryNothing` zeigt genau das (`storedIDs` von `[a, b]` auf leer).

Das ist die im Plan ausgewiesene Abweichung, keine Nachlässigkeit:
**`applyMerge` überträgt**, und was dort gelöscht wird, hält definitionsgemäß
nichts, was verloren gehen könnte. Der Zweck des Kriteriums — kein Verlust —
ist erfüllt; seine Formulierung war auf den falschen Binder gemünzt. Wer den
Wortlaut wörtlich nähme, bekäme leere Slots geschenkt, die nichts schützen.

### Kriterium 8 im Einzelnen: der Text ist nicht testbar

Die neue Zeile entsteht in `LoginSetsSheet.importResultText`, einer `private`
Methode im App-Target `MacSCPApp`. **Das Paket hat genau ein Testtarget,
`macSCPCoreTests`** (`ls Tests/` → nur dieses Verzeichnis). Ein Test über den
erzeugten Text ist damit unmöglich, ohne die Formatierung vorher aus der View
zu ziehen. Dieselbe Struktur, dieselbe Feststellung wie bei M27s Kriterium 10
— und dieselbe Konsequenz: **wer morgen die Zeile umbaut, bricht Kriterium 8,
und nichts wird rot.** Steht unten im Offenen.

## Die Abweichung von der Spec: aus einer Regel wurden zwei

Die Spec formuliert **eine** Regel für beide Binder: „Ein Binder, der löscht,
stellt dieselbe Frage wie der Verbindungspfad." Beim Planen hielt das nicht,
und der Plan hat es ausgewiesen statt geglättet:

- **Die Jump-Bindung** löscht einen Slot, den **nichts überträgt** — der alte
  Bastion-Slot ist die einzige Kopie. Dort ist die Deckungsfrage die richtige.
- **`applyMerge`** überträgt sehr wohl. Sein Defekt war nicht die fehlende
  Frage, sondern der **verschluckte Read**: `try?` macht aus „nicht lesbar"
  ein „nichts da", und danach wird gelöscht. Die Deckungsfrage hätte daran
  nichts verbessert; ein **werfender** Read schon.

**Was das über das Verallgemeinern sagt.** Die Spec war in der Sache richtig
und eine Ebene zu grob. „Beide löschen, also brauchen beide dieselbe Frage"
ist eine Gemeinsamkeit auf der Ebene der *Handlung*, während der Defekt auf
der Ebene der *Herkunft des Werts* sitzt: überträgt hier etwas, oder nicht?
Eine Regel, die zwei Stellen zusammenfasst, muss an dem Merkmal ansetzen, das
den Defekt erzeugt — nicht an dem, das beide teilen. Der Preis wäre nicht
harmlos gewesen: eine Deckungsfrage vor `applyMerge` hätte den werfenden Read
**nicht** ersetzt, und der Verlustweg wäre unter einem plausiblen Wächter
offen geblieben.

**Die zweite Abweichung und ihre Korrektur.** Der Plan schloss aus derselben
Spec-Passage, die Managed-Key-Probe werde „nirgends gebraucht". Das ist wahr
für die **Deckungsfrage** (ein `.privateKey`-Set zeigt `passphrase`, nicht
erforderlich, der „braucht nichts"-Zweig trägt den Fall) und **falsch für eine
Löschentscheidung**: ein Binder, der allein darauf löscht, entfernt den
Passphrase-Slot einer Sitzung, deren Schlüssel verschlüsselt ist und deren Set
nichts hält. **Die Task-1-Review hat das gefangen**, der Plan wurde korrigiert
(`3ce47f7`), und Task 3 kombiniert seither beide Fragen. Eine Behauptung, die
für einen Zweck stimmt, stimmt nicht für jeden Zweck.

## Acht Dinge, die dieser Meilenstein festhält

### 1. Der Plan kannte einen von vier Verlustwegen in `applyMerge`

Gebrieft war der **verschluckte Read**. Die Reviews fanden drei weitere:

1. **Ein leerer String zählte als „hat ein Secret".** `save` legt für jede
   manuelle Nicht-Agent-Sitzung einen Slot an, `""` eingeschlossen — ein
   unverschlüsselter privater Schlüssel hinterlässt genau das. Eine Passphrase
   geht nie in den Gruppierungsschlüssel des Planers ein, das leere Mitglied
   kann also **vor** einem mit echter Passphrase stehen. Es als Quelle zu
   nehmen trug `""` aufs Set und löschte danach die echte Passphrase — die
   einzige Kopie der Gruppe. **Ganz ohne Keychain-Fehler.**
2. **Das Fenster zwischen zwei Reads** (siehe Punkt 2 unten).
3. **Mitglieder mit VERSCHIEDENEN Secrets** wurden stillschweigend auf eines
   zusammengefaltet. Zwei `.privateKey`-Sitzungen mit gleichem Benutzernamen
   und Schlüsselpfad, aber verschiedenen Passphrasen, gruppieren zusammen —
   weder Passphrase noch Host stehen im Gruppierungsschlüssel. Maintainer-
   Entscheidung 2026-08-09: **abbrechen**. Ein Login-Set ist per Definition
   *ein* Zugangsdatum; halten die Mitglieder verschiedene, könnte das Set
   höchstens einem dienen — der Merge erzeugte also einen kaputten Zustand
   **und** vernichtete dabei ein Secret. Die Ablehnung nimmt nichts weg, sie
   lehnt einen Merge ab, der nie funktionieren konnte.

> **Jeder dieser drei wurde von einer Review gefunden, keiner von einem Test
> — auch nicht von den Tests, die in derselben Runde geschrieben wurden.**

Das ist genau das Muster der vier zurückgenommenen Runden, hier zum fünften
Mal. Tests prüfen die Annahmen, unter denen sie geschrieben wurden; ein
Verlustweg, den der Autor nicht gesehen hat, steht in seinen Tests auch nicht.
Die Prüfinstanz muss von außerhalb kommen.

### 2. Der beste Fix beseitigte eine Klasse, statt sie zu bewachen

Der Carry las zweimal: einmal zum Auswählen der Quelle, einmal zum
tatsächlichen Übertragen. Fix-Runde 2 hat daraus **einen** Read gemacht — der
gefundene Wert *ist* der übertragene. Damit fiel

- der TOCTOU-Spalt zwischen den Reads (die Keychain kann darin sperren, der
  Slot sich darin ändern, und die Antwort des **zweiten** Reads zählte),
- der zweite Knopf am Test-Double, den Runde 1 eigens hatte bauen müssen, um
  „Keychain sperrt ZWISCHEN den Reads" überhaupt darstellen zu können,
- und ein Test, der nur noch diesen Knopf prüfte.

Weniger Zeilen, weniger Tests, eine Fehlerklasse weg statt bewacht. **Ein
Wächter braucht künftig einen Wächter; ein beseitigter Zustand nicht.**

### 3. Task 3 hat die Alternative des Briefs verweigert und eine dritte Antwort belegt

Der Brief stellte die Managed-Key-Frage binär: Probe hinzunehmen oder nicht.
Der Implementierer hat aus dem Code eine dritte Antwort hergeleitet und die
Review hat sie nachvollzogen: ein set-gebundener Jump löst über
`LoginResolver.resolveJump` → `sshLogin(from set:)` auf — **genau ein Read,
und zwar des Slots des SETS**. Die beiden Aufrufstellen von
`ManagedKeyPassphrase.resolve` (Connect-Knopf im Formular, Füllpfad in
`ContentView`) schreiben dagegen das **eigene Passwort des Verbindungs-
formulars** aus dessen eigenem Schlüsselpfad. Der Slot eines verwalteten
Schlüssels liegt also **nie** auf dem Jump-Pfad; die Probe hätte den Jump
selbst dann nicht retten können, wenn sie die Passphrase hielte. Der Wächter
fragt deshalb den einzigen Beleg, den es an dieser Stelle gibt — den Slot des
Sets —, und ein leerer heißt „kann ich nicht feststellen", also **nicht
löschen**.

> **Ein Brief, der zwei Optionen anbietet, hat damit nicht bewiesen, dass es
> nur zwei gibt.**

### 4. Kommentardisziplin über den Meilenstein — mit einer Korrektur an der Aufgabenstellung

- **Task 1 war laut Review der erste Durchgang auf diesem Branch ohne einen
  einzigen falschen Kommentar** (die Review prüfte alle zwölf Sachaussagen
  gegen die Funktionen, die sie beschreiben). Übernommen aus dem Review-
  Ergebnis, hier nicht nachgeprüft.
- **Task 2, gegen die Berichte nachgezählt:** der Erstwurf hatte zwei falsche
  Kommentaraussagen (Runde 1 korrigierte sie); **Runde 1 setzte einen falschen
  Satz in den gerade korrigierten Satz** („each of those deletes is a no-op" —
  falsch, seit `""` als „kein Secret" gilt), korrigiert in Runde 2; **Runde 3
  hinterließ vier veraltete oder falsche Aussagen** (u. a. „only `.passphrase`
  groups reach this" und „zwei Carry-Fehler", es sind drei), alle in Runde 4
  korrigiert. **Runde 4 war die erste Kommentarrunde, nach der nichts mehr zu
  korrigieren war**, und die abschließende Re-Review verzichtete auf jede
  Einschränkung: „Can `applyMerge` still delete a member's only copy? no."
- **Korrektur an der Aufgabenstellung dieses Berichts:** dort hieß es, „die
  letzten **zwei** Runden" seien sauber gewesen. Die Berichte tragen das
  nicht — Runde 4 existierte gerade deshalb, weil Runde 3 vier Aussagen
  hinterlassen hatte. Sauber waren **Runde 4 und die Tasks 3 und 4**. Task 3
  hat außerdem eine eigene falsche Aussage (über einen Export-Default) **vor**
  dem Commit selbst gefunden und offengelegt.

Der Befund ist unangenehm konstant: **das Korrigieren eines falschen
Kommentars ist die riskanteste Stelle, an der man einen neuen schreibt.** Wer
einen Satz repariert, hat den Kopf beim alten Fehler und nicht beim neuen
Text.

### 5. Ein Befund jenseits von M28, für den Backlog

**`CoreL10n.string` liefert unter `swift test` für JEDEN Core-Schlüssel den
Schlüssel selbst zurück.** Empirisch belegt hat das die Task-2-Review mit
einem Wegwerf-Paket, das das Bundle-Layout nachbaut; für diesen Bericht
strukturell nachgeprüft: `macSCP_macSCPCore.bundle` liegt in `.build/debug/`,
**nicht** in `macSCPPackageTests.xctest` (`find … -name "*.bundle"` dort ohne
Treffer), und die übrigen zwei Kandidaten von `CoreL10n.bundle`
(`Bundle.main.resourceURL`, `Bundle.main.bundleURL`) zeigen unter
`swiftpm-testing-helper` in dessen eigenes Verzeichnis. Also greift der
`.main`-Fallback, und `localizedString(forKey:value:)` gibt den Schlüssel
zurück.

Folgen:

- Dutzende bestehende `#expect(error == CoreL10n.string(…))`-Zusicherungen
  über mehrere Testdateien vergleichen **Schlüssel mit Schlüssel** und können
  nicht scheitern.
- Nichts in der Suite fände einen Core-Schlüssel, der im Code falsch
  geschrieben ist, in `en.lproj` fehlt, oder dessen Platzhalter nicht zu
  seinen Formatargumenten passen. `LocalizableStringsTests` vergleicht nur
  Schlüsselmengen **zwischen** Sprachen, von der Platte.
- **Der Doc-Kommentar von `CoreL10n` behauptet das Gegenteil** („In the test
  target `Bundle.module`'s own search succeeds normally, so tests exercise the
  same lookup as production code") und ist damit falsch.

Ein Test, der `en.lproj` über `#filePath` einliest und die Schlüssel des Codes
als Teilmenge prüft, würde das schließen und liefe unter `swift test`.
**Eigener Durchgang, nicht M28.**

### 6. Die Jump-Bindung an ein artfremdes Set — gefunden beim Abschluss, geschlossen in T5/T6

Beim Belegen von Kriterium 6 fiel eine Lücke auf. Die erste Fassung dieses
Berichts meldete sie als offen; der Maintainer hat entschieden, sie **im
Meilenstein** zu schließen. Der Befund im Wortlaut von damals:

Der Set-Picker der Jump-Sektion listet **alle** Login-Sets, ungefiltert nach
`kind` (`ConnectionFormView`, `ForEach(sessionList.loginSets)` — der
Set-Picker der Sitzung selbst filtert dagegen mit `.filter { $0.kind == kind }`).
`LoginResolver.resolveJump` nimmt ein set-gebundenes Ziel ebenfalls ohne
`kind`-Prüfung an und reicht es an `sshLogin(from set:)`. Und ein WebDAV-Set
wird von `WebDAVFieldSchema.loginSet` mit `authKind: .password` gebaut.

Damit ergibt sich: ein **WebDAV-Set ohne Secret** ist laut Spec-Tabelle
„gedeckt" (Maintainer-Entscheidung aus M23: anonyme Freigaben bleiben
verbindbar), `setCoversItsLogin` antwortet `true`, der Privatschlüssel-Arm
greift nicht (`authKind == .password`) — **und `cleanOrphanedJumpSlot` löscht
den alten Bastion-Slot.** Der Jump löst danach über den leeren Slot des
WebDAV-Sets auf: Passwort weg, Jump nicht anmeldbar. Dasselbe in anderer Form
für ein **S3-Set mit** Secret: gedeckt, Slot gelöscht, und der Jump
authentifiziert sich fortan mit einem Secret Access Key als SSH-Passwort.

**Das ist genau die Fehlerklasse, gegen die M28 gebaut ist**, überlebt für
artfremde Bindungen — und aus demselben Grund wie die Managed-Key-Lücke, die
Task 3 geschlossen hat: die Deckungsfrage sagt „braucht **dieses Set** ein
Secret", während die Löschstelle „verliert **dieser Login** etwas, das er
braucht" wissen muss. Für den privaten Schlüssel wurde die Unterscheidung
gezogen, für den Fall `set.kind != .ssh` nicht.

**Geschlossen an allen drei Stellen, die der Befund als Kandidaten nannte** —
weil sie verschiedene Zustände abdecken und keine die andere ersetzt:

- **T5** (`28002dd`): ein dritter Arm im Wächter. `jumpSetDemonstrablyCovers
  ItsLogin` verweigert die Löschung, wenn `set.kind != .ssh` — der Bastion-Slot
  bleibt, egal wie „gedeckt" das artfremde Set aussieht.
- **T6** (`9cc5a78`): ein Wurf im Resolver (`LoginResolveError.jumpSetNotSSH`)
  plus `JumpLoginSetEligibility` als `kind`-Filter für den Picker. Der Filter
  regelt, was **neu** gewählt werden kann; der Wurf deckt, was **schon auf der
  Platte steht** und den Filter nie passiert hat.
- **T7** (`f9c9979`): derselbe `kind`-Wächter vor dem App-seitigen Füllpfad —
  siehe Abschnitt 7, denn dort saß das eigentliche Loch.

### 7. Der fünfte Credential-Pfad — was nur eine Whole-Branch-Review finden konnte

Die Whole-Branch-Review über `d657995..9cc5a78` meldete **Critical**: ein
WebDAV- oder S3-Passwort konnte einen SSH-Bastion-Host erreichen. Nicht ein
Verlust — eine **Preisgabe**, und damit schwerer als alles, wogegen M28
gebaut wurde.

Die Kette lief quer über beide Schichten und über vier Zustände:

1. Eine Sitzung, deren Jump an ein artfremdes Set gebunden ist — als Altlast,
   oder frisch erzeugt, weil der Login-Set-Import beim Ersetzen `id:
   match.id` wiederverwendet, während `fileSet.kind` ungeprüft durchgereicht
   wird: ein Import kann ein bestehendes SSH-Set unter derselben ID zu
   `.webdav` machen, auf die ein Jump noch zeigt.
2. `ContentView.resolveSelectedJumpLoginSet` → `fillJumpForm` las den
   Keychain-Slot des Sets **ohne `kind`-Prüfung** ins Formular. Kein
   `LoginResolver`, also auch kein `.jumpSetNotSSH`.
3. Ein an der Validierung gescheiterter Submit ließ die Werte stehen
   (`endEditing()` läuft auf diesem Pfad nicht).
4. Der Jump-Login-Modus war eine **blanke gespeicherte Property**, ohne die
   Aufräumregel, die `selectJumpSourceMode` und `selectJumpAuthChoice` beide
   haben — deren eigener Doc-Kommentar beschreibt wörtlich diesen Schaden.
   Umschalten auf „Manuell", speichern: `buildJumpSpec` schreibt
   `loginSetID: nil` und eine frische `secretID`, und das WebDAV-Passwort
   liegt im **eigenen** Jump-Slot der Sitzung. Ab da sieht `resolveJump` kein
   Set mehr, und die in T6 gebaute Verweigerung feuert nie.

Geschlossen in T7 an beiden Hälften: `kind`-Wächter vor dem Füllen
(`f9c9979`), Aufräumen beim Moduswechsel (`b42ba58`), Picker über den
Umschalter geführt (`e78ae99`).

**Warum ihn keine Task-Review sah.** Jede der sieben Task-Reviews prüfte
ihren eigenen Diff. Der Pfad besteht aber aus vier Teilen, von denen **keiner
für sich falsch ist**: ein Füllpfad ohne `kind`-Frage (harmlos, solange
Bindungen artgleich sind), ein Submit, der bei Fehlschlag das Formular
behält (gewollt), eine Property ohne Aufräumen (unauffällig neben zwei
Geschwistern, die aufräumen), ein Save, der manuelle Werte persistiert
(korrekt). Erst der Diff **als Ganzes** zeigt die Kette. Das ist derselbe
Befund wie in M24–M27, hier zum fünften Mal: **die Whole-Branch-Review ist
nicht die Wiederholung der Task-Reviews, sie sieht eine andere Klasse.**

### 8. Das App-Target hat kein Testtarget — und das ist hier keine Formalie

Beim Absichern von Abschnitt 7 wurde der `kind`-Wächter probeweise **ganz
entfernt**. Die volle Suite blieb **grün**. `Tests/` enthält genau ein
Verzeichnis (`macSCPCoreTests`); `MacSCPApp` ist ein ausführbares Target ohne
Testtarget. Zwei der drei T7-Verdrahtungen sind damit **strukturell nicht
pinnbar**.

Ersatzweise ist die Core-Seite festgenagelt: `JumpLoginSetEligibility.
isEligible`, der Moduswechsel bis in den Jump-Slot der Sitzung hinein, und
seit `360dc2d` die **Kopplung** zwischen dem Prädikat und dem `kind`-Wächter
in `LoginResolver.resolveJump` — zwei Codestellen, die eine Frage
beantworten, hielt vorher nichts zusammen. Die Gegenprobe (Resolver-Wächter
auf `.ssh || .webdav` aufgeweicht) macht ihn rot:

```
✘ Test theResolverRefusesExactlyTheSetsThePredicateRejects() recorded an issue at JumpLoginSetEligibilityTests.swift:65:13: Expectation failed: (resolverRefused → false) == (!JumpLoginSetEligibility.isEligible(set) → true)
```

Was bleibt: die zwei View-Verdrahtungen. **Kein Test verhindert, dass jemand
den `kind`-Wächter aus `resolveSelectedJumpLoginSet` entfernt.** Das gehört
in den Backlog, nicht in eine Fußnote — es ist dieselbe Struktur, die schon
Kriterium 8 unpinnbar macht.

## Zurückgestellte Kleinigkeiten (aus dem Ledger, zur Triage)

- **Agent-Gruppe mit widersprüchlichen Resten.** Eine ssh-agent-Gruppe hat gar
  kein sichtbares Secret-Feld; ihre Mitglieder sollen keinen Slot haben, aber
  `save` löscht mit `try?`, ein abgelehnter Delete lässt einen zurück. Halten
  zwei solche Reste verschiedene Werte, lehnt der Merge heute mit „sie
  speichern unterschiedliche Zugangsdaten" ab — für ein Login, das gar keine
  hat. **Sichere Richtung, verwirrende Meldung.** Der saubere Fix ist dieselbe
  Schemafrage, die Task 1 gebaut hat.
- **`applyImport` löscht den alten Jump-Slot eines ersetzten Datensatzes
  weiterhin bedingungslos**, wenn der Planer eine frische `secretID` vergibt.
  Gleiche Gestalt wie der gerade behobene Defekt — mit dem Unterschied, dass
  der Nutzer dort dem Ersetzen zugestimmt hat.
- **Erledigt in `603c3dc`:** der über einen Zeilenumbruch geteilte
  Testbezeichner (`saveJumpSwitchingManualToSessionDeletes` /
  `JumpSecretSlot`) und drei Zeilennummern-Zitate in Kommentaren.
- **`deleteLoginSet` meldet einen gescheiterten Read an der falschen Stelle.**
  Der Abbruch liefert `secretFailures: 0`, und `LoginSetsSheet.deleteSelected`
  setzt seine Inline-Meldung nur bei `secretFailures > 0`. Im Sheet sieht die
  Löschung deshalb wie ein stiller No-Op aus; die rote Zeile erscheint über
  `errorMessage` in der Sidebar, also **erst nach dem Schließen**. Nichts geht
  verloren, aber die Meldung steht nicht dort, wo der Nutzer hinsieht —
  dieselbe Lücke hat der bestehende Store-Fehlerpfad schon.
- **Der Ziel-Picker (nicht der Jump-Picker) hat keinen `kind`-Wächter.**
  `ConnectionFormView`s Login-Modus-Umschalter für das **Verbindungsziel** ist
  eine rohe Bindung, und `resolveSelectedLoginSet` fragt nicht nach `kind`.
  Heute folgenlos, aber nur durch einen Namensraum-Zufall: das Secret eines
  artfremden Sets landet unter dessen eigenem Präfix (`webdav.password`), das
  ein `.ssh`-Formular nie liest und `descriptor.apply` nie persistiert.
  **Sicher aus Versehen, nicht aus Konstruktion.**
- **Die FR- und PL-Übersetzungen** der drei neuen Schlüssel sind vom
  Implementierer und **nicht muttersprachlich geprüft** — derselbe stehende
  Vorbehalt wie für das übrige FR/PL in diesem Projekt.
- **Aus T1:** kein Test pinnt die wörtliche/ungetrimmte Regel für ein Secret
  aus reinem Weißraum; die „kein Read"-Reihenfolge ist nur für die Agent-Zeile
  gepinnt, nicht für die zwei Zeilen mit optionalem Secret.

## Für die Release-Notes

**Ein Satz** (wörtlich aus der Spec):

> Ein gespeichertes Passwort wird nicht mehr entfernt, wenn eine Verbindung
> oder ein Sprungserver auf ein Login umgestellt wird, das selbst keines
> hinterlegt hat.

## Was offen bleibt

Gegen den Branch geprüft, nicht aus M27 kopiert.

- **Der veraltete Slot einer set-gebundenen Sitzung.** Ziel des
  zurückgenommenen Fixes, bewusst nicht Teil von M28. Wer ihn angeht, hat mit
  `setCoversItsLogin` die Bedingung, die er dafür braucht — aber es ist ein
  eigener Durchgang mit eigener Entscheidung.
- **Die Editor-Reibung:** der Login-Set-Editor verlangt beim Bearbeiten das
  Secret erneut, bevor Speichern freigeschaltet wird, selbst wenn nur der Name
  geändert wird. Echter Befund, eigener Fix.
- **Der `CoreL10n`-Befund** (Abschnitt 5 oben), samt seinem falschen
  Doc-Kommentar.
- **Das App-Target hat kein Testtarget** (Abschnitt 8 oben). Betrifft
  Kriterium 8 (`importResultText` in der View) **und** die zwei
  T7-Verdrahtungen. Der gemeinsame Fix ist derselbe: entweder ein Testtarget
  für `MacSCPApp`, oder die betroffene Logik in Core ziehen. Nach diesem
  Meilenstein der stärkste Kandidat für den nächsten Durchgang — der einzige
  Critical dieses Branches lag in genau diesem blinden Fleck.
- **Der Ziel-Picker ohne `kind`-Wächter** (siehe Kleinigkeiten oben).
- **Ein app-weiter Audit-Bereich** ohne Sitzungsbezug. Unverändert auf dem
  Backlog.
- **Bestehende Waisen einsammeln** braucht eine Keychain-Enumeration; drei
  Meilensteine haben sie inzwischen bewusst abgelehnt.
- **Der 0-%-CPU-Testsuite-Hänger.** Eigene Akte
  (`2026-08-08-testsuite-haenger-untersuchung.md`); trat hier in keinem der
  fünf Läufe auf.
- **Der Release-Stau:** **385** Commits vor `origin/main` (M27 nannte 348, M26
  323). Weiter gewachsen.

## Der Review-Verlauf des Meilensteins

| Stufe | Ergebnis |
|---|---|
| Sieben Task-Reviews | fanden drei der vier `applyMerge`-Verlustwege und die Managed-Key-Präzisierung |
| Whole-Branch-Review (`d657995..9cc5a78`) | **Fix first** — 1 Critical (Abschnitt 7), 3 Important, 5 Minor |
| Fix-Welle (`b42ba58..603c3dc`) | alle vier Code-Befunde geschlossen, vier Mutationsproben rot-und-zurückgenommen |
| Scoped Re-Review (`9cc5a78..603c3dc`) | **Ship** — kein Critical, kein Important; drei Minor, zwei davon in `360dc2d` erledigt |

Die Re-Review hat alle zwölf Sachaussagen der Fix-Welle einzeln gegen die
Funktion geprüft, die sie beschreiben: **keine falsch.** Das ist auf diesem
Branch der erste Durchgang mit dieser Bilanz — nach über zwanzig falschen
Aussagen in acht Durchgängen, mehrere davon **beim Reparieren** falscher
Aussagen entstanden.

## Was in diesem Bericht NICHT verifiziert ist

- **Die Rot-Zustände von T2 und T3.** Nur die Gegenprobe zu Kriterium 5 (bei
  HEAD `1b8802c`) und die Kopplungsprobe aus Abschnitt 8 (bei HEAD `360dc2d`)
  wurden selbst erzeugt; die beiden anderen stehen oben als **übernommen**
  gekennzeichnet, ebenso die vier Mutationsproben der Fix-Welle (im
  Implementierer-Bericht `final-fix-report.md` wörtlich).
- **Die Laufzeitwirkung in der GUI.** Die App wurde **nicht** gestartet.
  Weder die neue Import-Zeile noch die neue Jump-Fehlermeldung
  (`form.jump.set.notSSH`) sind **beobachtet** — alle Aussagen zur App-Schicht
  stützen sich auf Quelltext und Katalogprüfung. Für die zwei ungepinnten
  Verdrahtungen aus Abschnitt 8 ist das die **einzige** verbleibende
  Prüfmöglichkeit.
- **Dass der Testsuite-Hänger nicht mehr auftritt** — er trat hier nicht auf,
  mehr ist damit nicht gesagt.
