# M18 — UI-Nachbesserungen Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Geteilte Sheet-Suche, SSH-Keys-Verwaltung als eigenes Sheet (mit Import + privatem-Key-Export), Login-Set-Zeilen-Kontextmenü und ein Settings-Fenster als Sidebar-Navigation mit Protokoll-Bereichen.

**Architecture:** Fast ausschließlich App-Layer (SwiftUI). Die Suche baut auf dem vorhandenen, getesteten Core-`FileSearch` auf (nicht geändert). Der einzige neue Core-Baustein ist ein SSH-Key-Import-Helfer (shellt `ssh-keygen` wie M17). Die SSH-Key-Kernlogik (`ManagedKeyStore`/`SSHKeyGenerator`/Keychain) und alle M17-Sicherheits-Invarianten bleiben unverändert.

**Tech Stack:** Swift (SwiftPM, `.swiftLanguageMode(.v5)`), SwiftUI + AppKit, Swift Testing, macOS 15+, System-`ssh-keygen`.

## Global Constraints

- Swift `.swiftLanguageMode(.v5)`, minimum macOS 15; **keine neue externe Dependency**.
- `FileSearch` (Core) wird nur **genutzt**, nicht geändert.
- **SSH-Key-Invarianten aus M17 unverändert:** Privatdateien **0600** / Ordner **0700**, Passphrase **nur** Keychain unter `key.id`, **kein** `~/.ssh`-Schreiben, `ssh-keygen` per **Argument-Array**, `SSHPrivateKeyLoader` unverändert.
- Privater-Schlüssel-Export **nur hinter Bestätigung + Warnung**; Import **räumt bei Fehlschlag auf** (kopierte Datei + Keychain-Slot); Key-Bytes **nie** geloggt.
- UI-Strings EN/DE/FR/PL mit **typografischen Zeichen** in nicht-englischen Werten (kein ASCII `"`/`'`), FR/PL KI-generiert.
- **NavigationSplitView-Settings vor Auslieferung per Idle-CPU-Rauchtest** prüfen (M11n-Lektion: Layout-Container auf macOS 26 können in eine Endlosschleife laufen).
- Conventional Commits (CI-Gate); Footer: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

**Verankerte Fakten (verifiziert):** `FileSearch.compile(query: String, isRegex: Bool) -> Result<FileSearchPredicate, FileSearchError>`; `FileSearchPredicate.matches(_ name: String) -> Bool` (`Sources/macSCPCore/Presentation/FileSearch.swift:51,97`). Verwaltungs-Sheets werden via `TabCommands`-Brücke geöffnet: `showKnownHosts`/`showLogins`/`showHiddenImports` (`MacSCPApp.swift:20-25`, Menüpunkte ~214-226) → `@State showKnownHostsSheet`/`showLoginSetsSheet` in `ContentView` (~227-241) → `.sheet(isPresented:)` (~696-698). `LoginSetsSheet`: List (~59-61), `row(_:)` (~133-154), Footer New/Edit/Delete (~71-90), `LoginSetEditorTarget`/`.sheet(item:)` (~31-34, 97-106), `selectedID` (~16). `KnownHostsSheet`: Table + eigenes Suchfeld (`filteredRows` ~47-53, TextField ~65-68). `SessionSidebar` Zeilen-`.contextMenu` (~461-478). `SettingsView`: TabView 6 Tabs (~27-80), `.frame(460×460)` (~79). SSH-Key-Kern: `ManagedKeyStore(directory:)` (`all/add/remove(id:secrets:)/key(forPath:)/keyDirectory`), `SSHKeyGenerator.generate`, `ManagedKey`/`KeyType`, `KeychainSecretStore()`; M17-`SSHKeysSettingsTab.swift` als Extraktions-Quelle. `SSHPrivateKeyLoader.authentication(username:keyPath:passphrase:)`. `ssh-keygen`-Process-Vorlage: `SSHKeyGenerator.swift`.

---

## File Structure

- `Sources/MacSCPApp/SheetSearchField.swift` — **create**: geteilte Suchleiste.
- `Sources/MacSCPApp/LoginSetsSheet.swift` — **modify**: Suche + Zeilen-Kontextmenü.
- `Sources/MacSCPApp/KnownHostsSheet.swift`, `HiddenImportsSheet.swift`, `AuditLogSheet.swift` — **modify**: Suche.
- `Sources/macSCPCore/SSH/SSHKeyImporter.swift` — **create**: Import-Helfer + Test.
- `Sources/MacSCPApp/SSHKeysSheet.swift` — **create**: SSH-Keys-Verwaltungs-Sheet (aus M17-Tab).
- `Sources/MacSCPApp/SettingsView.swift` — **modify**: SSH-Keys-Tab entfernen, Sidebar-Navigation, Protokoll-Bereiche.
- `Sources/MacSCPApp/ContentView.swift`, `MacSCPApp.swift` — **modify**: `showSSHKeys`-Brücke + Menüpunkt + `.sheet`.
- `Sources/MacSCPApp/ConnectionFormView.swift` — **modify**: „Schlüssel verwalten…" → Sheet.
- `Sources/MacSCPApp/Resources/{en,de,fr,pl}.lproj/Localizable.strings` — **modify**.

---

## Task 1: Geteilte Such-Komponente `SheetSearchField` + Referenz-Einbindung (Login-Sets)

**Files:**
- Create: `Sources/MacSCPApp/SheetSearchField.swift`
- Modify: `Sources/MacSCPApp/LoginSetsSheet.swift`
- Modify: `Sources/MacSCPApp/Resources/{en,de,fr,pl}.lproj/Localizable.strings`

**Interfaces:**
- Consumes: `FileSearch.compile(query:isRegex:)`, `FileSearchPredicate.matches(_:)`, `L10n.string`, `DesignTokens`.
- Produces: `struct SheetSearchField: View { init(text: Binding<String>, isRegex: Binding<Bool>, errorText: String?) }` (kompakt: TextField + Regex-Toggle + optionaler Fehlertext).

Reine SwiftUI — build-verifiziert.

- [ ] **Step 1: `SheetSearchField` schreiben**

`Sources/MacSCPApp/SheetSearchField.swift`:

```swift
import SwiftUI
import macSCPCore

/// A compact, reusable search bar for management sheets (M18). Wraps a text
/// field, a regex toggle, and an inline error label. The host view compiles a
/// `FileSearchPredicate` from `text`/`isRegex` (via `FileSearch.compile`) and
/// filters its own list — this view is presentation only.
struct SheetSearchField: View {
    @Binding var text: String
    @Binding var isRegex: Bool
    /// Non-nil when the current regex is invalid (host passes it in).
    var errorText: String?

    var body: some View {
        HStack(spacing: 8) {
            Image(systemName: "magnifyingglass")
                .foregroundStyle(DesignTokens.inkTertiary)
            TextField(
                L10n.string("search.placeholder", "Search"), text: $text)
                .textFieldStyle(.roundedBorder)
            Toggle(L10n.string("search.regex", "Regex"), isOn: $isRegex)
                .toggleStyle(.checkbox)
                .help(L10n.string("search.regex.help", "Match with a regular expression"))
            if let errorText {
                Text(errorText)
                    .font(.caption)
                    .foregroundStyle(.red)
                    .lineLimit(1)
            }
        }
    }
}
```

- [ ] **Step 2: Ein kleiner Host-Helfer (Predicate + Fehlertext)**

Damit jedes Sheet konsistent kompiliert, in `SheetSearchField.swift` einen freien Helfer ergänzen:

```swift
/// Compiles a predicate for a sheet's search state. Returns the predicate to
/// filter with (matches-all on empty/whitespace) and a localized error string
/// when the regex is invalid (so nothing is silently filtered to zero rows).
func sheetSearchPredicate(text: String, isRegex: Bool) -> (FileSearchPredicate, String?) {
    switch FileSearch.compile(query: text, isRegex: isRegex) {
    case .success(let predicate):
        return (predicate, nil)
    case .failure:
        // Invalid regex: match everything and show an error, rather than
        // hiding all rows behind a typo.
        let all = (try? FileSearch.compile(query: "", isRegex: false).get())
            ?? FileSearch.matchAll
        return (all, L10n.string("search.regex.invalid", "Invalid regular expression"))
    }
}
```

Prüfen: bietet `FileSearchPredicate` eine `matchAll`-Konstante oder liefert `compile("", isRegex:false)` einen Alles-Matcher? Der Datei-`FileSearch.swift` sagt: leeres Query → matcht alles. Nutze `FileSearch.compile(query: "", isRegex: false)` als Alles-Matcher (kann nicht fehlschlagen); entferne die `FileSearch.matchAll`-Zeile, falls es die Konstante nicht gibt. Kein erfundenes API.

- [ ] **Step 3: In `LoginSetsSheet` einbauen**

In `LoginSetsSheet`:
- Zwei `@State` ergänzen: `@State private var searchText = ""`, `@State private var searchIsRegex = false`.
- Über der `List` (vor Zeile ~52) das Suchfeld einsetzen:

```swift
            let (predicate, searchError) = sheetSearchPredicate(
                text: searchText, isRegex: searchIsRegex)
            SheetSearchField(text: $searchText, isRegex: $searchIsRegex, errorText: searchError)
                .padding(.bottom, 4)
```

- Die `List`-Quelle filtern: statt `sessionList.loginSets` eine gefilterte lokale Ableitung nutzen:

```swift
            let visibleSets = sessionList.loginSets.filter {
                predicate.matches("\($0.name) \($0.username) \($0.accessKeyID ?? "")")
            }
            List(visibleSets, selection: $selectedID) { set in row(set) }
```

(Die Empty-State-Bedingung entsprechend auf `visibleSets.isEmpty` beziehen, falls die Sheet heute auf `loginSets.isEmpty` prüft.)

- [ ] **Step 4: L10n**

Neue Keys in alle vier Kataloge (`search.placeholder`, `search.regex`, `search.regex.help`, `search.regex.invalid`). EN:
```
"search.placeholder" = "Search";
"search.regex" = "Regex";
"search.regex.help" = "Match with a regular expression";
"search.regex.invalid" = "Invalid regular expression";
```
DE:
```
"search.placeholder" = "Suchen";
"search.regex" = "Regex";
"search.regex.help" = "Mit regulärem Ausdruck suchen";
"search.regex.invalid" = "Ungültiger regulärer Ausdruck";
```
FR:
```
"search.placeholder" = "Rechercher";
"search.regex" = "Regex";
"search.regex.help" = "Filtrer avec une expression régulière";
"search.regex.invalid" = "Expression régulière invalide";
```
PL:
```
"search.placeholder" = "Szukaj";
"search.regex" = "Regex";
"search.regex.help" = "Dopasuj wyrażeniem regularnym";
"search.regex.invalid" = "Nieprawidłowe wyrażenie regularne";
```

- [ ] **Step 5: Build + Parität**

Run: `swift build && swift test --filter Localizable`
Expected: 0 (neue) Warnungen; Katalog-Parität grün. Verhalten (Codelesen): Tippen filtert die Login-Set-Liste live; Regex-Toggle + Fehlertext bei ungültigem Muster.

- [ ] **Step 6: Commit**

```bash
git add Sources/MacSCPApp/SheetSearchField.swift Sources/MacSCPApp/LoginSetsSheet.swift Sources/MacSCPApp/Resources
git commit -m "feat: add a shared search field and wire it into the login sets sheet

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 2: Suche in Known-Hosts / Hidden-Imports / Audit-Log

**Files:**
- Modify: `Sources/MacSCPApp/KnownHostsSheet.swift`, `HiddenImportsSheet.swift`, `AuditLogSheet.swift`

**Interfaces:**
- Consumes: `SheetSearchField`, `sheetSearchPredicate(text:isRegex:)` (Task 1).

Reine SwiftUI — build-verifiziert.

- [ ] **Step 1: Known-Hosts auf `SheetSearchField` vereinheitlichen**

In `KnownHostsSheet` das bestehende Ad-hoc-Suchfeld (`TextField(..., text: $searchText)` ~65-68) durch `SheetSearchField(text: $searchText, isRegex: $searchIsRegex, errorText: searchError)` ersetzen; ein `@State searchIsRegex = false` ergänzen; `filteredRows` (~47-53) auf den Predicate umstellen:

```swift
    private var filteredRows: [KnownHostRow] {
        let (predicate, _) = sheetSearchPredicate(text: searchText, isRegex: searchIsRegex)
        return allRows.filter { predicate.matches("\($0.key.host) \($0.key.fingerprint)") }
    }
```

Den `searchError` fürs Label separat ableiten (zweiter Aufruf von `sheetSearchPredicate` im View-Body oder eine gemeinsame `@State`); die genaue Feldnamen (`allRows`/`key.host`/`key.fingerprint`) an den realen Code anpassen.

- [ ] **Step 2: Hidden-Imports-Suche**

In `HiddenImportsSheet` (`List(rows)` ~81) zwei `@State` (`searchText`/`searchIsRegex`), `SheetSearchField` über der Liste, und die Liste auf `rows.filter { predicate.matches(searchString($0)) }` filtern — `searchString` = Alias/Host des `HiddenImportRow` (reale Felder aus `HiddenImportRow` ~28 übernehmen).

- [ ] **Step 3: Audit-Log-Suche**

In `AuditLogSheet` dasselbe Muster; `searchString` = die angezeigte Event-Zusammenfassung (Aktion + Host/Ziel + ggf. Zeit) — die realen Felder aus der Audit-Row/dem Event übernehmen, so wie sie heute in der Zeile gerendert werden.

- [ ] **Step 4: Build**

Run: `swift build && swift test --filter Localizable`
Expected: 0 neue Warnungen; Parität grün. Verhalten: alle vier Sheets filtern konsistent; Known-Hosts verhält sich wie zuvor plus Regex.

- [ ] **Step 5: Commit**

```bash
git add Sources/MacSCPApp/KnownHostsSheet.swift Sources/MacSCPApp/HiddenImportsSheet.swift Sources/MacSCPApp/AuditLogSheet.swift
git commit -m "feat: add search to the known-hosts, hidden-imports and audit sheets

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 3: Login-Set-Zeilen-Kontextmenü

**Files:**
- Modify: `Sources/MacSCPApp/LoginSetsSheet.swift` (`row(_:)` ~133-154)

**Interfaces:**
- Consumes: die bestehenden Edit-/Delete-Auslöser (`editorTarget`, Lösch-`confirmationDialog`-State), `selectedID`.

Reine SwiftUI — build-verifiziert.

- [ ] **Step 1: `.contextMenu` an der Zeile**

In `row(_:)` am äußeren Zeilen-Container ein `.contextMenu` ergänzen, das die Zeile zuerst selektiert und dieselben Aktionen wie die Footer-Buttons auslöst:

```swift
        .contextMenu {
            Button(L10n.string("loginSets.edit", "Edit…")) {
                selectedID = set.id
                editorTarget = LoginSetEditorTarget(existing: set)
            }
            Button(L10n.string("loginSets.delete", "Delete…"), role: .destructive) {
                selectedID = set.id
                isShowingDeleteConfirm = true   // <-- realen Lösch-State-Namen einsetzen
            }
        }
```

Die realen Namen einsetzen: den Edit-Auslöser (wie der „Edit…"-Footer-Button `editorTarget` setzt — dessen Konstruktor/Signatur übernehmen) und den Delete-Auslöser (wie der „Delete…"-Footer-Button den `confirmationDialog` öffnet — denselben `@State`-Bool). Die L10n-Keys der Footer-Buttons wiederverwenden (kein neuer Key).

- [ ] **Step 2: Build + Behaviour-Check**

Run: `swift build`
Expected: 0 neue Warnungen. Verhalten: Rechtsklick auf eine Zeile → Bearbeiten…/Löschen…; beide treffen den rechtsgeklickten Eintrag (Selektion wird zuerst gesetzt); Footer-Buttons unverändert.

- [ ] **Step 3: Commit**

```bash
git add Sources/MacSCPApp/LoginSetsSheet.swift
git commit -m "feat: add an edit/delete context menu to login set rows

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 4: Core — SSH-Key-Import-Helfer

**Files:**
- Create: `Sources/macSCPCore/SSH/SSHKeyImporter.swift`
- Test: `Tests/macSCPCoreTests/SSHKeyImporterTests.swift`

**Interfaces:**
- Consumes: `ManagedKey`/`KeyType`, `HostKeyFingerprint`, `SSHPrivateKeyLoader`, Foundation `Process`.
- Produces: `public enum SSHKeyImporter { public static func inspect(privateKeyURL: URL, passphrase: String?) throws -> ImportedKeyInfo }` mit `public struct ImportedKeyInfo { public let type: KeyType; public let fingerprint: String; public let publicKeyOpenSSH: String }` und `public enum SSHKeyImportError: Error, Equatable { case unreadable; case unsupportedOrEncrypted; case toolMissing }`.

Hinweis: Das eigentliche Kopieren in `keyDirectory` + `ManagedKey`/Keychain-Anlage macht die App (Task 5), weil dort `newID`/Name/Kommentar/Passphrase-Eingabe leben. Dieser Core-Helfer LIEST nur Typ/Fingerprint/Public-Key aus einer vorhandenen Datei (via `ssh-keygen`), damit die App valide Metadaten bauen kann.

- [ ] **Step 1: Failing-Test**

`Tests/macSCPCoreTests/SSHKeyImporterTests.swift`:

```swift
import Foundation
import Testing
@testable import macSCPCore

@Suite("SSHKeyImporter")
struct SSHKeyImporterTests {
    private func tempDir() -> URL {
        let dir = URL(fileURLWithPath: NSTemporaryDirectory())
            .appendingPathComponent("macscp-import-\(UUID().uuidString)")
        try! FileManager.default.createDirectory(at: dir, withIntermediateDirectories: true)
        return dir
    }

    private func keygen(_ args: [String]) {
        let p = Process()
        p.executableURL = URL(fileURLWithPath: "/usr/bin/ssh-keygen")
        p.arguments = args
        p.standardInput = FileHandle.nullDevice
        try! p.run(); p.waitUntilExit()
        #expect(p.terminationStatus == 0)
    }

    @Test func inspectsAnUnencryptedEd25519Key() throws {
        let dir = tempDir(); defer { try? FileManager.default.removeItem(at: dir) }
        let key = dir.appendingPathComponent("id_ed25519")
        keygen(["-t", "ed25519", "-f", key.path, "-N", "", "-q", "-C", "import-test"])

        let info = try SSHKeyImporter.inspect(privateKeyURL: key, passphrase: nil)
        #expect(info.type == .ed25519)
        #expect(info.fingerprint.hasPrefix("SHA256:"))
        #expect(info.publicKeyOpenSSH.hasPrefix("ssh-ed25519 "))
        // The inspected key must load through the existing loader.
        _ = try SSHPrivateKeyLoader.authentication(username: "t", keyPath: key.path, passphrase: nil)
    }

    @Test func encryptedKeyNeedsThePassphrase() throws {
        let dir = tempDir(); defer { try? FileManager.default.removeItem(at: dir) }
        let key = dir.appendingPathComponent("id_ed25519")
        keygen(["-t", "ed25519", "-f", key.path, "-N", "s3cr3t", "-q"])

        // Wrong/empty passphrase: public-key derivation (ssh-keygen -y) fails.
        #expect(throws: (any Error).self) {
            _ = try SSHKeyImporter.inspect(privateKeyURL: key, passphrase: nil)
        }
        let info = try SSHKeyImporter.inspect(privateKeyURL: key, passphrase: "s3cr3t")
        #expect(info.type == .ed25519)
    }
}
```

- [ ] **Step 2: Test rot**

Run: `swift test --filter SSHKeyImporterTests`
Expected: FAIL — „cannot find 'SSHKeyImporter'".

- [ ] **Step 3: `SSHKeyImporter` implementieren**

`Sources/macSCPCore/SSH/SSHKeyImporter.swift`:

```swift
import Foundation

/// Inspects an existing OpenSSH private key file so the app can import it as a
/// managed key (M18). Shells out to the system `ssh-keygen` (argument array,
/// never a shell string) to derive the public key (`-y`) and fingerprint
/// (`-l`). Reads only — copying the file into the key store and creating the
/// `ManagedKey`/Keychain entry is the app's job (it owns the id/name/comment).
public enum SSHKeyImporter {
    public struct ImportedKeyInfo: Equatable, Sendable {
        public let type: KeyType
        public let fingerprint: String
        public let publicKeyOpenSSH: String
    }

    public enum SSHKeyImportError: Error, Equatable {
        case unreadable
        case unsupportedOrEncrypted
        case toolMissing
    }

    public static func inspect(privateKeyURL: URL, passphrase: String?) throws -> ImportedKeyInfo {
        let tool = "/usr/bin/ssh-keygen"
        guard FileManager.default.isExecutableFile(atPath: tool) else {
            throw SSHKeyImportError.toolMissing
        }
        // Public key via `ssh-keygen -y -P <pass> -f <file>` (stdout).
        let pub = try run(tool, ["-y", "-P", passphrase ?? "", "-f", privateKeyURL.path(percentEncoded: false)])
            .trimmingCharacters(in: .whitespacesAndNewlines)
        guard pub.contains(" ") else { throw SSHKeyImportError.unsupportedOrEncrypted }
        // Fingerprint via `ssh-keygen -l -f <file>` → "<bits> SHA256:… comment (TYPE)".
        let lf = try run(tool, ["-l", "-f", privateKeyURL.path(percentEncoded: false)])
        let fingerprint = lf.split(separator: " ").first { $0.hasPrefix("SHA256:") }.map(String.init)
            ?? { throw SSHKeyImportError.unreadable }()
        return ImportedKeyInfo(
            type: keyType(fromOpenSSHPublicKey: pub),
            fingerprint: fingerprint,
            publicKeyOpenSSH: pub)
    }

    private static func run(_ tool: String, _ args: [String]) throws -> String {
        let p = Process()
        p.executableURL = URL(fileURLWithPath: tool)
        p.arguments = args
        let out = Pipe()
        p.standardOutput = out
        p.standardError = FileHandle.nullDevice
        p.standardInput = FileHandle.nullDevice
        do { try p.run() } catch { throw SSHKeyImportError.toolMissing }
        p.waitUntilExit()
        guard p.terminationStatus == 0 else { throw SSHKeyImportError.unsupportedOrEncrypted }
        let data = out.fileHandleForReading.readDataToEndOfFile()
        guard let s = String(data: data, encoding: .utf8) else { throw SSHKeyImportError.unreadable }
        return s
    }

    private static func keyType(fromOpenSSHPublicKey line: String) -> KeyType {
        if line.hasPrefix("ssh-ed25519") { return .ed25519 }
        if line.hasPrefix("ecdsa-") { return .ecdsa }
        return .rsa(bits: 0)   // bits unknown from the public line; not connect-relevant
    }
}
```

Hinweis: Der `throw`-in-`??`-Ausdruck (`.map(...) ?? { throw ... }()`) muss ggf. in ein `guard let` umgeschrieben werden, falls der Compiler ihn ablehnt — dann `guard let fingerprint = … else { throw SSHKeyImportError.unreadable }`. `keyType` liefert für RSA `bits: 0` (aus der Public-Zeile nicht ablesbar; RSA/ECDSA sind ohnehin nicht verbindbar).

- [ ] **Step 4: Test grün + Suite**

Run: `swift test --filter SSHKeyImporterTests` → PASS. Dann `swift build && swift test` → 0 Warnungen, alle grün.

- [ ] **Step 5: Commit**

```bash
git add Sources/macSCPCore/SSH/SSHKeyImporter.swift Tests/macSCPCoreTests/SSHKeyImporterTests.swift
git commit -m "feat: inspect an existing OpenSSH key for import

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 5: SSH-Keys-Verwaltungs-Sheet + Erreichbarkeit + „verwalten"-Knopf

**Files:**
- Create: `Sources/MacSCPApp/SSHKeysSheet.swift`
- Modify: `Sources/MacSCPApp/MacSCPApp.swift` (`TabCommands.showSSHKeys` + „Sessions"-Menüpunkt)
- Modify: `Sources/MacSCPApp/ContentView.swift` (`@State showSSHKeysSheet` + `.sheet` + Brücke)
- Modify: `Sources/MacSCPApp/ConnectionFormView.swift` (+ `LoginSetsSheet` Editor): „Schlüssel verwalten…" → `showSSHKeys`
- Modify: `Sources/MacSCPApp/Resources/…`

**Interfaces:**
- Consumes: `ManagedKeyStore` (`all/add/remove(id:secrets:)/key(forPath:)/keyDirectory`), `SSHKeyGenerator.generate`, `SSHKeyImporter.inspect` (Task 4), `ManagedKey`/`KeyType`, `KeychainSecretStore`, `SheetSearchField`/`sheetSearchPredicate` (Task 1), das M17-`GenerateKeySheet`.

Reine SwiftUI/App — build-verifiziert + Idle-CPU-Smoke.

- [ ] **Step 1: `SSHKeysSheet` aus dem M17-Tab extrahieren**

`Sources/MacSCPApp/SSHKeysSheet.swift` neu: die Liste + Aktionen + `GenerateKeySheet` aus `SSHKeysSettingsTab` (M17) in die Sheet-Hülle nach dem `LoginSetsSheet`-Muster übertragen (Titel, `SheetSearchField`, Liste, Footer-Buttons, feste `.frame(width: 720, height: 460)`). `searchString` je Key = `"\(key.name) \(key.comment) \(key.fingerprint)"`. Liste über den Predicate filtern (wie Task 1). Der `store = ManagedKeyStore(directory: SessionStore.defaultDirectory)`-Zugriff bleibt.

- [ ] **Step 2: Neue Aktionen (Footer + Zeilen-`.contextMenu`)**

- **Generieren…** (unverändert, `GenerateKeySheet`).
- **Importieren…**: `fileImporter`/`NSOpenPanel` wählt eine private Key-Datei; ein kleines Import-Sheet fragt Name/Kommentar + optionale Passphrase; dann: `SSHKeyImporter.inspect(privateKeyURL:passphrase:)` → bei Erfolg die Datei nach `store.keyDirectory.appendingPathComponent(newID.uuidString)` kopieren + `chmod 0600`, Passphrase (falls gesetzt) `KeychainSecretStore().savePassword(pp, for: newID)`, `ManagedKey(id: newID, name:, comment:, type: info.type, fingerprint: info.fingerprint, publicKeyOpenSSH: info.publicKeyOpenSSH, createdAt: Date(), hasPassphrase: pp != nil, fileName: newID.uuidString)` + `store.add`. **Fehlschlag räumt auf:** kopierte Datei + Keychain-Slot (`try? removeItem`, `try? deletePassword(for: newID)`).
- **Public-Key kopieren** / **exportieren…** (aus M17, unverändert).
- **Privaten Schlüssel exportieren…**: ein `confirmationDialog`/Alert mit Warntext (`keys.exportPrivate.warn`), bei Bestätigung `NSSavePanel` → die private Datei (`store.keyDirectory/fileName`) an den gewählten Ort kopieren.
- **Umbenennen…**: kleines Sheet für Name/Kommentar → `var updated = key; updated.name = …; updated.comment = …; store.add(updated)` (gleiche `id` → Upsert; Datei/Keychain unberührt).
- **Löschen…** (`store.remove(id:secrets: KeychainSecretStore())`, Bestätigung).

Alle Aktionen zusätzlich im Zeilen-`.contextMenu`.

- [ ] **Step 3: Erreichbarkeit (Brücke + Menü + Sheet)**

- `MacSCPApp.swift`: `var showSSHKeys: (() -> Void)?` in `TabCommands` (neben `showLogins`); im „Sessions"-Menü neben „Logins…" einen Button:

```swift
                Button(L10n.string("menu.sshKeys", "SSH Keys…")) {
                    tabCommands.showSSHKeys?()
                }
```

- `ContentView.swift`: `@State private var showSSHKeysSheet = false`; die Brücke setzen (`onShowSSHKeys: { showSSHKeysSheet = true }` bzw. `tabCommands.showSSHKeys = { showSSHKeysSheet = true }` — dem Muster von `showLoginSetsSheet` folgen, ~498-499/592-598); `.sheet(isPresented: $showSSHKeysSheet) { SSHKeysSheet() }` (~697).
- `ConnectionFormView.swift` **und** `LoginSetsSheet`-Editor: der „Schlüssel verwalten…"-`SettingsLink` (M17) wird ein `Button`, der `showSSHKeys` über dieselbe Umgebung/Closure auslöst wie „Logins verwalten…" es für Logins tut (reale Verkabelung übernehmen).

- [ ] **Step 4: L10n**

Neue Keys EN/DE/FR/PL: `menu.sshKeys` ("SSH Keys…"), `keys.import` ("Import…"), `keys.exportPrivate` ("Export private key…"), `keys.exportPrivate.warn` ("The private key will leave the protected store. Continue?"), `keys.rename` ("Rename…"), Sheet-Titel `keys.sheet.title` ("SSH Keys"), Import-Sheet-Felder. (M17-Keys für Generieren/kopieren/exportieren/löschen wiederverwenden.) Typografische Zeichen in DE/FR/PL.

- [ ] **Step 5: Build + Behaviour + Idle-CPU-Hinweis**

Run: `swift build && swift test --filter Localizable`
Expected: 0 neue Warnungen, Parität grün. Verhalten (Codelesen): „SSH-Schlüssel…" im Sessions-Menü öffnet das Sheet; „Schlüssel verwalten…" im Formular öffnet dasselbe Sheet; Import/Generieren/Umbenennen/privat-Export/Löschen funktionieren; Suche filtert. (Idle-CPU wird in Task 8 gemessen.)

- [ ] **Step 6: Commit**

```bash
git add Sources/MacSCPApp/SSHKeysSheet.swift Sources/MacSCPApp/MacSCPApp.swift Sources/MacSCPApp/ContentView.swift Sources/MacSCPApp/ConnectionFormView.swift Sources/MacSCPApp/LoginSetsSheet.swift Sources/MacSCPApp/Resources
git commit -m "feat: manage SSH keys in a dedicated sheet with import and export

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 6: SSH-Keys-Tab aus den Einstellungen entfernen

**Files:**
- Modify: `Sources/MacSCPApp/SettingsView.swift`
- Delete/Modify: `Sources/MacSCPApp/SSHKeysSettingsTab.swift` (falls Inhalt vollständig in `SSHKeysSheet` umgezogen)

- [ ] **Step 1: Tab entfernen + Dead-Code aufräumen**

In `SettingsView.swift` den `SSHKeysSettingsTab().tabItem { … "key" }`-Block (~64-69) entfernen. Ist `SSHKeysSettingsTab.swift` danach ungenutzt, die Datei löschen (oder die noch benötigten Teile — z. B. `GenerateKeySheet`, falls von Task 5 dorthin verschoben — sind bereits in `SSHKeysSheet.swift`; keine Doppelung, kein toter Code).

- [ ] **Step 2: Build**

Run: `swift build`
Expected: 0 neue Warnungen; keine „unused"-Warnung; die App kompiliert ohne den SSH-Keys-Tab.

- [ ] **Step 3: Commit**

```bash
git add Sources/MacSCPApp/SettingsView.swift Sources/MacSCPApp/SSHKeysSettingsTab.swift
git commit -m "refactor: drop the SSH Keys settings tab in favor of the sheet

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 7: Settings-Fenster → Sidebar-Navigation mit Protokoll-Bereichen

**Files:**
- Modify: `Sources/MacSCPApp/SettingsView.swift`
- Modify: `Sources/MacSCPApp/Resources/…`

**Interfaces:**
- Consumes: die bestehenden Bereichs-`struct`s (`GeneralSettingsTab` etc.), `showSSHKeys`-Auslöser (Task 5).

Reine SwiftUI — build-verifiziert + **Pflicht-Idle-CPU-Smoke**.

- [ ] **Step 1: `GeneralSettingsTab` in zwei Bereiche aufteilen**

Die Sections von `GeneralSettingsTab` (Sprache/Menübar/Updates vs. Hidden/Spalten/Auto-Refresh) in zwei `struct`s trennen: `GeneralSettingsSection` (Sprache/Relaunch, Menübar, Updates+Version+Check-Now) und `AppearanceSettingsSection` (versteckte Dateien, Datei-Spalten, Auto-Refresh). Die `store`/`updateModel`/`launchLanguage`-Injektion je nach benötigten Feldern verteilen (Updates → General; Columns/Hidden/Auto-Refresh → Appearance).

- [ ] **Step 2: Verschobene Sections in Protokoll-Bereiche**

- `TransfersSettingsTab`: die presigned-Ablauf-Section HERAUSlösen; der Rest (Parallelität/Bandbreite) bleibt „Übertragung".
- `TerminalSettingsTab`: die externes-Terminal-Ziel-Section HERAUSlösen; der Rest (Font/Größe/Cursor + Preview) bleibt „Terminal".
- Zwei neue `struct`s: `SSHSettingsSection` (externes-Terminal-Ziel + Knopf „SSH-Schlüssel verwalten…" → `showSSHKeys`) und `S3SettingsSection` (presigned-Ablauf).

- [ ] **Step 3: `SettingsView` als `NavigationSplitView`**

`SettingsView.body` von `TabView` auf `NavigationSplitView` umstellen: links eine `List` mit `selection`-Bindung über ein `enum SettingsSection: Hashable { case general, appearance, transfers, openWith, terminal, shortcuts, ssh, s3 }` (mit SF-Symbol + L10n-Titel pro Fall; die Protokoll-Bereiche unter einem `Section("Protokolle")`-Header); rechts ein `switch selection { … }`, das den passenden Bereichs-`struct` rendert. Default-Selektion `.general`. Die feste `.frame` an das Split-Layout anpassen (breiter, z. B. ~`width: 680, height: 480` — beim Umsetzen anhand des realen Inhalts feinjustieren, sodass kein Bereich scrollt/abgeschnitten wird).

- [ ] **Step 4: L10n**

Neue Bereichstitel EN/DE/FR/PL: `settings.section.appearance` ("View"/"Ansicht"), `settings.section.protocols` ("Protocols"/"Protokolle"), `settings.section.ssh` ("SSH"), `settings.section.s3` ("S3"). Bestehende Tab-Titel (`settings.tab.general` etc.) als Sidebar-Titel wiederverwenden.

- [ ] **Step 5: Build + Idle-CPU-Smoke (Pflicht)**

Run: `swift build`
Expected: 0 neue Warnungen. **Dann** Dev-Build starten, Settings-Fenster öffnen, jeden Bereich anklicken, Idle-CPU messen (`ps -o %cpu`): muss ~0 % bleiben, kein Layout-Sturm/Dauer-CPU (M11n-Lektion für Layout-Container). Bei Auffälligkeit die Split-View-Struktur anpassen, bevor committet wird.

- [ ] **Step 6: Commit**

```bash
git add Sources/MacSCPApp/SettingsView.swift Sources/MacSCPApp/Resources
git commit -m "feat: reorganize settings into a sidebar with protocol sections

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 8: Abschluss — Suite, Review, Push/Deploy

- [ ] **Step 1: Volle Suite + 0 Warnungen + Parität**

Run: `swift build && swift test && swift test --filter Localizable`
Expected: Build 0 Warnungen; alle Tests grün (inkl. `SSHKeyImporterTests`); Parität grün.

- [ ] **Step 2: Runtime-Idle-CPU-Smoke (gesamt)**

Dev-Build starten und prüfen: Settings-Sidebar (jeder Bereich), die vier Such-Sheets, das SSH-Keys-Sheet — Idle-CPU ~0 %, kein Layout-Sturm.

- [ ] **Step 3: Whole-Milestone-Review**

Opus-Whole-Branch-Review über den gesamten M18-Diff (`git merge-base develop HEAD`..HEAD, Basis = M17-HEAD `f2980b6`), Fokus auf die Global Constraints (SSH-Key-Invarianten unverändert, privat-Export nur hinter Warnung, Import-Aufräumen, FileSearch nur genutzt, kein Dead-Code nach dem Tab-Umzug, L10n typografisch).

- [ ] **Step 4: Push + CI + Dev-Build (auf Maintainer-Anordnung)**

Push nach `develop`, `gh run watch`, Dev-Build v1.8.0-dev nach `~/Desktop/macSCP-dev.app`. Kein Release/Tag.

---

## Self-Review

**1. Spec coverage:**
- Spec §1 (geteilte Suche, 5 Sheets) → Task 1 (Komponente + Login-Sets) + Task 2 (Known-Hosts/Hidden/Audit) + Task 5 (SSH-Keys-Sheet). ✅
- Spec §2 (SSH-Keys-Sheet + Import + privat-Export + Umbenennen + Erreichbarkeit) → Task 4 (Import-Core) + Task 5. ✅
- Spec §3 (Login-Set-Kontextmenü) → Task 3. ✅
- Spec §4 (Settings-Sidebar + Protokoll-Bereiche + General-Split + Umzüge) → Task 6 (Tab raus) + Task 7. ✅
- Spec §Tests → Task 4 (Core-Test) + Task 8 (Suite/Idle-CPU). ✅
- Spec §Sicherheit → Global Constraints + Task-8-Review. ✅

**2. Placeholder scan:** Offene Stellen mit klarer „realen Namen übernehmen"-Anweisung: reale Feldnamen in Known-Hosts/Hidden/Audit (Task 2), reale Edit/Delete-State-Namen (Task 3), reale „verwalten"-Verkabelung (Task 5), `FileSearchPredicate`-Alles-Matcher (Task 1 Step 2). Kein „TBD/TODO".

**3. Type consistency:** `SheetSearchField(text:isRegex:errorText:)` + `sheetSearchPredicate(text:isRegex:) -> (FileSearchPredicate, String?)`; `SSHKeyImporter.inspect(privateKeyURL:passphrase:) -> ImportedKeyInfo{type,fingerprint,publicKeyOpenSSH}`; `ManagedKey(id:name:comment:type:fingerprint:publicKeyOpenSSH:createdAt:hasPassphrase:fileName:)`; `TabCommands.showSSHKeys`; `showSSHKeysSheet` — über alle Tasks konsistent.
