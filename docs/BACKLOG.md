# macSCP — Backlog

**Status:** 2026-08-29. An index over the entries under
`docs/superpowers/specs/`, so they do not have to be searched for one by
one. Every entry there is a **secured idea or a measured finding**, not a
design — the designs are created only when the work is taken on.

This file contains no content, only pointers. Whoever changes something
changes it in the entry and at most updates the line here to match.

---

## Bugs

| | Entry | Core |
|---|---|---|
| BUG: a second session through the same jump does not open, the UI returns to the session info, and the first connection drops | **Open, recorded 2026-09-18** from the maintainer's report: after connecting through a jump, opening a second session over the same jump does not open it — the UI jumps back to the session info, and the first connection drops. Two halves, kept separate because only one was measured. **Read, with a scratch test confirming the predicate:** the "back to the session info" half has a cause. Every sidebar connect selects the row first, so the unconnected-tab branch in `ContentView+Detail.swift` runs; the session-info branch (`SessionOverviewView`, `:847`, `overviewSession(for: tab)` with `mode == .new`) is chosen before `ConnectionFormView` (`:876`), and the host-key card is rendered only inside the form (`ConnectionFormView.swift:383-384`). A jump session hits this first because the second session's target behind the jump is new, so its host key is unknown and a prompt is raised — which the overview then covers, along with any `.needsPerson` failure or pre-dial refusal text. A scratch test (SCRATCH-4) confirmed the predicate: `overviewSession(for:) != nil` while the host-key prompt is pending, so the overview branch wins regardless. **Measured, not reproduced:** the "first connection drops" half. 6 of 6 live-rig scenarios (Docker, password auth on both hops — the same session twice through one jump, two different sessions through one jump, the jump host directly then a session through it and the reverse, the same session twice with concurrent dials, two different jumps at once) left the first connection intact, and 4 of 4 App tests with fakes (including the real `connectFromSidebar` path against a jump that refuses at once) left tab 1 connected with 0 disconnects. No shared per-jump state was found by reading: each dial builds its own jump `SSHClient` (`CitadelFileSystem.swift:459-513`), `disconnect()` closes only its own client and jump client (`:1387-1391`), and nothing in Core or the App is keyed by the jump host or session. **Two open questions for the maintainer:** after the event, is the first tab still in the tab strip, and what does it show (the browser, the lost surface, or the overview)? And which login does the jump use — password, private key, or agent? Not started. |
| [Liveness assertion rebuild](superpowers/specs/2026-08-08-testsuite-hang-investigation.md) | **Done 2026-09-03** (`9d00aa90`, `0f9ebd2d`, `0b43601d`). The tally closed at 17 of 20 full gated runs red under the old fixed ceiling; the rebuilt ceiling is `min(max(ambient * 3, .seconds(2)), .seconds(4))` against dials of at least 5 s, planted probe 10/10 red both alone and under a loaded full gated run, unplanted 10/10 green alone and 10/10 green (liveness suite) in `MACSCP_ITEST=1 swift test`, and a green run now prints its ambient/gap/ceiling. Open: the frozen-peer hang reproduced during the measurement, cause still unmeasured; the false-red regime above the 4 s cap is reachable (ambient 5.726 s observed under a planted run) though no unplanted gap above 4 s has been seen yet. |
| Diagnostics: error text as a leak route | **Measured 2026-09-03** (Task 5 re-review of the connection-tools plan): `DialSupport.reason(for:)` renders `RemoteFSError` through `String(describing:)`, so any backend error that carries configuration text (an endpoint string with userinfo, a path) can reach a diagnosis row; `URLText.withoutUserinfo` is a backstop that a `/` in the password defeats. The plan pinned the two paths its own rows added. **Done 2026-09-04** (the diagnostics-leak-route plan, `bd6ec81f` + this row's own commit): `DialSupport.reason(for:)`'s `RemoteFSError` arm is now an exhaustive `switch`, no `default`, one fixed English sentence per case — 10 cases (`connectionFailed`, `authenticationFailed`, `jumpAuthenticationFailed`, `notFound`, `permissionDenied`, `protocolError`, `bucketListForbidden`, `bucketListEmpty`, `bucketLevelRefused`, `crossBucketRenameRefused`), plus the 7 `BucketLevelOperation` cases (`write`, `delete`, `createDirectory`, `deleteTree`, `rename`, `presignedURL`, `readStream`) covered through a parameterised iteration over `allCases`. Paths are kept (a path is this project's own string, the finding itself); the two free-text payloads (`connectionFailed(reason:)`, `protocolError(reason:)`) and the cross-bucket rename's two paths are dropped. Red observed before the fix, on a planted `RemoteFSError.connectionFailed(reason:)` carrying `s3://KEY:SECRET@host` through the dial-contribution seam: the plain shape (no `/` in the secret) was already stripped by `DiagnosticStep.init`'s `URLText.withoutUserinfo` backstop — only "one fixed sentence per case" was red on that shape; the slash-bearing shape (a secret with two `/` characters, the shape of a real S3 secret key) was red on all four expectations, the credential reaching the step's outcome, the plain-text report and the Markdown report, because the backstop's authority scan ends at the `/` before it finds an `@` to cut at. **The count of endpoint-bearing S3 sites was wrong twice, in two pushed commit bodies, and this row is the record.** `bd6ec81f`'s body said "four" `S3FileSystem` sites throwing `"Invalid S3 endpoint: <endpoint>"`; `cb90f9b1`'s body corrected that to three and then miscounted in its own way, presenting `:1062` as "a fourth site" carrying the endpoint under a different message — as if it were the only other one. Neither commit can be amended (both are pushed), so the enumeration below is the one to read. Recounted by hand at HEAD `6e16b677`, every line verified, `\(config.endpoint)` interpolated into a `RemoteFSError.connectionFailed(reason:)`: `"Failed to build S3 request URL for endpoint"` at `S3FileSystem.swift:736`, `:1062`, `:1171` (3); `"Invalid S3 endpoint host"` at `S3FileSystem.swift:1052`, `:1162` (2); `"S3 endpoint has no host"` at `S3FileSystem.swift:1215` (1); `"Invalid S3 endpoint"` at `S3FileSystem.swift:730`, `:1045`, `:1156` (3). Total **9 sites in `S3FileSystem.swift`**, and one more outside it — `S3RequestSigning.swift:32`, the second `"S3 endpoint has no host"`, which `bd6ec81f` named as the signer's — for **10 in the S3 backend**. (The "Invalid S3 endpoint host" lines also match a bare `grep` for "Invalid S3 endpoint", which is how a count of that message drifts; they are counted once, under their own message.) `reason(for:)` has 8 callers at HEAD, recounted in the same pass (the plan's own spec said 5; the extra 3 are the S3/WebDAV probes, which the fix reaches too): `Sources/macSCPCore/Diagnostics/ContributionProbes.swift:51, :97`, `Sources/macSCPCore/Diagnostics/DialProbes.swift:71, :80, :300` (the last unqualified, called from inside `DialSupport` itself), `Sources/macSCPCore/S3/S3AccessProbe.swift:149`, `Sources/macSCPCore/WebDAV/WebDAVClaimsProbe.swift:63, :83`. The guard (`Tests/macSCPCoreTests/DiagnosticsNoDescribingGuardTests.swift`, 8 `@Test`s after the final fix round, 5 before it) scans every `.swift` file under `Sources/macSCPCore/Diagnostics/` (11 at HEAD, all directly in it, none carrying a raw-string delimiter; the enumeration is recursive since the final fix round, so a file filed into a future subdirectory cannot escape the scan by being filed) for `String(describing:` — a negative check (`noDiagnosticsFileDescribesAnErrorValue`) over text blanked of comments AND strings, paired with two positives per CLAUDE.md "Guards that name what they watch" (the scan reaches at least 5 files including `DialProbes.swift`; the unblanked `DialProbes.swift` still states the rule in prose) and a self-test that plants the call and drives it through the guard's own scanning function. **The final fix round added a SECOND negative check** (`noDiagnosticsFileBuildsASentenceOutOfAnErrorValue`), because the first one could not see the shape that actually reaches a row: `SwiftSource.stripCommentsAndStrings` consumes a string literal to its closing quote as one span, so blanking a literal blanks what it interpolates, and `return "…\(String(describing: error))"` or `"\(error)"` — the spelling `DialProbes.swift`'s `protocolError` comment names as `SSHAgentClient`'s — were invisible to it by construction, not by accident. The second check reads the same files with only the COMMENTS blanked (a new `SwiftSource.stripComments`, same fail-closed behaviour, self-tested beside its sibling) and rejects `\(String(describing:` plus any interpolation of a single bare identifier that is one of two exact names (`error`, `err`) or ends in `Error`; a member access such as `\(operation.rawValue)` does not match, so the rows this module builds out of hosts, paths and durations stay legal. Its positive is `Tests/MacSCPTestSupport/ErrorInterpolationFixture.swift` — real compiling code in both shapes, under a directory this guard never enumerates — matched through the guard's own scanning function. Mutation-tested, both shapes, each caught by the new check alone: `RESULT: RED — 8 test(s) ran, and the plant was caught by: macSCPCoreTests.DiagnosticsNoDescribingGuardTests/noDiagnosticsFileBuildsASentenceOutOfAnErrorValue()/DiagnosticsNoDescribingGuardTests.swift:134:6` for a planted `return "\(error)"` and again for a planted `return "the connection failed: \(String(describing: error))"` in the `RemoteFSError` arm. The recursion was measured by hand rather than through `scripts/mutation-probe`: a plant that only ADDS an untracked file changes nothing `git diff` can see, and the script reported `PLANT CHANGED NOTHING`; planting `Sources/macSCPCore/Diagnostics/Nested/NestedProbe.swift` by hand turned `noDiagnosticsFileDescribesAnErrorValue` red naming that file, and removing it turned all 8 green again. Mutation-tested: `RESULT: RED — 5 test(s) ran, and the plant was caught by: macSCPCoreTests.DiagnosticsNoDescribingGuardTests/noDiagnosticsFileDescribesAnErrorValue()/DiagnosticsNoDescribingGuardTests.swift:89:6` (`scripts/mutation-probe --filter DiagnosticsNoDescribingGuardTests --apply "perl -0pi -e 's/return \"the connection failed\"/return String(describing: error)/' Sources/macSCPCore/Diagnostics/DialProbes.swift"`). **The `default` arm was measured, not assumed, and came back GREEN** (final fix round, 2026-09-04): `DialSupport.reason(for:)`'s last arm renders a foreign error's `localizedDescription`, behind `DiagnosticStep.init`'s `URLText.withoutUserinfo` backstop only — the backstop the slash-bearing shape defeats. `aForeignErrorsLocalizedSentenceNeverReachesTheRow` plants a `URLError(.unsupportedURL)` carrying a credential-bearing failing URL in its `userInfo` through the dial seam and reads back the step's outcome, its detail, the plain-text report and the Markdown report: none of them carries either half of the credential. `NSURLErrorDomain` has no per-code localized sentence that quotes the URL (a manually built one describes the domain and the numeric code and nothing else), and `localizedDescription` does not consult the failing-URL keys — the value is reachable only through `description`, which prints the whole `userInfo`, and through `String(describing:)`, and this module reaches for neither. So no `URLError`-to-fixed-sentence mapping was written; the test stays as the regression that would catch one being added. Sensitivity measured, not assumed: `RESULT: RED — 1 test(s) ran, and the plant was caught by: macSCPCoreTests.ConnectionDiagnosticsTests/aForeignErrorsLocalizedSentenceNeverReachesTheRow()/ConnectionDiagnosticsTests.swift:931:6` with the arm planted as `(error as NSError).description`. Two measurements worth keeping beside it: `URL(string:)` returns nil for a userinfo password containing `/`, so the URL-valued failing-URL key cannot hold this shape at all and the string-valued key is the only route (spelled as its own literal — the `Foundation` constant is deprecated as of macOS 15.4 and would be a build warning); and the first draft of that test leaked the credential through a `#require` on the URL-building expression, which printed the whole interpolated string in the failure message — CLAUDE.md's "two exits" caught in the act, fixed by naming the value and asserting only Bools. The information trade: `protocolError`'s server-composed text no longer reaches a row at all — some of those messages were genuinely useful (a server can quote the request line it refused), and dropping them was taken because the backends compose that text out of the same user-controlled endpoint field. The way back, if that detail is wanted again, is a separate structured field carried alongside the fixed sentence, not a wider `URLText.withoutUserinfo` — the backstop is structurally unable to close this hole, only not composing the description can (per `bd6ec81f`'s own commit body). |
| Wall-clock ceilings still in the tree | **Measured 2026-09-03** (final review of the connection-tools plan): three tests keep an upper bound on elapsed time that a starved three-core runner can overrun — `TerminalPanelViewModelTests.neverFiresWhenTheOpenFails` polls on the main actor under a 5 s ceiling (red at 10.149 s in one of ten local full runs, against an ambient control of 10.1995 s in the same run), plus the two ceilings the connection-tools final review parked and the NETSPIKE-gated one at `ConnectionDiagnosticsTests.swift:164` (re-find them with `grep -rn "elapsed <" Tests/`). **Update 2026-09-04** (`c93c79bb`): the four `elapsed <` ceilings are gone — `BoundedCloseTests` (red on CI run 33814867360 at 15.343890416999999 s against 15 s), the NETSPIKE-gated TCP ping, the subprocess cancellation, the liveness resolution race — each replaced by the ordering it was standing in for (a harness `.timeLimit` against a longer bound, or the outcome check already beside it). Still open: the polling helpers. Counted 2026-09-04 with `grep -rln "let deadline = ContinuousClock.now + " Tests`: 8 files carry a `waitUntil`-style helper with a wall-clock deadline (macSCPCoreTests/TerminalPanelViewModelTests.swift macSCPCoreTests/ConnectionViewModelTests.swift macSCPCoreTests/SubprocessRunnerTests.swift macSCPCoreTests/WebDAVFileSystemIntegrationTests.swift macSCPCoreTests/LoopbackHTTPStub.swift macSCPAppKitTests/AlreadyOpenSessionTests.swift macSCPAppKitTests/ReconnectPathTests.swift macSCPAppKitTests/ConnectAttemptHandoffTests.swift), plus the 200 × 10 ms loop at `TerminalPanelViewModelTests.swift:32`; the terminal test named above uses the 5 s one at `:1010`. Each is a ceiling the runner can overrun; the iteration-bounded `Task.yield` helpers (`TransferQueueViewModelTests`) are not, and are the model. A plan of its own: one shared helper, iteration- or notification-bounded under a harness limit. **A fifth shape, 2026-09-04** (`6832d5a1`): a duration on the PROBE's side is a ceiling on the runner's side — `ConnectionDiagnosticsTests.aProbeThatIgnoresCancellationDoesNotHoldTheStepPastItsDeadline` held its probe on a 12 s Dispatch timer and asserted the step returned first; CI run 33819886384 took 32.987 s to resume the test and the timer won (`stillRunning` false). The probe now waits on a `Gate` the test opens only after the step has returned, so the ordering is the test's own sequence; the harness `.timeLimit` is the only clock. Re-find such shapes with `grep -rn "asyncAfter\|Task.sleep(for: .seconds" Tests/` — a sleep whose expiry is compared against something the runner must schedule. **Done 2026-09-04** (`96e1b28e`, `da4ee4ba`, `d31637e2`, `d4423be8`, plus this row's own commit): the polling-without-a-clock plan closes it. One shared `pollUntil(_:every:_:)` (`Tests/MacSCPTestSupport/PollUntil.swift`) carries no deadline of its own — it ends only on cancellation, which every calling suite's `.timeLimit` performs — and both test targets now poll through it. **Removed**, counted by diffing `func`-name occurrences per file between `96e1b28e` and `d4423be8` (`git diff 96e1b28e..d4423be8 --stat`: 17 files changed): 10 helper function definitions — 7× `func waitUntil` (`AlreadyOpenSessionTests`, `ConnectAttemptHandoffTests`, `ReconnectPathTests`, `SSHTerminalViewSizingTests`, `ConnectionViewModelTests`, `TerminalPanelViewModelTests` ×2), 1× `func becomesTrue` (`SSHTerminalViewSizingTests`), 1× `func completesWithin` (`TransferQueueViewModelTests`), 1× `func tryClaim` plus the `RaceClaim` actor it existed for (`ConnectionViewModelTests`). **Callers**: `grep -rc "pollUntil(" Tests | awk -F: '{s+=$2} END {print s}'` = 117 (includes `PollUntil.swift`'s own definition line, `PollUntilTests`' 3 tests of the helper itself, and the guard's own 9 textual mentions of the string in doc comments and `.contains(...)` calls — the guard excludes its own file from what it scans). 16 suite files reach `pollUntil`, all carrying `.timeLimit(`: 12 direct callers (the text `pollUntil(` appears in the file) and 4 indirect callers reaching it only through `LoopbackHTTPStub.waitForRequests` (`S3SessionIsolationTests`, `S3RedirectAuthorizationMeasurementTests`, `S3RedirectControlTests`, `ConnectFailureSecrecyTests`); 23 files in `Tests/` carry `.timeLimit(` at least once (31 occurrences total, most predating this plan). **The guard** (`Tests/macSCPCoreTests/PollingGuardTests.swift`, 5 `@Test`s, each a negative paired with a positive per CLAUDE.md "Guards that name what they watch"): `theTreePollsThroughTheSharedHelper` (≥ 10 files call `pollUntil`), `noTestCarriesItsOwnDeadline` (no `let deadline = ContinuousClock.now` / `ContinuousClock.now.advanced(by:`), `noTestAssertsAnElapsedCeiling` (no `elapsed\s*<=?\s*\.`), `aFloorExistsSoTheCeilingCheckHasSomethingToRead` (a real `elapsed >=`/`elapsed >` exists, so the ceiling regex has something to read), `everyCallerOfPollUntilDeclaresATimeLimit` — structural rather than a flat per-file scan, because that version was red on `LoopbackHTTPStub.swift` (a helper file with no suite of its own) and missed the four indirect S3/secrecy callers: a file declaring neither `@Suite` nor `@Test` is a helper, every `func` it declares whose brace-balanced body contains `pollUntil(` is a helper name (found: `waitForRequests`), and every suite file that calls `pollUntil(` directly or calls a helper name must carry `.timeLimit(`, paired with two positive checks (the helper set is non-empty and contains `waitForRequests`; the direct-caller set is non-empty). Mutation-tested with `scripts/mutation-probe`, both RED: `RESULT: RED — 5 test(s) ran, and the plant was caught by: ✘ Test noTestCarriesItsOwnDeadline()` (planted a `let deadline = ContinuousClock.now + .seconds(5)` before one `pollUntil` call in `TerminalPanelViewModelTests`) and `RESULT: RED — 5 test(s) ran, and the plant was caught by: ✘ Test everyCallerOfPollUntilDeclaresATimeLimit()` (stripped `.timeLimit(.minutes(1))` from `ConnectFailureSecrecyTests`, an indirect-only caller). **Fixed along the way**: `LivenessProbeRaceTests.swift`'s comment quoting `elapsed < .seconds(20)` verbatim matched the guard's own ceiling regex (CLAUDE.md, "Source-scanning guards read comments too") — reworded to prose, no behavioural change. `SSHTerminalViewSizingTests`'s 15 `pollUntil` calls and `LivenessProbeDropIntegrationTests.waitForIdleQueue` had silently dropped their pre-migration poll interval (10 ms and 50 ms respectively, both deliberate choices) to the 5 ms default when Task 3 migrated them; both now pass `every:` with the original interval. **Done 2026-09-04** (`2ddc59f4` Task 1, `79f6e693` Task 2, plus this row's own commit — the "ceilings under other spellings" plan): every site the paragraph above named is closed. `Box.waitForFirstDelivery(timeout:)` lost its parameter and its ceiling task, replaced by `withTaskCancellationHandler` around `parkUntilDelivered()` whose `onCancel` resumes the waiter. `LoopbackTLSStub.swift:110`'s `ready.wait(timeout: .seconds(60))` and `EmbeddedKeyPorterTests.swift:159`/`:171`'s two `finished.wait(timeout: .seconds(10))` calls are now `wait()`, ending only through the calling suite's `.timeLimit`. `ConnectMainActorLivenessTests.swift`'s five-second `DeadlineNotEnforced` sibling is gone; the bootstrap is awaited directly (`try await bootstrap.connect(...).get()`) under the suite's own `.timeLimit`, with the floor (`elapsed > .milliseconds(300)`) and the query-started check both kept. `CitadelShellIntegrationTests.swift`'s `collectUntil(timeout:)` lost its timeout parameter and its ten-second sibling sleeper — it reads `shell.output` until the marker appears or the task is cancelled — and `shellExitEndsOutputStream` reads `Task.isCancelled` instead of racing a second sleeping child. Mutation-tested, four RESULT lines across the two tasks: `RESULT: RED — 26 test(s) ran, and the plant was caught by: ✘ Test embedNeverOpensAnExternalKeyPath()` (the latch never raised, Task 1); `RESULT: RED — 5 test(s) ran, and the plant was caught by: ✘ Test firesOnceWhenTheShellIsRunning() ✘ Test waitsForTheFlushWhenTheShellIsStillOpening()` (the delivery callback never fires, Task 1); `RESULT: RED — 4 test(s) ran, and the plant was caught by: ✘ Test theConnectDeadlineAlsoCoversNameResolution()` (the connect deadline moved past the harness limit, Task 2); `RESULT: RED — 6 test(s) ran, and the plant was caught by: ✘ Test echoRoundtrip()` (a marker that never appears, gated `MACSCP_ITEST=1`, Task 2). `PollingGuardTests` gained the two remaining spellings under this row's own commit: `noLatchIsWaitedOnWithATimeout` (no `wait(timeout:` anywhere in `Tests/` outside `AsyncSignalTests.swift`, `Support/AsyncSignal.swift`, and the one exemption below, matched by the sentence beside it rather than by file name) and `noSleepingChildRacesWorkInAGroup` (no `addTask` whose body's first statement is `Task.sleep(for:`, scanned over comment-and-string-blanked source — a private stripper copied from `TabContextMenuWiringGuardTests`' own, this project's stated preference over a shared one that has already drifted once). Both mutation-tested RED: `RESULT: RED — 7 test(s) ran, and the plant was caught by: macSCPCoreTests.PollingGuardTests/noLatchIsWaitedOnWithATimeout()/PollingGuardTests.swift:100:6` (a `wait(timeout:)` call planted in `PollUntilTests.swift`, outside every exemption) and `RESULT: RED — 7 test(s) ran, and the plant was caught by: macSCPCoreTests.PollingGuardTests/noSleepingChildRacesWorkInAGroup()/PollingGuardTests.swift:145:6` (an `addTask` planted with `Task.sleep(for: .seconds(3))` as its first statement, in `CitadelFileSystemIntegrationTests.swift`). **Still open, and staying, per the plan's own Global Constraints**: `SubprocessRunnerTests.swift:422`'s `started.wait(timeout: startBound)` — the bound IS the saturation this test measures, gated `MACSCP_SATURATION`, and exempted by that same sentence rather than by its file name; `AsyncSignalTests.swift:39`, `:64`, `:83` — the tests OF the bounded API `wait(timeout:)` itself, which the site above still calls. A fourth open item, from Task 2's own fix round 1 (`ac91cb9c`, this row's sibling task): `ConnectMainActorLivenessTests.theConnectDeadlineAlsoCoversNameResolution` used to await a NIO `EventLoopFuture` with `.get()`, which the framework documents as not respecting cancellation — so a `.timeLimit` could record the red and then the run would park on an await nothing would release, the same 0% CPU shape `docs/superpowers/specs/2026-08-08-testsuite-hang-investigation.md` describes, reached from the other side. `awaitCancellably(_:)` replaced it there. Two more `.get()` calls on a NIO future carried the identical hazard and no suite `.timeLimit` at all, so a hang on either would have had no ceiling to stop it: `AgentAuthTests.swift:88` (`promise.futureResult.get()` inside `nextOffer`) and `SSHPrivateKeyLoaderTests.swift:399` (`keyPromise.futureResult.get()`) — verified at HEAD 2026-09-04, the natural second and third callers of `awaitCancellably`. **Done, this commit**: `awaitCancellably` moved out of `ConnectMainActorLivenessTests.swift`, its original and only caller, into a public helper at `Tests/MacSCPTestSupport/AwaitCancellably.swift` (`MacSCPTestSupport`'s target dependencies gained `NIOCore` and `NIOConcurrencyHelpers`, which both test targets already carried transitively through `macSCPCore`); both sites now call it and carry their own `.timeLimit(.minutes(1))` (neither suite had one before). `PollingGuardTests.noEventLoopFutureIsAwaitedWithGet` closes the gap structurally: no `.get()` in `Tests/` may follow an identifier ending `future`/`Future`/`futureResult`, scanned over comment-and-string-blanked source per CLAUDE.md "Source-scanning guards read comments too" (`EventLoopFuture` itself ends in `Future`, and several doc comments, including this helper's own, spell `` `EventLoopFuture.get()` `` verbatim to explain the hazard). Counted at the parent (`a61df6bd`, before conversion) with `git grep -n "\.get()" a61df6bd -- Tests/`: **12** `.get()` calls under `Tests/`, not the 9 this row and `6f79b7bd`'s own commit body first said — that undercount silently dropped 3 of the 12 lines, all three from `Tests/macSCPAppKitTests/`, when the final enumeration was written from memory mid-session instead of re-read from the full `Tests/`-wide grep the sentence itself cites; a fix round (reviewer-caught, `2026-09-04`) recounted and corrected both this row and `PollingGuardTests`' own doc comment. The 12, in full: 2 were the `EventLoopFuture.get()` sites above (converted), 6 were `Result<Value, Error>.get()` (`BandwidthBucketTests.swift:97`, `HostKeyValidationTests.swift:76`, `EmbeddedKeyPorterTests.swift:171`, `UpdateCheckerTests.swift:18`, `SSHAgentClientTests.swift:32`, and `SSHTerminalViewSizingTests.swift:327`'s `try outcome.get()` — an unrelated stdlib API the new guard's regex does not match, since none of their receiver identifiers end `future`/`Future`/`futureResult`), and 4 were prose mentions of `` `EventLoopFuture.get()` `` (not calls): `ConnectMainActorLivenessTests.swift:428` and `:503`, plus `LivenessProbeRaceTests.swift:13` and `LivenessProbeDropIntegrationTests.swift:1077`, both in `Tests/macSCPAppKitTests/`. The guard itself has three known limits, all structural rather than oversights: its helper derivation stops at one hop — a helper file's function counts as a polling helper only when its own body names `pollUntil` directly, so a helper that instead called another helper function would go unseen, though no file in the tree does that today, counted by hand — and its `.timeLimit(` check reads a whole file as one span rather than one suite at a time, so `LivenessProbeDropIntegrationTests.swift`, which holds an unlimited suite (`FileCloseBoundRelationTests`, declared around `:115`) beside the gated, ten-minute-limited suite (declared around `:907`), passes today only because both of the file's `pollUntil` calls happen to sit in the limited suite — a `pollUntil` added to the unlimited one would pass just the same, and even a comment mentioning the trait anywhere in the file would satisfy the check as written. A third, found in fix round 1 of the ceilings-under-other-spellings plan's Task 3: `noSleepingChildRacesWorkInAGroup` required the sleep to be the literal first statement inside the added child's braces, and `AsyncSignal.race(timeout:_:)` (`Tests/macSCPCoreTests/Support/AsyncSignal.swift:119`-`143`) was exactly the shape that check exists to catch — a sleeping child beside the real work inside a task group — but wrapped the sleep in an error-handling block one level in, so the token right after the brace was that wrapper rather than the sleep call, and the scan passed over it without matching. Nothing enforced that shape until then but the site's own tests (`AsyncSignalTests.swift`, which exercise `wait(timeout:)` directly) and the saturation site's comment (`SubprocessRunnerTests.swift:422`, "the bound IS the measurement" — the one caller of `wait(timeout:)` left in the tree, so a behavioural change here would show up there first). **Turned into a named exemption, this commit** (guard-followups task): the regex now also matches a sleep wrapped one level inside a `do {}` (`addTask(?:\([^)]*\))?\s*\{\s*(?:do\s*\{\s*)?(?:try\??\s+)?(?:await\s+)?Task\.sleep\(for:`), and `AsyncSignal.race`'s `addTask` (`:124`-`138`) is excluded only while its comment beside the sleep (`:126`-`131`) carries the sentence "the timeout IS the API under test here" — matched by that sentence rather than by file name, the same shape `noLatchIsWaitedOnWithATimeout`'s saturation-site exemption already used, so a rewording without also removing the shape turns the check red instead of quietly staying green (verified by deleting the sentence locally: 2 issues, both from `noSleepingChildRacesWorkInAGroup`, then restored). `SleepingChildRegexFixture.swift` gained a second demonstration, `demonstratesTheDoWrappedBannedShape`, and the guard's own positive check against it moved from "matches something" to a count of exactly 2. Mutation-tested RED: `RESULT: RED — 10 test(s) ran, and the plant was caught by: macSCPCoreTests.PollingGuardTests/noSleepingChildRacesWorkInAGroup()/PollingGuardTests.swift:174:6` (a `do {}`-wrapped sleeping child planted in `PollUntilTests.swift`, outside every exemption; reverted after measuring). `PollingGuardTests` carries 10 `@Test`s at HEAD, unchanged in count by this fix — one existing test widened, none added. Three production-bound `deadline: now.advanced(by:)` arguments to `ICMPEcho.waitForReply`/`NetworkTrace.waitForAnswer` (`ICMPEchoTests.swift:411`, `NetworkTraceTests.swift:790`, `:895`) are a parameter of the code under test, not a test's own ceiling, and read a local `now` rather than `ContinuousClock.now`, so neither guard pattern reaches them. Two iteration-bounded `waitUntil` helpers read no clock and stay, per the plan's architecture note: `EditSessionManagerTests.swift:147` and `TransferQueueViewModelTests.swift:308` (both `Task.yield`-style, bounded by an iteration count, not a deadline). Two normal-path joins are Never-failing and each preceded by `continuation.finish()`, so they end today and would be unkillable only if the task they join ever stopped ending: `_ = await adHoc.value` at `ReconnectPathTests.swift:604`, `_ = await task.value` at `ConnectAttemptHandoffTests.swift:397`. `LivenessProbeDropIntegrationTests`'s printed "was still in flight 60s later" (`:1240`, `:1387`; a 300 × 200 ms loop each) is a floor, not a measurement — the line only prints once the test has already failed. **A footer deviation, 2026-09-04**: three commits on `develop` carry the footer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` instead of this project's `Claude Fable 5` — `8f66d41c`, `88c079a8`, `42bc9460` — left unrewritten: `git log --format=%H 8f66d41c..develop` names 329 commits after the first of the three, and their 8-character hashes appear 206 times as substrings across `docs/**/*.md` (counted 2026-09-04) — rewriting `8f66d41c` would move every commit after it, breaking that citation trail for a footer typo. The deviation is recorded here instead of corrected. **Recount 2026-09-04** (at `bd6ec81f`): the row's own method left "their" ambiguous between the three footer commits and the full range, so here is the exact command for each number. Commit count — `git log --format=%h 8f66d41c..HEAD | wc -l` — is 331. Hash-reference count — summing, over every one of those 331 short hashes, its occurrences as a substring across `docs/**/*.md` (`git log --format=%h 8f66d41c..HEAD | while read -r h; do grep -roE "$h" --include='*.md' -r docs; done | wc -l`) — is 210. Both numbers grew from the row's own (329 / 206) because two more commits landed on `develop` between that measurement and this one; the three footer hashes themselves still account for only 5 of the 210 references. **A fourth footer deviation, 2026-09-05**: `c42ffd5a` (the detachable-tabs plan's Task 5 commit) also carries `Claude Fable 5.1 <noreply@anthropic.com>` — pushed together with `3fd16e67` on 2026-09-05, the trailer check printed the mismatch but did not gate the push, and it is left unrewritten for the same reason the three above are: it is pushed history, and the project's precedent is to record such a commit here rather than rewrite it. **A sixth spelling, 2026-09-04** (`ad133cc4`): a `Task.detached` sleeping timer racing detached work through a shared continuation — `WebDAVFileSystemWriteTests.withDeadline(seconds:)`, red on CI run 33856445475 at 23.984 s against 5 s; now `abandonable`, resumed with `nil` only by the cancellation the suite's `.timeLimit` performs. `PollingGuardTests` does not scan this spelling either (its sleeping-child check looks inside `addTask`); re-find with `grep -rn "Task.detached" Tests/` and read each for a sleep beside work. |
| [Dev-build UI follow-ups](superpowers/plans/2026-09-03-dev-build-ui-followups.md) | **Done 2026-09-03.** Settings → Transfers "Always show full paths" (`f198548a`: every transfer row shows source → destination from the `TransferRowPaths` fold when on; default off; CI green 33784239558). The sidebar's error caption (folder-move cycle and siblings) has a close button and clears itself after six seconds (`ece5aaf9`). Both from the maintainer's first session with the diagnostics dev build. **Done — this follow-up:** the third red caption, `hiddenImportsErrorMessage` (`SessionSidebar.swift`), now has the same close button and six-second auto-dismiss as the other two. It reaches the sidebar as a plain `let`, not `@State` (owned by `ContentView`, since both the "Hide" context-menu action and the startup/refresh read can set it), so the clear goes through a new `onDismissHiddenImportsError` callback into `ContentView.dismissHiddenImportsError()` rather than a local property write. `SessionSidebarErrorGuardTests` extended to the third banner, `HiddenImportsErrorDismissGuardTests` added for the `ContentView`-side half — both source-scan only, the same treatment `jumpRestoreErrorMessage` already had: `hiddenImportsErrorMessage` is `@State` on a `View` struct, and a bare, unmounted `ContentView` drops writes to its own `@State` (measured directly), so a behavioral read-back test would not be evidence of anything. |
| [Dev-build UI follow-ups 2](superpowers/plans/2026-09-03-dev-build-ui-followups-2.md) | **Done 2026-09-04.** Four maintainer findings from the second dev-build session. S3: an endpoint with a port connects (`43e06e88`, `7fcd1a0e`): one parse (`S3FieldSchema.endpointComponents`; schemeless = https, port honoured, IPv6 literal), an IP-literal host forces path-style addressing with the toggle greyed and a hint line, and the form's footnote names the origin the request will use (`<bucket>.host:port` under virtual-host addressing). Diagnostics: the panel's footer keeps its buttons whole (`7481db8e`, `ed55cfa2`): fixed-size buttons, a label-less scope picker with an accessibility label, `minimumWidth` 600 derived from attributed-string metrics (French widest, ≈589pt), a two-row fallback. S3 presets: one per Hetzner Object Storage location, fsn1/hel1/nbg1 per docs.hetzner.com read 2026-09-04 (`c60d1153`), the importer maps those hosts to their preset. Connection form: the fields scroll when the window is short, the button row stays put (`bc94bac1`, `95b64021`), with a guard whose span is `body`'s own. Final review fixes (`9af168d2`): two guard scan spans corrected (a trailing brace in the declaration text made `declarationBodyRange` scan a body instead of its struct — twice in the tree), the Hetzner-id comment no longer claims stored sessions reference a preset id, an imported IP-host bookmark is asserted path-style on the stored record. Along the way the four remaining `elapsed <` ceilings were removed after CI run 33814867360 (`c93c79bb`; see the wall-clock row). 4207 tests green at `9af168d2`. Open: `EndpointFootnoteWiringGuardTests`' paren balancer runs over comment-stripped, not string-stripped source (a `)` inside a string argument would miscount; none exists today); the maintainer may still prefer the scope picker on its own row in the narrow footer; no view was rendered by any test — the dev build is the check. |
| CI logs lose lines | **Measured 2026-09-03** (run 33741778350): the Actions job log dropped 113 test verdicts and 4 suite verdicts under interleaved Swift Testing output — a suite printed "passed" while one of its tests had no verdict line, and the run's one failing test never appeared by name (run 33698102652 lost 60 suite verdicts the same way). **Done 2026-09-04** (`1ad47f4f`): the Unit-Tests step runs `swift test --event-stream-output-path`, a step that runs on red and green alike prints every `issueRecorded` event as a `::error::` naming its test and lists the `testEnded` verdicts that say "failed after", and the file is uploaded as the `test-events` artifact (14 days). Measured on run 33829938823 (Xcode 16.4, Swift 6.1.2): the flag is accepted, the artifact is 594485 bytes. Open: nothing has been red since, so the step has named no failing test yet; the local runs that lose the verdict line (piped `swift test`) are not covered — pass the same flag locally when a red has no name. **Partly closed for one local runner, 2026-09-04** (this row's own commit, ceilings-under-other-spellings Task 3): `scripts/mutation-probe` now runs `swift test --event-stream-output-path` itself and reads RAN and the caught-test list from the event file first, falling back to the stdout parse only when that file is empty — measured on exactly the shape that lost a verdict locally (Task 2's `--filter theConnectDeadlineAlsoCoversNameResolution` against a planted 90 s connect timeout): the probe now reports `RESULT: RED — 2 test(s) ran, and the plant was caught by: macSCPCoreTests.ConnectMainActorLivenessTests/theConnectDeadlineAlsoCoversNameResolution()/ConnectMainActorLivenessTests.swift:397:6` instead of `NO TESTS RAN`. Piped `swift test` on its own, outside the probe, is still uncovered. **Dated, 2026-09-04**: that RESULT line was measured before the RAN-inflation fix this row's sibling task made to this same script (a suite's own `testEnded` counted alongside its tests' — "Wall-clock ceilings still in the tree", above) — the correct count for that run is 1 test ran, not 2. The shape itself is also no longer reproducible as written: `theConnectDeadlineAlsoCoversNameResolution`'s suite carries a three-minute `.timeLimit` since `ac91cb9c`, and a 90 s planted connect timeout no longer outlives it — `ac91cb9c`'s own commit body replanted the same probe against the raised limit and got GREEN at 90 s, moving the plant to `.seconds(600)` to reproduce red again. What is preserved here is the shape (a narrow `--filter` losing its run's only verdict to interleaved output), not a run this exact command still produces at HEAD. |
| `deleteTree` does not delete a plain file (S3, WebDAV) | **Measured 2026-09-04** (while building the CLI test matrix, Task 1). `RemoteFileSystem.deleteTree(at:)`'s own contract says "A plain file behaves exactly like `delete`"; two of the three backends do not. **WebDAV**: `WebDAVFileSystem.deleteTree` calls `simple(method: "DELETE", path: path, isDirectory: true)`, which appends a trailing slash — against the rig's Basic vhost, `DELETE /dav/<file>.txt/` answers **400** and `DELETE /dav/<file>.txt` answers **204** (curl, 127.0.0.1:18080). **S3**: `S3FileSystem.deleteTree` resolves through `resolvePrefix`, which returns `key + "/"`, and a file's key has nothing beneath it, so the batched `DeleteObjects` runs over zero keys and the call **succeeds having deleted nothing**. **SSH** is correct (`CitadelFileSystem` removes the file). Found because a test cleanup written as `try? await deleteTree(...)` left its seeded file on both servers through a full green run; re-planted as a mutation probe 2026-09-04, S3 and WebDAV red, SSH green. Consequence beyond the harness is READ FROM THE CALL SITES, not exercised through the UI: both production callers hand `deleteTree` whatever the user selected, files included — `RemoteBrowserViewModel.deleteItems` (`RemoteBrowserViewModel.swift:701`, which then records the path as deleted) and `macscp-cli rm --recursive` (`RmCommand.swift:40`). Counted 2026-09-04: two call sites of the protocol method in `Sources/` outside the implementations. Open: either both implementations honour the contract (WebDAV sending the DELETE without the trailing slash for a non-directory; S3 deleting the object key itself when the prefix walk finds nothing), or the contract changes to say `deleteTree` is directories-only and both call sites are made to choose — red first per backend on the rig, either way. **Done 2026-09-04** (`4587c856` WebDAV, `e7169c1a` S3, plus this follow-up commit for the CLI matrix's own workaround). Before: WebDAV's `DELETE .../<file>/` answered **400**; S3's `deleteTree` walked `<key>/` under a file's key and batched **zero** keys, returning success having deleted nothing. After: both backends `stat` the path first and derive the request from what they find — WebDAV sends `DELETE` without the trailing slash for a file; S3 sends a single `DELETE` on the file's own key instead of the batched walk (one extra listing per call, the same parent listing `stat` already does elsewhere — see the paging row below). **The WebDAV lookup's cost, measured 2026-09-04** from Apache's own access log (`docker logs macscp-test-webdav`, one gated test run per kind, no new machinery): a plain file costs **one** extra request (`PROPFIND /dav/<file>` → 207, then `DELETE /dav/<file>` → 204); a collection costs **two** (`PROPFIND /dav/<name>` → **301**, `PROPFIND /dav/<name>/` → **207**, then `DELETE /dav/<name>/` → 204) — mod_dav redirects a collection addressed without its trailing slash and `URLSession` follows it, so `WebDAVFileSystem` sees ONE lookup while the wire carries two. A path with nothing there costs two as well (both URL shapes answered 404, no DELETE sent). Gated cases added and green against the rig: `deleteTreeRemovesAPlainFile` (WebDAV, Task 1), `deleteTreeRemovesAPlainObject` (S3, Task 2). The consequence read from the call sites above stayed a call-site reading for both: Task 2's report (`.superpowers/sdd/2026-09-04-delete-contracts-s3-webdav/task-2-report.md:141-143`) records that `RemoteBrowserViewModel.deleteItems` is the S3 browser's only route to `deleteTree`, so deleting a plain object through the GUI would have reported success and left the object in place — no GUI was driven, in that task or since. `Tests/macSCPCoreTests/Support/CLIMatrix.swift`'s `removeRemote`/`removeRemoteTree`, written to work around exactly this defect, now each make one `deleteTree` call; `MACSCP_ITEST=1 swift test --filter CLIMatrix` (55 tests) stays green with the workaround gone. |
| [CLI test coverage](superpowers/plans/2026-09-03-cli-test-matrix.md) | **Done 2026-09-04** (`7b31087b`, `4842adf2`, `1426dc59`, `9689d952`), one shared helper (`Tests/macSCPCoreTests/Support/CLIMatrix.swift`) and one case file (`Tests/macSCPCoreTests/CLIMatrixITests.swift`). Counted 2026-09-04 with `awk` over `@Suite`/`@Test`: **55 test functions, 8 ungated** (`CLIMatrixCoverage` — derivation and sensitivity guards needing neither rig nor binary) **and 47 gated** behind `MACSCP_ITEST=1`: `CLIMatrixSSH`/`CLIMatrixS3`/`CLIMatrixWebDAV` at 12 `@Test`s each (one, `putOverAnExistingFile(action:)`, parameterised over `ConflictAction.allCases`, so **14 cases per backend** — `ls --json`, `mkdir`, `put`, `get`, all three `--on-conflict` actions, a directory refused by both transfers, `rm`, `rm --recursive`, the root-delete escape hatch, an unknown host key refused then accepted, a changed host key as a hard stop, `--password-command`), `CLIMatrixSessions` at 6 `@Test`s (one parameterised over `ConnectionKind.allCases`, so **8 cases** — every session listed, the group filter reaching subgroups, the tag filter case-insensitive, both filters combined, one case per backend kind, a case-insensitive name substring), and `CLIMatrixCommands` at 5 binary-only guards (the subcommand list itself, every offered subcommand driven by some case, the connection flags asked per command, neither transfer command offering `--recursive`, the `--on-conflict` value list matching `ConflictAction`). `swift test`: 4254 tests in 358 suites, green. `MACSCP_ITEST=1 swift test`: 4254 tests in 358 suites, green, ~91s. `MACSCP_ITEST=1 swift test --filter CLIMatrix`: 55 tests in 6 suites, green, ~1.7s. Rig re-verified clean against its SSH/WebDAV/S3 baseline after every run; zero `macscp-cli-matrix-*`/`cli-matrix-local-*` temp directories survive. **Skipped: exactly two behaviours**, both on S3 and WebDAV only (neither authenticates a host key) — the unknown-host-key refusal and the changed-host-key hard stop — each printing `CLIMatrix: skipping … — this backend authenticates no host key`; the unknown-key case additionally asserts the complement (`ls --non-interactive` connects and the store stays empty) rather than skipping silently. **Pinned gaps, not capability-gated** (re-measured 2026-09-04 against the built binary): `put --recursive`/`get --recursive` do not exist on any backend (`macscp-cli put --recursive a b` → exit 64, `Error: Unknown option '--recursive'`) — the CLI's transfers are single-file by design, per `TransferSourceError.isDirectory`'s own doc comment; `--on-conflict rename` does not exist on any backend either (`macscp-cli put --on-conflict rename a b` → exit 64, `Error: The value 'rename' is invalid for '--on-conflict <on-conflict>'. Please provide one of 'fail', 'skip' or 'overwrite'.`) — `ConflictAction` has no such case. `CLIMatrix.binaryURL()`'s duplication with `CLIRoundtripITests.locateCLIBinary()` is closed (one definition, both files call through it); `CLIRootHelpTests`, `CLISessionNameCompletionTests` and `CLISessionsJSONRoundtripTests` still each carry their own small self-contained copy of the same lookup — a deliberate design noted at `CLISessionsJSONRoundtripTests.swift:53-59`, left alone as out of this pass's scope. Seven `status == 0` failure messages in `CLIMatrixITests.swift` (`:74`, `:120`, `:152`, `:179`, `:243`, `:336`, `:372`) interpolated `stderrText` from runs whose environment carries the backend's secret — the CLI itself prints a source label (`OutputFormatter.note("secret source: \(secret.sourceLabel)")`, `Sources/MacSCPCLI/SessionConnecting.swift:45`), never the value, and no rig endpoint URL carries userinfo (`http://127.0.0.1:19000`, `http://127.0.0.1:18080/dav`, credentials passed separately), so nothing had leaked; **closed in the follow-up commit** anyway, per CLAUDE.md's "A value a test must not leak has two exits, not one" — all seven now compute a `leaksSecret` `Bool` before the expectation and quote only the exit status and `stderrText.count`/`stdoutText.count`, never the text. Open: the five rows below, surfaced while building this matrix. |
| S3 `delete(path:)` on a directory is a silent no-op | **Measured 2026-09-04** (Task 2 of the CLI test matrix, probe R5) — sibling of the `deleteTree`-on-a-file row above: same contract, the other operation. `S3FileSystem.delete(path:)` (`Sources/macSCPCore/S3/S3FileSystem.swift:387-395`) resolves its key through `S3FileSystem.RootMode.resolve(path:)` (`Sources/macSCPCore/S3/S3FileSystem.swift:76-94`) — both recounted at this commit's HEAD, the first having drifted from the `:378-382` written when the row was opened, which normalizes the path and splits it on `/` — so a directory's own path (`<name>/`) and the bare name (`<name>`) resolve to the identical key, never the marker key `<name>/` that `mkdir` actually wrote. S3's `DeleteObject` is idempotent and answers success whether or not a key exists, so the call returns cleanly having removed nothing, and the marker key survives in the bucket — observed as two surviving objects in `macscp-seed`, removed by hand. Reachability beyond the harness, read from the call site rather than driven: `macscp-cli rm` (non-recursive) calls this method, but `RmCommand` `stat`s first and `TransferSourceGuard.checkDeletable` refuses a directory before `delete` is ever reached, so the CLI does not expose it; the GUI's call sites were not audited for this one. Open: same choice as the sibling row — either `delete(path:)` is made to notice a resolved key with content beneath it, or the contract is narrowed to files-only and callers that might hand it a directory are made to choose. **Done 2026-09-04** (`e7169c1a`, plus this follow-up commit for the CLI matrix's own workaround). Before: `DeleteObject` answers **204** whether or not the key exists, so a directory's request (the bare, unwritten key) and a genuinely missing key both returned success having deleted nothing. After: `delete(path:)` calls `stat(path:)` first, after the bucket-level guard; `stat` raises `.notFound` itself for a missing key, and a `.directory` entry throws `RemoteFSError.protocolError(reason: "S3 delete: <path> is a directory")` before any DELETE is sent — one extra listing request per call. Gated cases added and green against the rig: `deleteRefusesADirectoryAndLeavesItsMarkerForDeleteTree` and `deleteOnAKeyThatIsNotThereIsNotFound` (both S3, Task 2; the second goes one case beyond the brief, confirming MinIO really answers `DeleteObject` 204 for a key that never existed — the premise the whole lookup rests on). `Tests/macSCPCoreTests/Support/CLIMatrix.swift`'s cleanup no longer needs to know any of this: `removeRemote`/`removeRemoteTree` each make one `deleteTree` call and let `verifyGone`'s own `stat` decide the outcome; `MACSCP_ITEST=1 swift test --filter CLIMatrix` (55 tests) stays green. |
| WebDAV `delete(path:)` on a collection deleted it | **Measured 2026-09-04** (final fix round of the delete-contracts plan) — the third cell of the same backend × operation table as the two rows above, and the only one that was destructive rather than silent. `RemoteFileSystem.delete`'s contract (`Sources/macSCPCore/RemoteFS/RemoteFileSystem.swift:31-34`) says the call removes a FILE, `.notFound` for nothing and `.protocolError` for a directory; `WebDAVFileSystem.delete(path:)` sent `DELETE /dav/<name>` unconditionally, and mod_dav removes a collection addressed that way — recursively, for a populated one. Observed red on the rig before the fix: the Basic CRUD round trip's `delete` on an empty collection threw nothing at all, and the `stat` behind it came back `.notFound(path: "/m21-crud-basic-8E592D39-38A5-46B7-AEE1-67B16F188933/sub-renamed")` (the two issues recorded: `Expectation failed: an error was expected but none was thrown` at `WebDAVFileSystemIntegrationTests.swift:193:19`, and the `Caught error:` from that `stat` at `:122:6`) — the collection really was gone. Reachability beyond the harness, READ FROM THE CALL SITES, not driven through the UI: counted 2026-09-04, `delete(path:)` has exactly ONE caller in `Sources/` outside the backends themselves — `RmCommand` (`Sources/MacSCPCLI/RmCommand.swift:48`), which `stat`s first and lets `TransferSourceGuard.checkDeletable` refuse a directory before `delete` is reached. `RemoteBrowserViewModel.deleteItems` (`Sources/macSCPCore/Presentation/RemoteBrowserViewModel.swift:701`) calls `deleteTree`, never `delete`, and the transfer engine calls neither. So no production path could reach it; unlike its two siblings above, this deviation was reachable only through the protocol itself. **Done 2026-09-04** (this commit). After: `delete` `stat`s first — the same single lookup `deleteTree` does, at the cost measured in the row above — and throws `RemoteFSError.protocolError(reason: "WebDAV delete: <path> is a directory")` for a `.directory`, with `stat` itself supplying the `.notFound` half. Gated: the Basic CRUD round trip now asserts the refusal and reads the collection back BEFORE `deleteTree` removes it (a refusal that deleted first and threw afterwards would pass the thrown error alone). Stubbed: `deleteOnACollectionIsRefusedWithoutSendingADelete` — whose load-bearing assertion is the positive one, that the request list is exactly `["PROPFIND"]` — and `deleteOnAPlainFileLooksUpThenDeletesTheFileURL`. |
| The S3 delete lookup pages the whole parent listing | **Done 2026-09-05.** `delete(path:)` and `deleteTree(at:)` no longer call `stat`; both go through a new private `deleteLookup(path:)` (`Sources/macSCPCore/S3/S3FileSystem.swift:350-379`, returning `DeleteLookup { .file, .directory, .absent }` defined at `:320-324`) — two constant requests instead of `listedEntry`'s page walk over the parent prefix. First a signed `HEAD` on the key itself: 200 is `.file`, 404 falls through, anything else is the ordinary status mapping. Then, only if the `HEAD` 404'd, a one-key `ListObjectsV2` on `<key>/` (`prefix=<key>/&max-keys=1`) answers "is anything at all there" through a new `S3ListParser.hasAnyEntries` (`Sources/macSCPCore/S3/S3ListParser.swift`) — deliberately not the existing `parse`, which drops a `Contents` entry equal to the query prefix as "the directory being listed"; an empty directory's own folder-marker key (`createDirectory`'s zero-byte `<key>/`) IS that entry, so `parse` would have called an empty directory absent. `list` is unchanged: it still goes through `listedEntry`'s paged `CommonPrefixes` walk, which `deleteLookup` never needs. The `RemotePath.normalizedAbsolute(path) == "/"` short-circuit `listedEntry` used for the bucket root in `.bucket` mode is replicated at the top of `deleteLookup` for the same case. `delete`/`deleteTree`'s existing contracts (directory → `protocolError`, absent → `notFound`, plain file → single `DELETE`) are unchanged; only the lookup's cost is. Unit tests (`Tests/macSCPCoreTests/S3FileSystemTests.swift`) pin the request shape with `FakeS3Transport`: `deleteOnAFileSendsHeadThenDeleteAndNeverAParentListing` asserts the exact method sequence `["GET", "HEAD", "DELETE"]` (connect probe, then the two) and a negative beside it (no request after the probe carries a `delimiter` query — the shape only a parent listing has); `deleteOnADirectoryThrowsProtocolErrorAndSendsNoDelete` pins the one-key list's own query (`max-keys=1`, `prefix=sub/`) and answers it with the directory's bare marker key to exercise the empty-directory edge case, throws `protocolError`, sends no `DELETE`; `deleteOnAMissingKeyThrowsNotFoundAndSendsNoDelete` answers the one-key list empty and throws `notFound`. All existing `deleteTree` batching/escaping/per-key-error tests were updated to the new two-request pre-check and stay green, as does every other delete-shaped test in the file (bucket-list mode, the toggle-off root case, the error-path-naming tests). Full unit suite green (`swift test`, 4905 tests, 416 suites). Gated (`MACSCP_ITEST=1`, rig started from this checkout, `down` after): every existing S3 delete/deleteTree case against real MinIO stays green, plus a new `deleteOnAFileCostsExactlyTwoRequestsAgainstMinIO` (`Tests/macSCPCoreTests/S3FileSystemIntegrationTests.swift`) that wraps the real session's transport in a method-recording decorator (the counting seam `S3FileSystem.connect(_:transport:)` exposes) and asserts the exact sequence `["HEAD", "DELETE"]` for a real delete — a bare request COUNT was tried first and rejected: the rig's seed bucket is small enough that the old parent-listing lookup and the new `HEAD` both cost exactly one request there, so only the method sequence (`GET` vs `HEAD`) actually discriminates old from new; proven red against the pre-existing `stat`-based lookup (`deleteMethods → ["GET", "DELETE"]`, stashing the two source files back to HEAD) before being made green here. Full gated suite green too (`MACSCP_ITEST=1 swift test`, 4905 tests, 416 suites). **Updated 2026-09-06**, same effort as the row below ("A key that is both an object and a prefix"): `deleteLookup` (`Sources/macSCPCore/S3/S3FileSystem.swift:362-404`, `DeleteLookup` at `:323-328`, now `{ .file, .directory, .both, .absent }`) runs its one-key list UNCONDITIONALLY — even after a `HEAD` 200 — because a `HEAD` alone cannot tell a plain file apart from a key that is ALSO a prefix; before, the list ran only when the `HEAD` 404'd. `deleteLookup` itself still costs exactly two requests, always; what changed is that a PLAIN FILE's total under `delete` grew from two requests to three (`HEAD`, the one-key list, `DELETE`) — the third is the ambiguity check, not a wider lookup. `deleteOnAFileSendsHeadThenDeleteAndNeverAParentListing` was renamed `deleteOnAFileSendsHeadThenAOneKeyListThenDeleteAndNeverAParentListing` and now asserts `["GET", "HEAD", "GET", "DELETE"]`, with the negative recast as the list's `prefix` naming the key itself (`dir/file.txt/`) and never the shorter parent prefix (`dir/`) a page walk over siblings would use. Four more file-shaped delete tests gained the same extra canned response and stayed green (`deleteNotFoundResponseThrowsNotFound` — whose own comment and assertions were also widened, having been found to pass for the WRONG reason: without the intervening list response, its second canned 404 was silently consumed as the list's answer rather than the `DELETE`'s, `.notFound` either way — plus `aDeleteRoutesIntoTheBucketNamedByThePath`, `oneLevelInsideABucketTheSameOperationsGoThrough`, `anErrorInListModeNamesThePathWithItsBucket`, `withTheToggleOffAnErrorNamesTheSamePathAsBefore`). The gated `deleteOnAFileCostsExactlyTwoRequestsAgainstMinIO` was renamed `deleteOnAFileCostsExactlyThreeRequestsAgainstMinIO` and now asserts `["HEAD", "GET", "DELETE"]`. Full unit suite green again (`swift test`, 4946 tests, 419 suites) and full gated suite green again (`MACSCP_ITEST=1 swift test`, 4946 tests, 419 suites). |
| A key that is both an object and a prefix | **Measured 2026-09-04** (parked by the Task 2 review of the delete-contracts plan, written in the final fix round). S3 permits `a.txt` and `a.txt/child` to coexist, and `listedEntry(at:)` takes `page.items.first(where: { $0.name == leafName })` (`Sources/macSCPCore/S3/S3FileSystem.swift:310`) — the FIRST name match, in the order `S3ListParser` appended it, which is the response's document order. So which kind such a key reports is decided by the server's listing order, not by any rule this code states. The ambiguity is pre-existing; the delete lookups are what made it load-bearing. Where the object wins, `deleteTree` deletes the object and orphans the children — the exact reverse of what it did before the lookup, when it walked the `<key>/` prefix and left the object standing. Probed on the rig 2026-09-04 with both keys written by raw signed PUT: MinIO stores and serves both (`GET .../<key>` → `obj`, `GET .../<key>/child` → `kid`, both 200) but reports NEITHER the child nor a `CommonPrefixes` entry for it in any listing — `?list-type=2&delimiter=/` came back `KeyCount 3` naming only the object, and `?list-type=2&prefix=<key>` `KeyCount 1`. So on THIS server the object always wins and the child is invisible to every listing the code makes, `deleteTree`'s walk included; real S3, which lists both, is where the ordering question is actually live — and that half is UNMEASURED here, no AWS endpoint was called. Both probe keys were deleted afterwards and the bucket verified back at its seed (`a.txt`, `sub/b.txt`). Open: state a rule instead of inheriting the listing's — prefer the prefix for `deleteTree` (delete the object AND the subtree) and refuse for `delete` (ambiguous: `protocolError`), or narrow the question at the source by refusing to create such a key. **Done 2026-09-06.** The rule: `delete(path:)` on a key that is BOTH an object and a prefix REFUSES with `RemoteFSError.protocolError(reason: "S3 delete: <path> is both an object and a folder")`; `deleteTree(at:)` on the same shape removes the object AND the subtree. Stated once, S3-specific and hedged as the backend's own rule, on `RemoteFileSystem.delete(path:)`'s doc (`Sources/macSCPCore/RemoteFS/RemoteFileSystem.swift:31-38`) — `stat` and `list` are unchanged this round, so the listing-order note above still describes what they do; only the delete side no longer depends on it. Implementation: `deleteLookup` (`Sources/macSCPCore/S3/S3FileSystem.swift:362-404`) now runs its one-key list unconditionally, even after a 200 `HEAD`, and reports a fourth case, `DeleteLookup.both` (`:323-328`), when both the `HEAD` and the list say yes; `delete` (`:475-493`) refuses on `.both`; `deleteTree` (`:628-680`) treats `.both` like `.directory` for the existing prefix-batch walk and then issues one more `DELETE` on the bare key afterward, since that walk enumerates `<key>/*` and never reaches the bare key itself (`resolvePrefix` always appends the trailing slash). Unit tests (`Tests/macSCPCoreTests/S3FileSystemTests.swift`, `FakeS3Transport`): `deleteOnAKeyThatIsBothAnObjectAndAPrefixRefusesAndSendsNoDelete` answers `HEAD` 200 and a non-empty one-key list, asserts the exact both-error and the exact request sequence `["GET", "HEAD", "GET"]` with a negative beside it (no `DELETE` is ever sent); `deleteTreeOnAKeyThatIsBothAnObjectAndAPrefixRemovesTheObjectAndTheSubtree` asserts the exact sequence `["GET", "HEAD", "GET", "GET", "POST", "DELETE"]` — `HEAD`, the one-key list, the subtree list, the batch `DeleteObjects`, then the bare key's own `DELETE` last — with the batch body pinned to contain the child's key and not the bare key. All plain-file and directory delete/deleteTree tests stay green (see the row above for what else they had to absorb once the list started running unconditionally). Full unit suite green (`swift test`, 4946 tests, 419 suites). Gated (`MACSCP_ITEST=1`, rig started from this checkout, `down` after): every existing S3 delete/deleteTree case against real MinIO stays green (`MACSCP_ITEST=1 swift test --filter S3`, 289 tests, 42 suites; full gated `swift test`, 4946 tests, 419 suites) — but NO gated test exercises `.both` itself. Attempted and measured instead of assumed: a key `x` and a key `x/child` were written through `fs.write` and then probed directly against the running container (`docker exec macscp-test-minio mc find/ls/cat`, 2026-09-05/06), reconfirming this row's own 2026-09-04 finding from the opposite direction — `mc cat` retrieves `x/child` (the object exists, a direct GET answers it) but `mc find` and `mc ls --recursive` never list it while `x` also exists as a bare key, in either creation order, and removing `x` makes it reappear in listings. `deleteLookup`'s one-key `ListObjectsV2` on `x/` is exactly this kind of call, so on THIS rig `delete("x")` never observes the ambiguity and never refuses — a rig limitation (documented as a comment in `Tests/macSCPCoreTests/S3FileSystemIntegrationTests.swift`, before `RecordingHTTPTransport`), not a code defect. Open: the `.both` code path is proven only by the unit tests' canned responses; real AWS S3, the one server this row already flagged as listing both halves, remains unmeasured — no AWS endpoint was called here either. |
| No `ProtocolCapabilities` axis describes host keys | **Measured 2026-09-04** (Task 3 of the CLI test matrix). `ProtocolCapabilities` carries `supportsShell`, `permissionModel`, `supportsSymlinks`, `atomicRename`, `directoriesAreReal`, `resumeMode`, `supportsPresignedURL`, `supportsRemoteChecksum`, `transport` — none says whether the client remembers the server's identity, and `transport` is the near-miss wrong answer: SSH is `.alwaysEncrypted`, S3/WebDAV `.optionalTLS`, but a WebDAV vhost on TLS is still `.optionalTLS` and still has no host key. `CLIMatrix.hasHostKeys(operation:)` (`Tests/macSCPCoreTests/Support/CLIMatrix.swift:485`) and the file commands' own gate, `CLIMatrix.supports(_:named:operation:)` (`:440`), instead read the answer off an exhaustive `switch` over the connection config the CLI itself builds — a structural boundary (a fourth protocol does not compile until someone says which arm it takes) rather than a capability claim. Open: a real `hasHostKeys` (or `serverIdentity`) axis on `ProtocolCapabilities` would turn this into an actual capability gate and give the GUI one place to ask the same question, instead of the matrix's own derived switch being the only place it is answered. Not designed here — this task was test-only. **Done, this commit**: `ProtocolCapabilities` gained `authenticatesHostKey: Bool` (`Sources/macSCPCore/Capabilities/ProtocolCapabilities.swift`). Counted 2026-09-04 with `grep -rn "ProtocolCapabilities(" Sources Tests`: exactly 3 init call sites exist, all in `BackendDescriptor.swift` — `sshDescriptor` (`:430`, `authenticatesHostKey: true`), `s3Descriptor` (`:464`, `false`), `webdavDescriptor` (`:499`, `false`); no test constructs a `ProtocolCapabilities` of its own. `CLIMatrix.hasHostKeys(operation:)` now reads `capabilities.authenticatesHostKey` instead of its own exhaustive switch. `BackendDescriptorTests.authenticatesHostKeyMatchesWhichBackendsConnectClosureUsesTheDecider` PINS the three values in a hand-written exhaustive switch over `ConnectionKind` — a second, independently-typed COPY of the same three booleans, not a derivation of them, so a fourth backend does not compile there until someone says which side of this it is on. **Fix round, 2026-09-04** (on `cab8c7b4`): a reviewer caught this row, and that test's own doc comment, both calling the switch above "an independent derivation of the same fact" — nothing in it reads `BackendDescriptor.swift`, and both are corrected to say "pin" rather than "derivation". The actual derivation is new, this round: `BackendDescriptorHostKeyWiringGuardTests` (`Tests/macSCPCoreTests/BackendDescriptorHostKeyWiringGuardTests.swift`), a source-scanning guard over `BackendDescriptor.swift`'s comment-and-string-blanked text — it locates each of the three `ProtocolCapabilities(` init sites and the `connect:` closure that follows it (both by brace/paren balance), and asserts the closure whose descriptor declares `authenticatesHostKey: true` names its `HostKeyDecider` parameter and forwards it into the body, and the closures whose descriptor declares `false` discard it (`_`, or named but unused). Two positive companions (exactly 3 descriptors found; the SSH closure's body really does mention its decider) sit beside the negative, and four scanner self-tests drive the same parsing function both directions over synthetic sources. Mutation-tested (`scripts/mutation-probe`, staged first, reverted after): flipping S3's `authenticatesHostKey` to `true` (its closure still discards the decider as `_`) — `RESULT: RED — 7 test(s) ran, and the plant was caught by: macSCPCoreTests.BackendDescriptorHostKeyWiringGuardTests/everyDescriptorsHostKeyClaimAgreesWithWhetherItsConnectClosureForwardsTheDecider()/BackendDescriptorHostKeyWiringGuardTests.swift:63:6`. `MACSCP_ITEST=1 swift test --filter CLIMatrix` (65 tests) and `swift test` (full suite) stay green. |
| `--non-interactive` cannot be told apart from its own absence in this harness | **Done 2026-09-05.** Closed by a real pseudo-terminal: `Tests/MacSCPTestSupport/PTYSubprocess.swift` (`posix_openpt`/`grantpt`/`unlockpt`/`ptsname_r`, then `posix_spawn` with the slave opened INSIDE the child via `posix_spawn_file_actions_addopen` for fd 0 — `adddup2`ing a parent-opened slave onto 0/1/2 does not acquire a controlling terminal, measured against `/usr/bin/tty`; only a fresh `open(2)` by a `POSIX_SPAWN_SETSID` session leader does) drives the built CLI with `isatty(stdin)` genuinely true. The measurement: `CLIMatrixSSHITests.nonInteractiveUnderAPTYRefusesWithoutPrompting` (`Tests/macSCPCoreTests/CLIMatrixITests.swift:1044`) runs `ls --non-interactive --json <target>` against a fresh known-hosts file under the PTY — the prompt text `Trust this host? [y/N]` (`Sources/MacSCPCLI/SessionConnecting.swift:147`, one occurrence in `Sources/`, counted by `grep -rn`) does NOT appear, the refusal text does, exit code 11 (`CLIExitCode.hostKeyUnknown`); `askUnderAPTYPromptsAndNoRefuses` (`:1076`) runs the identical command WITHOUT the flag under the same PTY — the prompt DOES appear, the case writes `n\n`, and the CLI refuses with the same exit code 11. Under a real terminal the flag is provably the only difference between the two outcomes, closing the gap `CLIMatrixCases.anUnknownHostKeyIsRefusedUntilAccepted` could not: that case runs under `SubprocessRunner`, which hands every child `/dev/null` for stdin, so `HostKeyPolicy.decision(for: .ask, hasTTY: false)` already resolves to `.reject` before the flag is read at all, and the refusal it measures is the POLICY's, not the flag's. Production code needed no change — the row's own hedge against fixing something speculatively did not apply, because the flag already works; only the harness lacked a terminal to prove it. Measured separately, in the course of building the PTY runner: `DispatchSource.makeProcessSource(identifier:eventMask:.exit)` crashes this toolchain with `SIGTRAP` the instant its event handler fires (reproduced eight times, including with an empty handler), so the child's exit is reaped on a dedicated `Thread` blocking in `waitpid(2)` instead; and a PTY master read after `waitpid` confirms the child has exited loses ALL buffered output — 40 of 40 concurrent runs and 5 of 5 serial runs came back with zero bytes — unless a SECOND slave reference, held open by the parent and closed only once the child is confirmed dead, keeps Darwin's tty driver from tearing the line down (and discarding its queue) the moment the child's own three fds close; both are written up in `PTYSubprocess.swift`'s own doc comment. |
| `macscp-cli help <unknown-subcommand>` exits 0 and prints the root help | **Measured 2026-09-04** (Task 1 of the CLI test matrix; re-measured 2026-09-04 against the built binary for this row: `macscp-cli help definitely-not-a-subcommand` → exit 0, root `OVERVIEW`/`USAGE` printed). Same for `macscp-cli <unknown> --help` — ArgumentParser's own behaviour, not something macSCP controls. `CLIMatrixCommands.theSubcommandsComeFromTheBinaryItself` (`Tests/macSCPCoreTests/CLIMatrixITests.swift:1284`) discriminates on the USAGE line instead of the exit code for exactly this reason: a real subcommand's help says `USAGE: macscp-cli <name> …`, an unknown one falls back to the bare `USAGE: macscp-cli <subcommand>`. Open: nothing to fix in this project; recorded because a script or a user relying on the exit code to detect an unknown subcommand name would be surprised by it. **Done, this commit**: `MacSCPCLI.main()` (`Sources/MacSCPCLI/MacSCPCLI.swift`) now checks `CommandLine.arguments` for the `help <name>` shape BEFORE calling `asyncParseAsRoot()`: a `<name>` that is not one of `configuration.subcommands.map { $0._commandName }` prints `Unknown subcommand '<name>'` to stderr (`OutputFormatter.note`) and exits `CLIExitCode.usage` (2), instead of ever reaching ArgumentParser's built-in `HelpCommand` — whose `buildCommandStack(with:)` walks as far into the subcommand tree as the given names match and stops at the root the moment the first one does not, which is what produced the exit-0-with-root-help behaviour this row measured. `help ls` (a real subcommand) and `help` alone (no name at all) are both unaffected, still exit 0; the root `--help` output the coverage guard (`CLIMatrix.subcommands(binary:)`) parses is untouched, since this only intercepts the `help <name>` shape, never `--help`. `macscp-cli <unknown> --help`, the sibling case this row also named, is unaffected by this commit — a different, parse-time path through ArgumentParser, out of scope here. `CLIMatrixCommands.theSubcommandsComeFromTheBinaryItself` (`Tests/macSCPCoreTests/CLIMatrixITests.swift`) no longer carries the stale negative half that pinned the old exit-0 fallback; `helpOnAnUnknownSubcommandIsAUsageError`, added beside it, now pins the fix — `help definitely-not-a-subcommand` → exit 2 with stderr naming it, `help ls` → exit 0 as the required positive. Observed red before the fix (`unknown.status → 0` against the expected `2`, empty stderr), green after. `MACSCP_ITEST=1 swift test --filter CLIMatrix` (65 tests) stays green. |
| Six stale `macscp-cli-matrix-*` temp directories, observed once, cause undetermined | **Observed 2026-09-04** (Task 3 of the CLI test matrix, before that task's first test run): six `$TMPDIR/macscp-cli-matrix-*` directories sharing one mtime, 01:29:26 — during Task 2's work window (its commit lands 02:47:31), about 22 minutes before Task 3's first build. Five were empty; one held `sessions-v2.json` naming a single `capabilities-*` session — the label `CLIMatrixCoverage.anOperationIsGatedByTheBackendsOwnCapabilities` uses, a case that tears down normally. Cause not determined: an interrupted `swift test` run fits the one directory with a session file, not the five empty ones, and no theory was pursued further than that. Removed by hand; every run made since — Task 3's own three, and this task's `swift test`, `MACSCP_ITEST=1 swift test` and gated filter — left zero `macscp-cli-matrix-*` and zero `cli-matrix-local-*` directories behind. **Closed 2026-09-06** (investigation, no code change). The one directory carrying a `sessions-v2.json` for a `capabilities-*` session is caused by the `defer`-after-the-loop bug in `anOperationIsGatedByTheBackendsOwnCapabilities` (fixed in `4842adf2`, 2026-09-04, and still in place at HEAD — confirmed with `git merge-base --is-ancestor 4842adf2 HEAD` and `git log --oneline -3 -- Tests/macSCPCoreTests/CLIMatrixITests.swift`), whose own comment at `CLIMatrixITests.swift:1765-1768` records exactly this fix: the test used to collect every rig into a dictionary across the loop and `defer` its cleanup only afterward, so a run that stopped partway through — before the fix landed at 02:14:11, minutes after the six directories' shared mtime of 01:29:26 — left every rig made so far, including the one whose session file survived, with no handle to tear it down. The five empty directories match no code path in `CLIMatrix.make` (`Tests/macSCPCoreTests/Support/CLIMatrix.swift:130-151`): `createDirectory` (`:134-135`) is immediately followed by the one write that can fail (`SessionStore(...).upsert`, `:143`), wrapped in a `do`/`catch` that self-removes the directory on any throw (`:144-146`), and all but one of the fifteen call sites pair `try CLIMatrix.make(...)` with a `defer { rig.tearDown() }` on the very next line — the sole exception being the loop case just above, which defers even earlier, before the loop starts. That leaves only a hard kill landing in the narrow window between `createDirectory` succeeding and the write completing — a gap no `defer` can protect, since a killed process runs no deferred code at all — as CONSISTENT WITH the five empty directories; this is not established as their cause, only as the one gap the code's own structure cannot close. No recurrence since: every run made in this task (`swift build --build-tests`; `swift test`, 4946 tests; `MACSCP_ITEST=1 swift test --filter S3`; the full gated `MACSCP_ITEST=1 swift test`, 4946 tests, run twice) left zero `macscp-cli-matrix-*` and zero `cli-matrix-local-*` directories in `$TMPDIR` afterward. A repro (SIGKILL an early `MACSCP_ITEST=1 swift test --filter CLIMatrix` run and inspect `$TMPDIR`) was not run because it would only confirm what the code already cannot prevent — a kill between two lines defeats any `defer`, and no version of `make` could close that gap without a mechanism (a lock file, a journal) this project has never needed for a test fixture. |
| [Third-party notices](superpowers/specs/2026-08-20-backlog-dependencies.md) | **Done 2026-09-03.** `scripts/third-party-notices` generates `THIRD_PARTY_NOTICES.md` from `Package.resolved` (`9b86f1d3`, 12 pins, 12 sections, both forks name their upstream); `ThirdPartyNoticesTests` pins that every identity has a section (`e16b14ae`, red proven by a renamed heading, full suite 3732 green), hardened by `0ef63269` (`test(notices): a heading inside a licence fence is not a section`) so a `## ` line inside a fenced licence block cannot stand in for a real section — a fourth test, full suite 3735 green. Correction to the plan's premise: swift-crypto 3.15.1 has no standalone BoringSSL LICENSE under `Sources/CCryptoBoringSSL` — the vendored sources carry per-file Apache-2.0 headers instead, so the notices file holds swift-crypto's own `LICENSE.txt`/`NOTICE.txt` and does not name BoringSSL separately; a later pin that vendors a BoringSSL LICENSE would be picked up by the generator but is not checked by the test. **Residuals done, this commit.** `--check` now runs as its own CI step ("Third-party notices are up to date", right after Build, since it needs `.build/checkouts/`) — a pin bump that leaves the file stale now fails CI instead of leaving all four `ThirdPartyNoticesTests` cases green. `scripts/package-app` copies `THIRD_PARTY_NOTICES.md` into `Contents/Resources/` (the same two-line copy-then-`test -f` shape already used for `CHANGELOG.md`), closing the legal-notice gap for distributed builds; the scratchpad dev-build recipe got the same line. An unknown flag (`scripts/third-party-notices --bogus`) now exits 2 with a usage line instead of silently regenerating and overwriting the committed file — a `case` statement replaced the old single `--check` comparison. Five new `ThirdPartyNoticesTests` cases pin all three: two read-and-non-vacuous positives (`packageAppScriptIsReadable`, `ciWorkflowIsReadable`), the packaging-script and CI-workflow content checks themselves, and the unknown-flag refusal (paired with `--check` still working, run through `SubprocessRunner` against the real script). Verified red beforehand by stashing the script fix and re-running the new subprocess test (3 issues), restored clean. |
| [Host-key types in the rig](superpowers/plans/2026-09-02-host-key-types-rig.md) | **Done 2026-09-02.** One sshd per host-key type (ports 2231–2235), `ssh-keyscan` proof in the rig README, one gated test per type pinning the recorded key type, a mismatch hard stop on ECDSA. Measured: ed25519 and all three ECDSA curves green; **a server with only an RSA host key could not be connected to** that morning (`keyExchangeNegotiationFailure`, the client offered no RSA algorithm) — fixed the same evening, see the RSA host keys row below. File keys per type were already pinned; agent auth per type came with B-2. Record in the key-formats entry. |
| [RSA host keys](superpowers/plans/2026-09-02-rsa-host-key-fork-change.md) | **Done 2026-09-02.** Fork 0.3.9 gave `NIOSSHPublicKeyProtocol` a `hostKeyAlgorithmNames` beside its blob prefix; macSCP registers one pair (`ssh-rsa` blob, offered as `rsa-sha2-512`, PKCS#1 v1.5 over SHA-512 via `_CryptoExtras`) before every connect. The gated RSA row flipped green: port 2235 records `ssh-rsa`, fingerprint identical to `ssh-keyscan`'s; a tampered RSA key is a hard stop; a modulus below 1024 bits (OpenSSH's floor) is not a key. Verification proved by unit tests (valid / flipped byte / wrong digest) and four planted defects. Only `rsa-sha2-512` is offered; a server restricted to `rsa-sha2-256` still fails — add the second digest if one is measured. Upstream PR candidate: the fork change. |
| **B-1** | [Bug list](superpowers/specs/2026-08-20-bugs.md) | Connecting to a dead host blocks the app. Cause measured: Citadel's deadline is set to 30 s and was never passed through. **Fixed** — the deadline is passed through and the attempt is cancellable, and the open half was **measured 2026-08-28** (`ConnectMainActorLivenessTests`): the main thread does not block; it was an operator-facing bug. Still unmeasured: a real hanging nameserver (needs privileges the rig lacks). |
| **B-2** | [SSH key formats](superpowers/specs/2026-08-31-backlog-ssh-key-formats.md) | **Done 2026-09-02** (12 commits). The loader names the key type before parsing (Citadel\'s `SSHKeyDetection`); the message says what the key is and that RSA/ECDSA connect through the ssh-agent — with the **verified** Go-server RSA caveat named, which the entry had wrongly called unmeasured. Measured on the rig: P-384, P-521 and a passphrase-protected key through the agent. RSA from a file still cannot connect (Citadel signs SHA-1); the Go-server case is now fixable in the project\'s own swift-nio-ssh fork — see [RSA via agent vs Go servers](superpowers/specs/2026-09-01-backlog-rsa-agent-go-servers.md). **2026-09-02, later:** the agent path is a ten-cell matrix (five types × with/without passphrase), all green. **File keys done 2026-09-02:** RSA and ECDSA private key files authenticate directly (Citadel fork `0.12.1-noix.2`: RSA-SHA2 from upstream PR #135 with the SHA-1 fallback off, an ECDSA container parser); OpenSSH 10.3 accepts the fork's blob typing (measured); ten of ten file cells green; the key manager offers all three types. **Go-based servers accept RSA since 2026-09-02 (night):** swift-nio-ssh fork 0.3.10 (`userAuthAlgorithmName`) + Citadel `0.12.1-noix.3` + the agent type type the blob `ssh-rsa` and keep `pkalg`/signature `rsa-sha2-512`; measured on an SFTPGo container in the rig (refused before, accepted after, file and agent), OpenSSH unchanged. |
| **B-3** | [Connecting to S3 without a bucket](superpowers/specs/2026-08-31-backlog-s3-without-bucket.md) | **Done 2026-09-02** (both halves). A visible "Start at the bucket list" toggle; `ListBuckets` on connect with named outcomes; bucket rows open and nothing else — every transfer entry point asks one predicate in both directions; list mode stores no bucket. Measured on the rig with a root key and a scoped key (MinIO filters the list rather than denying it). A follow-up from another session (`78a0916f`) made the permissions editor follow `permissionModel`: S3/WebDAV files get an "Info" sheet without an editor. Left open: Servinga unmeasured. |
| A local or dynamic forward's per-connection failures are never reported | **Open, recorded 2026-09-06** by the final review of the port-forwarding plan. `LocalForwardListener.start` and `SOCKS5Listener.start` both take an `onFailure:` and `RemoteForward.start` an `onConnectionFailure:` — one call per accepted connection whose channel through the server could not be opened — and `LiveTunnelRuntimeFactory.start` (`Sources/macSCPCore/Tunnels/TunnelRuntime.swift`) passes none of the three. So in production a `-L` whose target refuses the connection gives the user a closed socket and NOTHING else: no diagnostic-log line, no state change, the tunnel still `.active`. The parameters exist and the unit tests drive them, which is why this went unnoticed — the seam is built, it is simply not wired. **The fix shape**: the factory forwards each failure into the runner's observer as a `.debug` line in the `tunnel` category (the per-connection lines already go there), and repeated failures — a target that is down rather than one connection that lost a race — reach the state, so a forwarding nobody can use stops looking healthy. The threshold is the open question: the tunnel itself is up, so `.failed` is a lie, and a new case is a model change. **Done 2026-09-17** (`b9ee7d22`, `bc639257`, technical-backlog plan Task 7): `.active` gained `failedConnections: Int = 0` and `lastFailure: TunnelFailureKind? = nil`; a per-connection failure logs one `.debug` line (`tunnel <name> connection failed port=<bound> <kind.sentence>`, no `reason=`, no payload text) and increments the count and last failure, the next successfully opened connection resets both to 0/nil, and entering `.active` from `.connecting`/`.reconnecting` (including after a reconnect) starts at 0/nil — matching the maintainer's 2026-09-16 decision ("Log + Zähler im Status"); no new lifecycle state. `stateLabel` shows "Active · %lld connections failed" (stringsdict, four languages) once `failedConnections > 0`, and a new `stateTooltip(_:)` carries the translated last failure on all four surfaces (profiles sheet, autostart sheet, Dock, sidebar). The threshold question stays open, by the same decision: a forwarding failing every connection still shows a green glyph and badge — see the new row below. |
| A jump hop added after a slot drop cannot authenticate | **Open, recorded 2026-09-16** by the final review of the maintainer-decisions plan. The drop predicates (`SessionListViewModel.sessionServesAJumpHop(_:)`, `setServesAJumpHop(_:)`, `Sources/macSCPCore/Presentation/SessionListViewModel.swift:875`, `:863`) keep a slot only while a jump hop reads it AT DROP TIME. A jump added AFTER the drop reads the now-empty slot: the jump path fills `form.jumpPassword = resolved.login.secret ?? ""` (`Sources/MacSCPAppKit/ContentView.swift:2913`, `:2961`) and `ConnectionViewModel.buildJumpConfig` passes `passphrase: jumpPassword.isEmpty ? nil : jumpPassword` (`Sources/macSCPCore/Presentation/ConnectionViewModel.swift:1954`); no `ManagedKeyPassphrase.resolve` sits on that path, so the hop has no passphrase. The durable fix is that fallback on the jump path, which would retire both jump predicates. **Done 2026-09-17** (`561fc589`, technical-backlog plan Task 5): a jump hop whose login is a private key and whose own/set/referenced slot is empty now resolves the passphrase from the managed key's slot too, through `LoginResolver.fallingBackToManagedKeyPassphrase(_:keys:secrets:)`, reached by `resolvedJumpLogin(for:)`/`resolvedJump(for:)`/`fillJumpForm`. The jump predicates were **not** retired: `sessionServesAJumpHop`/`setServesAJumpHop` still gate the slot-drop rule of decision (a) in the PEM design — their only callers are its two slot drops, `Sources/MacSCPAppKit/ContentView.swift:2665` and `:2741` — kept on purpose, as defence in depth, not retired as this row once expected. The new save guard (`jumpUsesStoredManagedPassphrase`), which stops the fallback value from being written back into the jump's own slot on save, asks `SessionSecretPolicy.usesStoredManagedPassphrase`, not either predicate (this row first said the predicates gated it; corrected 2026-09-17 at the plan's final review). See the PEM design doc's "Decisions 2026-09-16" for the correction. |
| "Update login set" drops the set's slot and redials after a failed save | **Open, recorded 2026-09-16** by the final review of the maintainer-decisions plan. `SessionListViewModel.saveLoginSet(_:secret:)` (`Sources/macSCPCore/Presentation/SessionListViewModel.swift:919-936`) returns nothing and reports a failed `upsert` only through `errorMessage`; `ContentView.repointLoginSet(_:)` (`Sources/MacSCPAppKit/ContentView.swift:2720-2730`) goes on to the probe/jump gate, may call `dropLoginSetSecret(for:)`, and calls `retryConnect(_:)` either way — so a set whose new key path was never written can lose its own slot and redial against the old PEM path. **Done 2026-09-17** (`615a417f`, technical-backlog plan Task 5): `saveLoginSet(_:secret:)` is `@discardableResult` and now returns whether the write succeeded; `repointLoginSet` drops the slot and redials only behind a positive check of that result (`guard sessionListViewModel.saveLoginSet(set, secret: nil) else { return }`, `ContentView.swift:2732`), and a failed save leaves both the slot and the dial alone, with `errorMessage` already showing the failure. A dedicated guard (`ConvertKeyWiringGuardTests.updatingTheSetDropsAndRedialsOnlyAfterASuccessfulSave`) requires the condition to read the save call positively and every drop/redial after it. |
| A corrupt `managed_keys.json` surfaces only as a missing passphrase | **Open, recorded 2026-09-16** by the final review of the maintainer-decisions plan. Since `12573d5a` an unreadable key store answers nil in `ManagedKeyPassphraseSecretSource.secret(for:)` (`Sources/macSCPCore/Sessions/ManagedKeyPassphraseSecretSource.swift:60`) — deliberately, so sessions whose key it does not manage are not stopped. The cost: a session using a MANAGED encrypted key with an empty own slot then fails later, asking for a passphrase, with nothing pointing at the store. Not started. |
| An unattended CLI run may need a second Keychain consent | **Open, recorded 2026-09-16** by the final review of the maintainer-decisions plan. The CLI chain's last link, `ManagedKeyPassphraseSecretSource` (`Sources/macSCPCore/Sessions/CLISecretSources.swift:223`), reads the managed key's own Keychain item, a different item from the session's; macOS asks consent per item, so a cron or CI job granted only the session's item can fail on the key's. Read from the code, not measured with a signed binary. Reached for any SSH private-key session with an empty own slot whose key is managed and encrypted. Not started. |
| A tab dial against a server without the SFTP subsystem never returns | **Open, recorded 2026-09-17** by Task 8 of the technical-backlog plan, while measuring the new `sshd-nosftp` rig service (127.0.0.1:2236). NIOSSH completes the subsystem-request write locally; the server's refusal only raises an inbound `ChannelFailureEvent`, which Citadel's SFTP handlers ignore, and `openSFTP`'s chain waits on the server's version reply, `client.responses.sftpVersion.futureResult` (`.build/checkouts/Citadel/Sources/Citadel/SFTP/Client/SFTPClient.swift:566-567`), with no timer of its own — the connect timeout does not cover it. Cancel abandons such a dial rather than ending it, leaking one SSH connection (and its dedicated agent-auth event-loop group) per attempt. "Open Terminal" dials SFTP too (`CitadelFileSystem.connect`), so a terminal-only user cannot reach such a server at all. Measured: a first red run of the gated control test hung past its 5-minute suite limit and was killed by hand; the gated suite now avoids awaiting such a dial directly, racing it against the server's own refusal log line instead, and abandons one suspended dial per run. Fix shape: a timeout on `openSFTP`'s version wait, or reuse of `SSHForwardingConnection`'s no-SFTP path for tabs behind a fallback. **Done 2026-09-17** (`19c6dc70`, the next-build plan's Task 1): the SFTP version wait is now bound by the session's own connect timeout; on expiry the client is closed through the existing delayed group release and the dial throws a new `SFTPStartError.noResponse`, mapped to catalogue key `core.connect.sftpUnavailable` in four languages ("This server did not start SFTP. It may not offer SFTP at all."), `DialSupport.reason(for:)`'s fixed sentence ("the server did not start the SFTP subsystem") and `ConnectFailureKind.other`. Cancelling such a dial now closes the client instead of abandoning it. Gated `ForwardingWithoutSFTPITests`'s control case asserts the typed error directly, replacing the log-racing workaround. Residual, its own row below: a dial that fails partway can still release the agent-auth event-loop group while Citadel's login timer is pending. |
| A dial that fails partway shuts the agent-auth group down while Citadel's login timer may still be pending | **Open, recorded 2026-09-17** by Task 8's fix round 1 of the technical-backlog plan. Citadel's `ClientHandshakeHandler.init` (`ClientSession.swift:76-83`) schedules a 10 s login timeout per hop that is never cancelled by a failed dial. The success path now outlives that timer before releasing its dedicated event-loop group (`CitadelFileSystem.releaseAfterCitadelTimer`, added in `feb0a55e` for `SSHForwardingConnection.disconnect()` and both tab release sites), but the FAILURE path — a host-key rejection or an auth failure partway through the handshake, for tabs and forwardings alike — still shuts the group down at once while that timer may be pending. Not reproduced in three quick dials; recorded as the gap the fix round's own report left open. Not started. |
| A stopped forwarding attempt's buffered connection reports still publish intermediate `.active` states while they drain | **Open, recorded 2026-09-17** by Task 7's fix round 1 of the technical-backlog plan. `TunnelRunner.releaseCurrent()` now awaits the per-attempt report reader before returning, so a stopped attempt's reports can no longer reach the *next* attempt — but the reader still drains and PUBLISHES every buffered report of the stopping attempt itself first, as intermediate `.active` states and `.debug` log lines, before `.stopped`. A test with 20,000 buffered `channelOpenFailed` reports measured this at 0.07 s and found it cheap there, but nothing bounds the cost if the buffer were larger, and dropping reports once `stop()` had been called would be cheaper still. A related gap: the wait is correct only because a finished stream guarantees its reader ends — nothing bounds `stop()`'s wait on the reader itself, so a reader that regressed to not ending would hang it, and with it the quit sequence (today mitigated by `QuitWatchdog`/`BoundedStep`, not by a bound on `stop()`). Fix shape: drop reports once stopping began, and/or give the reader await its own bound. **Done 2026-09-17** (`9a96e1c9`, the next-build plan's Task 2): a report that reaches the runner once `stop()` has begun is now dropped instead of applied as an intermediate `.active` state and log line; the reader still drains and is still awaited on release, and the next attempt's reports are applied again. The reader-await-its-own-bound half stays open (not attempted). |
| A SOCKS5 reply write's own handler-removal failure is no longer reported | **Open, recorded 2026-09-17** by Task 7's fix round 1 of the technical-backlog plan. The maintainer's ruling that a failed `confirm` write is always the client's failure, not the tunnel's, made `LocalForwardListener.accepted`'s `catch where replying` branch stop calling `onFailure` for it — but that branch also catches a DIFFERENT failure from the same call: `SOCKS5Handshake.succeed`'s own `removeHandler`, which is an internal step, not a client-caused one, and is now just as unobserved as the confirm-write failure it was folded in with. Fix shape: tell the two apart before deciding whether to report. Not started. |
| A stop race in `RemoteForward.swift` reports the same stop inconsistently, and a reconnect can show a transient failure count | **Open, recorded 2026-09-17** by Task 7's review of the technical-backlog plan (deferred M2/M3). Two `guard open.track(...)` stop races throw the identical `TunnelFailure.connectFailed(reason: "the forward has been stopped")` (`RemoteForward.swift:287` and `:321`), but only the second is inside the surrounding `do`/`catch` that maps it through `pairFailure` and calls `onConnectionFailure?` — so stopping mid-connect is counted as a connection failure in one case and silent in the other, for the same event. Separately: because a connection's own failure and the forward's overall reconnect share one report stream, a transient "N connections failed" can flash in the state before it settles into `.reconnecting` when the underlying SSH connection drops mid-flight. Not started. |
| A forwarding that fails every connection still shows a green glyph and badge | **Open, recorded 2026-09-17**, the residual of "A local or dynamic forward's per-connection failures are never reported" above (now Done): per the maintainer's 2026-09-16 decision ("no new lifecycle state"), `failedConnections`/`lastFailure` are visible only in the label and tooltip; the glyph and badge colour stay green for an `.active` tunnel regardless of how many — or what fraction — of its connections are failing. The threshold question the old row raised (how many/what fraction before it stops looking healthy) is unresolved by design, not forgotten. Not started. |
| A stale own jump slot wins over the managed key's slot, and the save guard silently skips a typed correction | **Open, recorded 2026-09-17** by Task 5's review of the technical-backlog plan (deferred M4). `LoginResolver.fallingBackToManagedKeyPassphrase` only applies when the jump's own/set/referenced slot is EMPTY; a stale value already sitting in that slot still wins, the same trap the target login already has. The new save guard (`jumpUsesStoredManagedPassphrase`) compounds it: it was added so a form filled by the fallback and then saved could not duplicate the passphrase into the jump's own slot — but by the same logic it also skips writing a typed correction into an already-non-empty jump slot, silently. Not started. |
| A saved forwarding refused by an unreadable store still stops its running tunnel first, and three smaller gaps beside it | **Open, recorded 2026-09-17**, residual of "`TunnelStore` writes rewrite an unreadable file from empty" above (now Done). `TunnelManager.save`/`remove` call `discardRunner` **before** the store write, so a save or delete refused over an unreadable `tunnels.json` still stops that profile's running tunnel although nothing was actually changed on disk. Three smaller gaps from the same fix: orphan rows of a session deleted while the store was unreadable resurface as stopped rows, invisible in any sheet but present in `allProfiles`, once the file is repaired; the CLI's `sessions rm` prompt and `--verbose` output say "0 forwardings" for a session whose forwarding count could not be read, rather than saying the count is unknown; and CLI exit code 13 is documented at `CLIExitCode.swift:22` as a transport failure ("the transport failed before authentication — DNS, TCP, TLS") but is now also returned for an unreadable forwarding store, which is not that. **Done 2026-09-17** (`208c1f32`, `8bd29c25`, `5fc4f876`, `ece85285`, `cf5f2f20`, `c043c8f1`, `d94f962d`, the next-build plan's Task 2): `TunnelManager.save`/`remove` now write the store before calling `discardRunner`, so a refused write leaves a running tunnel running (`aRefusedSaveOrRemoveLeavesTheRunningTunnelRunning`). Orphan rows are filtered by one shared `listed(_:known:)`, used by `init`, `reload()` (all four of its callers: `startAutoStart`, `reloadAutoStartProfiles`, `save`, `remove`) and the activation reconcile, so a session deleted while the store was unreadable no longer resurfaces once the file is repaired, and the reconcile now also discards any runner it does not list. `sessions rm` no longer promises a deletion it will not do: over an unreadable store the question asks about the session alone ("Delete session X? Its forwardings could not be read and will be left in the forwarding list.") and the `--verbose` summary says the count is unknown ("Deleted X; the number of its forwardings is unknown (the forwarding list could not be read); keychain entry left in place.") instead of claiming "0" forwardings were deleted (`SessionRemovalWording.swift`). `CLIExitCode.swift:23`'s `connection` bullet now also names an unreadable forwarding list and an unreadable/unwritable session store. |
| `tunnels start --json` falls back to the kind's generic sentence when the runner holds no reason | **Open, recorded 2026-09-17** by the final review of the technical-backlog plan (Task 6 review, deferred M4). The `failed` line's `reason` is written as `reason ?? kind.sentence` (`Sources/macSCPCore/CLI/TunnelStartRendering.swift:90`; the text line does the same at `:197`), where `reason` is `TunnelRunner.failureReason` (`Sources/macSCPCore/Tunnels/TunnelRunner.swift:149`, read at `Sources/macSCPCore/CLI/TunnelForegroundRun.swift:215`). A `.failed` state whose runner recorded no reason therefore prints the kind's generic sentence, and nothing in the line marks that the detail is missing, so a script reading `reason` cannot tell a generic answer from a specific one. Whether any production path reaches `.failed` without a reason is not measured. Not started. |
| A per-connection failure logged while a forward is torn down prints `port=-` | **Open, recorded 2026-09-17** by the final review of the technical-backlog plan (Task 7 review, deferred M4). `TunnelRunner.connectionFailed` reads `boundPort` for its `debug` line (`Sources/macSCPCore/Tunnels/TunnelRunner.swift:559`), and `boundPort` is `runtime?.boundPort` (`:134`). `releaseCurrent()` sets `runtime = nil` (`:514`) before it awaits the report reader (`:520`), so a failure report the reader delivers during that drain is logged with `port=-` instead of the port the forward was bound to. The `active` line already reads the port off the started runtime instead (`:436`). Not started. |
| The S3 test rig cannot start: MinIO images are no longer on Docker Hub | **Open, recorded 2026-09-17** by Task 1 of the next-build plan, re-pulling the Docker test rig's images. `openssh-server`, `httpd`, `sftpgo` and `curl` still pull; `minio/minio` and `minio/mc` both answer "pull access denied … repository does not exist" — so the S3 rig (MinIO) cannot start and every `MACSCP_ITEST=1` S3 case is unreachable until the rig moves to another source or registry for those two images. All SSH/WebDAV/SFTPGo services stayed up. Not started. |
| The launch-time window autosave can land on `NSScreen.main` | **Open, recorded 2026-09-17** by Task 3's re-review of the next-build plan, deferred to the final fix / BACKLOG. Task 3's own fix round 1 (`7cb822bd`) fixed the case where *resuming* a suspended autosave places the window on `NSScreen.main` rather than its own display (see "The main window's size across launches" above, now Done) — but the *launch-time* name set (`applyFrameAutosave(to:)`, first call on a fresh window) likely lands on `NSScreen.main` too, by the same AppKit fact, and fires on every launch, not only after a shrink/connect cycle — ranked above the already-fixed case. Not reproduced in a running app; read from the code. Not started. **Sight check outstanding**, its own row below. |
| Task 1's review minors: SFTP connect-timeout doc and wording | **Open, recorded 2026-09-17** by Task 1's review of the next-build plan, deferred to the final fix and not picked up by any later commit (verified at HEAD: none of the four are fixed). M1: `SettingsStore.connectTimeoutSeconds`'s doc comment (`Sources/macSCPCore/Settings/SettingsStore.swift:725-739`) does not yet say the same value now also bounds the SFTP version wait, not only the TCP connect. M2: the Settings footer text `settings.connection.timeout %lld` ("Give up after %lld seconds", `Sources/MacSCPAppKit/Resources/en.lproj/Localizable.strings:77`, all four catalogs) can understate a tab dial's worst case, which is now up to roughly 2× the value (TCP connect, then separately the SFTP wait) — and the jump-hop wording beside it. M3: a comment at `ConnectionViewModel.swift:1212` referencing the old, unbound wait. M4: the `failureKind(for:)` doc sentence wording for the new `.other` case. Not started. |
| ~~Task 2's review deferred minor: a drop-scope test comment~~ | **Struck 2026-09-17** (final review): unreconstructable. The ledger (`.superpowers/sdd/2026-09-17-next-build/progress.md:9`) records this only as "M5 drop-scope test comment", the ledger is gitignored (`.gitignore:10`), and no `task-2-review.md` exists. A grep for "drop" and "scope", together and separately, over every test file Task 2 touched (`Tests/macSCPAppKitTests/TunnelManagerTests.swift`, `Tests/macSCPCoreTests/CLISessionsEditingTests.swift`, `Tests/macSCPCoreTests/SessionRemovalWordingTests.swift`, `Tests/macSCPCoreTests/TunnelRunnerTests.swift`) and over the full diff of Task 2's commits (`git log -p 19c6dc70..d94f962d -- Tests/`) finds neither word in any comment. Nothing to fix; nothing left to reconstruct it from. |
| Task 3's review deferred minors: a guard's spelling sensitivity and a shared helper move | **Open, recorded 2026-09-17** by Task 3's review of the next-build plan, deferred past fix round 1. Two items: the guard added for the fix (matching literal `isPrimaryWindow: isPrimaryWindow`) is sensitive to spelling rather than reading the symbol structurally, per CLAUDE.md "Guards that name what they watch"; and `body(after:in:)`-shaped logic the fix introduced could move into a shared helper instead of staying duplicated. From the re-review, three more: `isActiveTabConnected` has no literal guard pinning it; the full-screen guard binds two independent `.contains` calls where one would do; and an inactive-connected/active-unconnected tab under-persists a live resize (the live-resize save is connected-only, per Task 3's own Minor fix, but this combination still under-persists). Not started. |
| Task 5's review deferred minor: a duplicated dialog scanner | **Open, recorded 2026-09-17** by Task 5's review of the next-build plan. The toolbar-transfer confirmation dialog's source-scanning guard duplicates a scanner that already exists elsewhere in the test tree rather than reusing it. Not started. |
| Task 6's review deferred minors: terminal copy on select / paste on right click | **Open, recorded 2026-09-17** by Task 6's review of the next-build plan, four items. Paste runs inside `menu(for:)`, so a synthetic menu request (VoiceOver's "show menu" gesture) could paste too; a `rightMouseDown`-based alternative was not taken. The copy-pasteboard reassignment guard skips `MacSCPTerminalView.swift` and spells the file name literally rather than deriving it. The right-click tests leave named pasteboards unreleased. `getSelection()` is built on every click while copy on select is on, rather than only when a selection actually ends. Not started. |
| Task 7's review deferred minors: notifications | **Two of four resolved by `8984d29c`** (final review of the next-build plan, 2026-09-17): `ErrorNotifier.shared` no longer exists (`git grep ErrorNotifier.shared` finds nothing) — the forwarding hook is installed once, in `MacSCPApp.init`, and reads the app's one `SettingsStore` instance rather than a singleton. `settings.json` is no longer read per forwarding failure either: `SettingsStore.init` loads `raw` once and `boolValue(for:)` reads it from memory, so `store.notificationsEnabled` has no disk read and no "unreadable = on" path. **Still open**: a notification posted while the app's very first authorization prompt is still pending is lost. A transfer-failed and a connection-lost notification can both fire for the same underlying drop, by design (not de-duplicated across the two event kinds). Not started. |
| Sight checks for the 2026-09-17 build | **Open, recorded 2026-09-17**, grouped from the next-build plan's task reviews rather than one row per check — none of these were run in a live app during implementation or review. **Main window size** (Task 3): the size after a relaunch, resuming on a second display, and the full-screen exclusion. **Settings window resizability** (Task 4): edge drag, zoom button. **Toolbar transfer confirmation** (Task 5): the dialog wording in all four languages. **Terminal copy on select / paste on right click** (Task 6): control-click, VoiceOver "show menu", right click on the scrollbar, right-click during a multi-line bracketed paste, Option-right-click for the snippet menu, drag/double/triple/shift-click copy, the Settings section in all four languages. **Notifications** (Task 7): a banner while frontmost but the window is a background window, the per-tab latch releasing once that window becomes key, a large failing folder transfer producing exactly one banner per tab, the first-permission authorization prompt, the Settings → General toggle in all four languages, and whether `.onChange(of: transferFailureCounts)` fires at all for a minimized or fully occluded window. Not started. |

## Security and testability

| Entry | Core |
|---|---|
| [Capability boundary instead of a guard](superpowers/specs/2026-08-22-backlog-connection-capability.md) | **Implemented 2026-08-28.** Deciders are types, the dialing happens module-internal — both proved from outside by planting compile errors. Open and named: `import Citadel` compiles in the app layer (SwiftPM search path), this gap sits below the types and keeps the scan plus the import allow-list alive. |
| [Tests reaching real stores](superpowers/specs/2026-08-22-backlog-test-isolation.md) | `ContentView` hard-wired the keychain and the session store. A test consequently wrote into the real keychain. A seam now exists; the entry holds the rule and the remaining cases. |
| The maintainer's real diagnostic log folder holds lines shaped like test fixtures | **Open, recorded 2026-09-18**, observed in passing while investigating the maintainer's 2026-09-18 reports, not attributed to any test suite. `~/Library/Logs/macSCP/macSCP-2026-09-16.log`, `-17.log` and `-18.log` hold transfer and tunnel lines that read like fixture data rather than the maintainer's own activity, for example `path=/ziel/2.txt` (`macSCP-2026-09-16.log:5`, `macSCP-2026-09-17.log:28`) and a tunnel named `web-93266EEC` (`macSCP-2026-09-17.log:54-62`). A test suite may be writing into the maintainer's real diagnostic log directory rather than an isolated one — the same class of leak as [Tests reaching real stores](superpowers/specs/2026-08-22-backlog-test-isolation.md) above, whose seam and rule this route is not yet covered by. Not started. |
| [How far can the UI be tested?](superpowers/specs/2026-08-21-backlog-ui-test-coverage.md) | **Decided 2026-08-28: XCUITest struck for now** — pulling in a second build system while the first has not yet shipped just defers the problem. The trade-off (guard / ViewInspector / XCUITest) stays readable; a bug of this class reaching a user is what brings it back. |
| [Teardown against a frozen peer](superpowers/specs/2026-08-25-backlog-teardown-with-frozen-peer.md) | **Done 2026-08-29.** `disconnect()` never returned against a silent peer; specifically `sftp.close()` hung. Three of the four teardown stages are now bounded — the fourth (`cancelAll`) was measured and its deadline was reverted, because it caught nothing and cost the synchronous sweep. With an open shell: ≥31 s with no return before, 10.3 s and `.lost` after. The unbounded call is additionally **structurally excluded** (`BoundedSFTPSession`). |
| [Unbounded file closes](superpowers/specs/2026-08-28-backlog-unbounded-file-closes.md) | **Fixed 2026-09-02** (`55e6830`, review fixes `17abf12`). All eight counted `SFTPFile.close()` sites are structurally bounded: `BoundedSFTPSession.openFile` now hands out `BoundedSFTPFile`, whose only close is `closeBounded()` against the 5 s session bound, and Citadel's raw `SFTPFile` type is no longer nameable from `CitadelFileSystem.swift` (proved by a planted compile-error). Gated tests green, ~5.2–5.3 s against the red's ~10.5 s; the two arms that used to hang now cost up to that same 5 s where they previously never returned. Open: `write`'s success-path close AND `readStream`'s EOF close both now swallow a non-`ok` CLOSE status instead of throwing — parked for the maintainer, not decided here (a three-case `closeBounded()` outcome would cover both without touching `BoundedClose` or `BoundedSFTPSession`); the separate cancellation finding (a cancelled Task does not interrupt an in-flight SFTP read/write against a frozen peer) is unchanged and still undesigned. |
| [Two open questions from the closing review](superpowers/specs/2026-08-26-backlog-open-questions-review.md) | **S3 half measured and cleared 2026-08-28:** `Authorization` is not carried along across any redirect (10 cases, 2 origin forms, 5 status codes). **M3 fixed 2026-08-29** (`a07640c`): the dial's origin is bound to the attempt, not the tab — a refused connect cannot contribute one. Design: [2026-08-28-two-open-questions-design.md](superpowers/specs/2026-08-28-two-open-questions-design.md). |
| [S3 runs on the shared URL session](superpowers/specs/2026-08-29-backlog-s3-shares-the-url-session.md) | **Done 2026-08-29.** S3 now has its own `ephemeral` session and releases it in `disconnect()`; the `.shared` default in `URLSessionHTTPTransport.init` is gone, four call sites name their session. The open measurement is answered, and unfavorably so: `sendStreaming` caches **identically** — a `max-age` response was served to a second process, body and all, from disk. |
| [S3 follows redirects without control](superpowers/specs/2026-08-28-backlog-s3-redirects.md) | **Done 2026-08-29** (`9e96025`), only buildable once S3 had its own session. Same origin gets re-signed and followed — which at the same time fixes the functional bug that a legitimate redirect arrived unsigned; a foreign origin (scheme, host **and** port) is refused, and the message names both origins without the path. The wrong `Host` disappears through the re-signing. |
| Host-key fingerprints in the diagnostic log (maintainer decision) | **Measured 2026-09-06**, found by the Task 5 re-review of the port-forwarding plan. `DialSupport.reason(for:)` renders `HostKeyError.mismatch` as `host key MISMATCH for <host>: expected <fp>, got <fp>` — both FINGERPRINTS (the comment on `HostKeyError.mismatch` says so; never the keys), and **nine** call sites in `Sources/` pass an error to `DiagnosticLog.shared.log(…, reason:)` — `grep -rn -A2 "DiagnosticLog.shared.log(" Sources | grep -c "reason:"`, counted 2026-09-06 at the final review of the port-forwarding plan; the `-A2` window is what the one-line form misses, which reports 5 because most of these calls wrap. **Two more reach it indirectly**, through `TunnelRunner`'s own private `log(…, reason:)` wrapper (`grep -n -A2 "^ *log(" Sources/macSCPCore/Tunnels/TunnelRunner.swift | grep "reason:"`, the same day) — so eleven places in all, and the nine include the wrapper's own body. The row said seven when it was written (`7af90e40`); the port-forwarding plan added the rest. A mismatch writes both fingerprints to disk at `.info`. The diagnostic-log plan's constraint reads "Never logged, at any level: … host keys (the decision and the key TYPE only)". A fingerprint is a hash of the server's public key, not the key and not a secret; whether the constraint means it literally is the maintainer's call. If yes: the mismatch arm keeps the host and drops both fingerprints (the App's mismatch alert still shows them), one guard test pins the line. Not started. **Done 2026-09-16** (`8db25be8`): decided — leave them out. `DialSupport.reason(for:)`'s `HostKeyError.mismatch` arm (`Sources/macSCPCore/Diagnostics/DialProbes.swift:211-223`) now returns `"host key MISMATCH for \(host): the presented key differs from the recorded one"`, fingerprints pattern-discarded; every consumer of that one function — the diagnostic log, the audit-row `lastFailureReason`, the diagnostics report — is covered by construction. The App's `core.hostkey.mismatch %@ %@ %@` alert and the CLI's stderr (`CLIErrorMapping.swift:202`) build their own sentence directly from `expected`/`presented` and keep showing both. The surfaces that lost the fingerprints — nine render sites in three families, counted 2026-09-16 at the plan's final review (Task 1's report named only the first): the forwarding failure reason `TunnelState.failed(reason:)` (the forwardings sheet's state column `TunnelProfilesSheet.swift:317`, the autostart sheet's `TunnelAutostartSheet.swift:146`, the Dock menu's tooltip `TunnelDockPresence.swift:154`, the sidebar glyph's tooltip `SessionSidebar.swift:1538`); the audit `connectFailed` row's detail (the session overview's history `SessionOverviewView.swift:486-487`, the audit log sheet's detail column `AuditLogSheet.swift:153` and its export `:292`); and the diagnostics step reason (the panel's row `DiagnosticsPanel.swift:200-202`, the copied report `DiagnosticReport.swift:140`/`:210`). **After `8db25be8` a forwarding's mismatch shows the presented fingerprint nowhere**: the known-hosts sheet (`KnownHostsSheet.swift:151-154`) lists only RECORDED fingerprints and a mismatching presented key is never stored; the only places a person sees a presented fingerprint are the App's alert on a manual tab connect and the CLI's stderr, and a forwarding raises neither. Record: `superpowers/specs/2026-09-10-pem-private-keys-design.md`'s "Decisions 2026-09-16" section. |
| `localFileSystemListWritesStartAndDoneWithoutAnEntrySlowLine` is a wall-clock claim | **Observed 2026-09-06** by the Task 6 implementer of the port-forwarding plan: red once in a full `swift test` under load on `#expect(!contents.contains("entry slow"))` in `Tests/macSCPCoreTests/DiagnosticLogSharedSinkTests.swift`, green alone and in every other run. The test asserts that a local listing does NOT cross the slow-entry threshold — a ceiling on the runner's speed, the shape CLAUDE.md's "A wall-clock ceiling in a test measures the runner" forbids. Fix shape: inject the threshold (or the clock) so the test proves the line is absent when the entry is fast by construction, and present when a fake clock makes it slow — not that this machine was fast today. **Done 2026-09-17** (`92c0a9fc`, technical-backlog plan Task 4): `LocalFileSystem` gained an injectable `metadataNow: @Sendable () -> ContinuousClock.Instant` (default `ContinuousClock().now`), read by `metadata(for:)` around each probe; `DiagnosticLogSharedSinkTests` now proves the line absent with `metadataNow` pinned to one instant (every entry measures exactly zero) and present with an `AdvancingClock` that makes every reading slow. No elapsed-time assertion remains in either test. |
| The session-name authority is a second copy | **Measured 2026-09-06** by the Task 1 review of the CLI plan: `SessionListViewModel.save` looks up `sessions.first(where: { $0.name == name })` itself, while the conflict WARNING the form shows is computed by `SessionNameRule.conflict(_:among:excluding:matching: .exact)` through `SessionNameConflict`/`SessionNameCollision`. Two copies of one rule agree today; a change to one (trimming, case) silently desynchronises the warning from the outcome. Fix shape: `save` asks `SessionNameRule.conflict` for the session it replaces. **Done 2026-09-17** (`c0ee0261`, technical-backlog plan Task 4): `SessionListViewModel.save` now finds its target with `SessionNameRule.conflict(name, among: sessions, matching: .exactAsSaved)` and stores `SessionNameRule.asSaved(name)` (trimmed) as the name, for both a new and a replaced session — pinned by a red case where the hand-written `==` and the rule disagreed (a name differing only in surrounding whitespace). |
| A forwarding dial opens an SFTP channel it never uses | **Noticed 2026-09-06** in Task 4 of the CLI plan: `TunnelConnection.connect` goes through `CitadelFileSystem.connect`, which opens an SFTP subsystem channel on every connection, so a forwarding-only session against a server with SFTP disabled (`Subsystem sftp` commented out, or a jump box) fails for a reason unrelated to forwarding, and every tunnel pays one channel it never reads. Fix shape: a connect path that stops after user auth (the SSH client without the SFTP child channel), used by `TunnelConnection` only; the app's tabs keep the SFTP channel. **Done 2026-09-17** (`c479fd37`, `f0d9f4ff`, `feb0a55e`, technical-backlog plan Task 8): a new rig service `sshd-nosftp` (`127.0.0.1:2236`, SFTP subsystem disabled via a custom-cont-init hook) measures it; `CitadelFileSystem.connect` was factored into a shared `connectAuthenticated(config:connectTimeout:knownHosts:onUnknownHostKey:establish:)` (TOFU, jump handling and auth in one place) plus a new internal `SSHForwardingConnection.connect`, which calls it with a step that never opens SFTP; `TunnelConnection` uses the new path, tabs still use `CitadelFileSystem.connect`. Measured against `sshd-nosftp`: a forwarding dial connects and reaches `.active` carrying bytes; a tab dial against the same server is refused SFTP by the server (logged) — and, newly found while measuring it, **hangs rather than failing**: see the new row below. |
| `TunnelStore` writes rewrite an unreadable file from empty | **Measured 2026-09-06** in Task 5 of the CLI plan: `TunnelStore.load()` returns an empty store for a present-but-undecodable `tunnels.json` (by design for the glyph and autostart readers), and `upsert`/`delete`/`deleteAll` all go through `load()` before `persist`, so the first write after a corrupt or version-mismatched file silently replaces every profile with the one being written. The activation reload no longer treats such a read as a deletion (`readProfiles() -> Result`); the write paths still do. Fix shape: writes go through `readProfiles()` and refuse (throw) on an undecodable file, leaving it for the user to inspect; the app's sheet shows the refusal. **Done 2026-09-17** (`f31212b3`, technical-backlog plan Task 2): `upsert`/`delete(id:)`/`deleteAll(for:)` go through a private `writableFile()` that switches on the same `decode()` `readProfiles()` uses and throw `TunnelStoreError.unreadable(path:)` on `.failure`, leaving the file byte-identical; the lenient readers (glyph, autostart) still read an unreadable file as empty. The App surfaces the throw through `tunnel.store.unreadable` in all four catalogs; the CLI maps it to "could not be read" and exit 13 (`.connection`, the same code an unreadable session store already used). `sessions rm` does **not** block on a `deleteAll` refusal — it warns to stderr naming the file and still removes the session, exit 0, to avoid a session nobody can delete until the file is repaired by hand. The App's `forgetEverything(for:)` was widened: on any `deleteAll` failure it now also stops that session's runners and removes its rows from the in-memory mirror before any `await`, because the store no longer silently rewrites itself empty on delete, so a deleted session's tunnels would otherwise keep running. Residual gaps, each its own new row below: `TunnelManager.save`/`remove` still stop a profile's runner *before* the write, so a refused save over an unreadable file still stops a running tunnel; orphan rows of a deleted session resurface as stopped rows once the file is repaired; `sessions rm`'s prompt/`--verbose` say "0 forwardings" over an unreadable file; exit 13 is documented as a transport failure (`CLIExitCode.swift:22`) but is now also used for store failures. |
| [A test parked on a bare continuation outlives its time limit](superpowers/specs/2026-09-05-backlog-continuation-outlives-time-limit.md) | **Done 2026-09-05.** Both open questions answered. (a) CI: the last three green `develop` runs before this fix (33972030162, 33959532085, 33955366191) measured the `Unit-Tests` step at 181 s, 230 s and 184 s — a maximum of 230 s (3 m 50 s); `.github/workflows/ci.yml`'s `Unit-Tests` step now carries its own `timeout-minutes: 8` (480 s, 2.09x that maximum) below the job's existing 20-minute budget, so a parked test fails the step near its own limit instead of running to the job's. (b) The rule: every test-side `withCheckedContinuation`/`withCheckedThrowingContinuation` outside `Tests/MacSCPTestSupport/` goes through a new cancellation-aware helper, `awaitResumption`/`awaitResumptionThrowing` (`Tests/MacSCPTestSupport/AwaitResumption.swift`) — `withTaskCancellationHandler` racing the body's own continuation against cancellation, the same shape `awaitCancellably` already used for NIO futures. 15 files converted their bare waits (16 helper functions, including `TerminalPanelViewModelTests`' `Box.waitForFirstDelivery`, which also dropped a now-redundant hand-rolled `withTaskCancellationHandler`); 12 files were read and found to depend on the RAW continuation's behavior instead — a mock deliberately never resuming to model a frozen, uncancellable peer (`BoundedCloseTests`, `TeardownStageTests`, `LivenessProbeRaceTests`, `LivenessProbeCancellationTests`, and the `Gate` types in `ConnectionDiagnosticsTests`/`LocalFileSystemTests`/`DiagnosticLogSharedSinkTests`, all of which test that a step's own cancellation must NOT unstick an in-flight probe), a hand-built race already bounded by construction (`WebDAVFileSystemWriteTests.abandonable`, `LivenessProbeDropIntegrationTests.BoundedRun`), a gate whose cancellation must complete the wait NORMALLY rather than throw (`TerminalPanelViewModelTests.ParkGate`, `SSHTerminalViewSizingTests`' `OpenGate`/`OpenGates`), or a body that cannot be `@Sendable` (`TabDragTests`, capturing a non-`Sendable` `NSItemProvider`) — each marked with the sentence `PollingGuardTests.noBareContinuationEscapesAwaitResumption` scans for ("the continuation IS the API under test here"), read by a NEGATIVE check paired with a positive (the pattern matches something, and the helper itself is adopted by ≥ 15 files — `PollingGuardTests.theAwaitResumptionHelperExistsAndIsAdopted`). Mutation-probe: a planted bare `withCheckedContinuation` in `ConnectionConfigResolutionTests.swift` came back `RESULT: RED — 1 test(s) ran, and the plant was caught by: macSCPCoreTests.PollingGuardTests/noBareContinuationEscapesAwaitResumption()`. `swift test` (4899 tests, 416 suites) green throughout. **Fix round 1, 2026-09-05** (code review): two findings. Critical — `AgentEnvLock` is an EXCLUSIVE hand-off primitive, unlike the broadcast latches also converted; its `acquire()` appended a continuation to `waiters` and never removed it on cancellation, so a cancelled waiter's stale entry made `release()` hand the lock to a task that no longer existed and never clear `isLocked` — a permanent leak in a process-wide `static let shared` lock. Fixed by giving `awaitResumption` an optional `onCancel:` closure, run under the same once-latch as the resume race so it fires at most once and never after a real resume, and giving each waiter a token `AgentEnvLock` removes under its own lock when that closure runs; `AgentEnvLockTests` (new) reproduces the exact scenario — A holds, B queues, B is cancelled, A releases, a fresh C must still acquire without waiting — red beforehand (`queuedCount → 1`, then the suite's own 60 s `.timeLimit`, with the underlying OS process still running past it until killed by hand — the same shape this whole entry is about, reproduced one layer up), green after. Important — the exemption in `noBareContinuationEscapesAwaitResumption` was FILE-level (one sentence anywhere in a file exempted every bare continuation in it); tightened to a 12-line proximity window above each specific use (`PollingGuardTests.continuationExemptionWindow`, the same size as `IconTooltipLintTests.hintWindow`), which raised the count from 12 files to 13 call sites across those same 12 files — `SSHTerminalViewSizingTests` carries two (`OpenGate`, `OpenGates`), each now with its own nearby sentence. Proven by planting a third, far-from-any-sentence bare continuation in that same file: `RESULT: RED — caught by macSCPCoreTests.PollingGuardTests/noBareContinuationEscapesAwaitResumption()`, reverted clean. `swift test` (4908 tests, 417 suites) green throughout. **Fix round 2, 2026-09-05** (code review): cancel-BEFORE-the-body-registers. Round 1's `onCancel` only fired `cleanup` when a continuation had ALREADY been stored in `awaitResumption`'s own `state` — a task cancelled before it is even scheduled reaches `withTaskCancellationHandler` already cancelled, so `onCancel` fired while `state` was still `.waiting(nil)`, found nothing stored, and skipped `cleanup`, while `body` — running independently on an unstructured, never-cancelled `bodyTask` — still went on to register its token later: orphaned, the exact leak, one layer earlier. Fixed with the SAME lock deciding both questions: `onCancel` now fires `cleanup` whenever the transition to `.cancelled` succeeds, regardless of whether a continuation was stored yet, and `body` is now invoked from INSIDE that same `state.withLockedValue` critical section, so `body` and the cancellation transition can never interleave — either `body` runs to completion first (and the later `cleanup` correctly finds and undoes whatever it registered) or the transition wins first and `body` is never called at all (the "does not append" shape, needing no separate undo path). Red first: `AwaitResumptionTests.aTaskCancelledBeforeItIsScheduledStillThrowsCancellationError` failed instantly (`cleanupCalls → 0`, expected 1); `AgentEnvLockTests.aWaiterCancelledBeforeItRegistersDoesNotLeakTheLockForever` (B created and cancelled in the same synchronous step, no `pollUntil` before the cancel) hung on task C past the suite's own 60 s `.timeLimit` with NO output at all until the process was killed roughly 8 minutes later — stdout was fully buffered while parked, so even the timeLimit's own "exceeded" report never reached the log until termination — the row's own finding, reproduced yet another layer up, exactly as expected. Green after the fix, fast (under 10 ms, no hang). `swift test` (4923 tests, 418 suites) green throughout. **Fix round 3, 2026-09-05** (code review): `AgentEnvLock.acquire()`'s FAST, uncontested path set `isLocked = true` and resumed its own continuation as an ordinary `body`, leaving `awaitResumption`'s `state` at `.waiting` until a SEPARATE monitor task later transitioned it to `.done` — a cancellation landing in that gap threw `CancellationError` at a caller that had, in fact, already been handed an irrevocable commitment (unlike a QUEUED waiter, which `cleanup` can always undo). Fixed by adding `tryImmediate`, an optional synchronous callback checked in the SAME `state.withLockedValue` step that decides everything else: if it commits a value, `state` moves straight to `.done` and the outer continuation resumes right there, so a cancellation racing against that one atomic step can only land before or after it, never inside. `AgentEnvLock.acquire()` now passes its check-and-take-the-lock logic as `tryImmediate`, keeping `body` for the queued (revocable) path only. The four orderings — cancel before register, cancel while queued, cancel after the fast path commits, resume then cancel — are now traced in prose in `awaitResumption`'s own doc comment. Tests: `AgentEnvLockTests.aTaskCancelledInTheSameStepAsAnUncontestedAcquireNeverLeaksTheLock` (B created and cancelled in the same synchronous step against a lock with no holder; either outcome of the race must leave `isLocked` false and let a fresh C acquire without waiting) run 20 times before the fix: **20/20 passed, 0 hung** — this exact reproduction shape did not exhibit the race, because round 2's own gate (`body` only runs if `state` has not already moved to `.cancelled`) already wins this specific timing pattern (cancel synchronously at task creation) on this machine, before `bodyTask` gets scheduled at all; the race the finding names is real by code inspection (traced above and in the doc comment) but this particular test does not reliably provoke it. A second, more direct test — `AwaitResumptionTests.tryImmediateCommitsAtomicallyWithStateSoALateCancelIsIgnored` — forces the ordering deterministically (signals from inside `tryImmediate` itself, so the cancel is issued only once the commit has already run) and is green after the fix; it cannot run at all against the pre-round-3 API, which had no `tryImmediate` parameter. Full detail, including the 20-run table, in the task report. `swift test` (4933 tests, 419 suites) green throughout. |
| The SOCKS5 handshake has no timeout | **Open, recorded 2026-09-06** by the final review of the port-forwarding plan — a Task 3 review hand-off that never reached Task 5's brief, which is how it survived to the final review. A client that connects to a dynamic forward's `-D` port and then stalls mid-greeting holds an accepted socket plus a task parked on `SOCKS5RequestBox.value()` (`Sources/macSCPCore/Tunnels/SOCKS5Handshake.swift`) until the tunnel's `stop()`, and nothing caps how many such clients there may be: the park ends only when the conversation settles or the channel closes. **Accepted for now, and the reason is the tool this imitates**: `ssh -D` behaves the same way, so this is not a regression against it, and the listener binds loopback by default, so the reachable set is local processes. **The fix shape**: a per-handshake deadline that closes the socket and resolves the box with a failure, plus a cap on how many handshakes may be parked at once, refusing beyond it. The box's own continuation would then need the cancellation handler `OpenPortBox` (`RemoteForward.swift`) already carries — stated at its declaration. **Done 2026-09-17** (`86135e0a`, `db432037`, technical-backlog plan Task 3): `SOCKS5Listener` carries a 30 s deadline (`socks5HandshakeDeadline`) and a 64-handshake cap (`socks5ParkedHandshakeLimit`), both named constants with their reasoning in a comment; a stalled client is closed and its box resolved with `SOCKS5HandshakeError.deadlineExpired` once the deadline fires, a client beyond the cap is refused immediately with `tooManyParkedHandshakes` (socket closed, no box parked), and `SOCKS5RequestBox.value()` now carries the same `withTaskCancellationHandler` `OpenPortBox` has. A refused or timed-out handshake writes one `.debug` `tunnel` line naming only the local port. Neither failure reaches the tunnel's `onFailure` — a client's own SOCKS5 failure is still not a tunnel failure, by the existing rule. |
| An MCP server in the app (maintainer wishlist) | **Not started.** Recorded 2026-09-16 from the maintainer's wishlist (item 1): an MCP (Model Context Protocol) server built into macSCP, which can be enabled or disabled per tag, per group and per session, plus permissions for what MCP is allowed to do — create, connect, and so on (the list is open-ended in the wish). Exists today: nothing — no occurrence of `MCP` or "Model Context Protocol" in `Sources/`, `README.md` or this file (grep, 2026-09-16). The nearest automation surface is `macscp-cli` (`Sources/MacSCPCLI/`: `sessions`, `tunnels`, `ls`, `get`, `put`, `mkdir`, `rm`, `diagnose`); the tags a per-tag switch could key on exist (`StoredSession.tags`, `Sources/macSCPCore/Sessions/StoredSession.swift:132`). Constraints any automation surface inherits and a design must state rather than rediscover: the TOFU rules (a key mismatch is a hard stop, an unknown key needs explicit consent, no accept-anything path — CLAUDE.md "Architecture invariants"), so an MCP client cannot be the party that consents; the CLI's "no secret through the CLI" rule (`docs/superpowers/specs/2026-09-06-cli-store-and-tunnels-design.md:58`) carried over — no tool that takes, stores or returns a password, passphrase or key; the leak-route findings (Bugs, "Diagnostics: error text as a leak route") apply to everything a tool returns. Open questions: the transport (a stdio server the client launches, or a socket served by the running app — the latter needs an authentication story of its own); whether "disabled" hides a session from listing or only refuses operations on it; how per-tag, per-group and per-session switches combine when they disagree; which operations the permission list covers — creating/editing/deleting sessions, connecting, reading/writing/deleting files, running terminal commands (the last is arbitrary remote command execution, the widest of them). Not started. |
| U2F/FIDO2 security keys (maintainer wishlist) | **Not started.** Recorded 2026-09-16 from the maintainer's wishlist (item 23): "U2F/FIDO2 token support". This reopens a recorded decision: FIDO2 `sk-*` keys are "Not covered, by decision" in the key-formats record (`docs/superpowers/specs/2026-08-31-backlog-ssh-key-formats.md:442-444`), and its 2026-09-10 addendum keeps `sk-*` "as decided" (`:446-448`). Exists today: the loader names the type and refuses it rather than failing a parse (`Sources/macSCPCore/SSH/SSHPrivateKeyLoader.swift:36`, `:82`; `SSHKeyImporter.swift:156`). Open questions: which route is meant — an `sk-*` key file whose every signature needs the token (touch/PIN), or a token-held key offered through the ssh-agent route `SSHAgentClient` already speaks (`Sources/macSCPCore/SSH/SSHAgentClient.swift`); whether the forks (`NoiXdev/swift-nio-ssh` 0.3.10, `NoiXdev/Citadel`) can carry the `sk-*` public-key algorithms at all is **unmeasured**, and any change there falls under CLAUDE.md "Forks are a debt with a review date"; talking to a token directly means a USB HID / CTAP2 dependency that is not in the dependency graph today — a supply-chain decision. Security-relevant: the token's user-presence check must stay with the user. Not started. |
| `TunnelManager.forgetEverything`'s "rows leave the mirror before any await" ordering is pinned by no test | **Open, recorded 2026-09-17** by the final review of the technical-backlog plan (Task 2 review, deferred minor 3). When `deleteAll(for:)` fails, `forgetEverything(for:)` removes the deleted session's rows from `allProfiles` (`Sources/MacSCPAppKit/TunnelManager.swift:221`) before its first `await` (`discardAndForget`, `:222`), and its doc comment (`:203-205`) names that order as what makes `start(_:decider:)`'s guard refuse those rows. The cases that drive it (`Tests/macSCPAppKitTests/TunnelManagerTests.swift:744`, `:870`) read their results after `forgetEverything` has returned, so, per that review and not re-measured here, moving the removal after the `await` would leave them green. **Done 2026-09-17** (`d1243789`, the next-build plan's Task 2): a new case parks the first stop on a gate and reads the mirror, and a racing start, from inside it — observing the ordering deterministically rather than after the call has returned. |
| The CLI's `sessions rm` aborts when `tunnels.json` decodes but cannot be written, and no test pins it | **Open, recorded 2026-09-17** by the final review of the technical-backlog plan (Task 2 review, deferred minor 5). `StoreEditing.deleteSession` (`Sources/MacSCPCLI/StoreEditing.swift:211`) catches only `TunnelStoreError.unreadable` (`:214`); any other error from `deleteAll(for:)` (`:213`) propagates before `sessionStore().delete(id:)` (`:219`), so the session is not removed — the behaviour the doc comment states (`:208-210`). The tests beside it cover a readable store (`Tests/macSCPCoreTests/CLISessionsEditingTests.swift:596`) and an undecodable one (`:629`); none plants a store that decodes but refuses the write. **Done 2026-09-17** (`d1243789`, the next-build plan's Task 2, test-only): a new case plants a `tunnels.json` that decodes but cannot be written (a user-immutable flag) — `sessions rm` exits 13 with the write's own error, and both the session and the file are left untouched, pinning the doc comment's stated behaviour rather than changing it. |
| An SFTP open started in the background on the forwarding path would go unnoticed, its error never caught | **Open, recorded 2026-09-17** by the final review of the technical-backlog plan (Task 8 review, deferred M3). The forwarding dial must never ask for SFTP (`SSHForwardingConnection.connect`, `Sources/macSCPCore/SSH/SSHForwardingConnection.swift:56`). What pins that today: `theForwardingPathOpensNoChannelWhereTheTabPathOpensOne` (`Tests/macSCPCoreTests/SSHForwardingConnectionTests.swift:124`) reads the fake server's channel count once, right after the dial returns, and the gated `ForwardingWithoutSFTPITests` counts the rig's SFTP refusals. The review's probe B planted an `openSFTP` started in a background task whose error nothing awaits, and no test caught it; that plant's error is never caught either. Recorded from that review, not re-measured here; why each check misses it (for the unit case, plausibly a request that arrives after the count is read) is not measured. Not started. |

## Interface

| Entry | Core |
|---|---|
| [Sessions, tabs, sidebar](superpowers/specs/2026-08-20-backlog-sessions-tabs-sidebar.md) | **Fully done** (eleven of eleven, completed 2026-08-29). The pointer stays because the justifications and measured starting states in it still apply. |
| [Management sheets](superpowers/specs/2026-08-20-backlog-management-sheets.md) | **Fully done** (2026-08-29): items 1, 2, 3 and 5 implemented, item 4 dropped. The facet filter is one control for three sheets, chained with the search via a testable value; the values come from the rows, and with fewer than two values no picker appears. Found along the way: **all three** sheets carried the same silent lie — "unfiltered" meant "search field empty". The pointer stays because of the justifications, especially item 4. |
| [Fine polish on the tabs](superpowers/specs/2026-08-27-backlog-tab-polish.md) | **Done 2026-08-27/28**, re-verified 2026-08-29: the insertion marker highlights the target tab (`TabStripView.dropTarget`), and switching terminal ↔ files hangs off `TabMenuEntry.pane(_:_:)` in the tab menu, with `PaneToggleState` as the single source of truth. The pointer stays because the justifications in it apply. |
| [Snippet editor: usability](superpowers/specs/2026-08-21-backlog-snippet-editor-interaction.md) | **Done 2026-08-30.** Variables fold without remembered state; a variable with an error cannot be collapsed, which turns "collapse all" into "show me only the problems". Insertion, autocomplete on `{{`, and the hint for an undeclared `{{NAME}}` — as a **display**, not a send block. **Done 2026-09-02:** a `{{DB}}` for an environment variable gets its own sentence (declared, but as an environment variable — nothing is filled in; write it as `$NAME`), a display beside the first. **Left open:** the row's insert path appends at the end; a foreign `{{foo}}` is flagged, blocking nothing. **The insert path done 2026-09-06:** a pure Core helper, `SnippetBodyInsertion.insert(_:into:at:)`, decides where `{{NAME}}` lands — appended (the old, unchanged rule: one space of separation unless the command is empty or already ends in whitespace) when there is no selection, or in place, replacing a selection, when there is one; tested for no selection, a caret mid-text, a non-empty selection, a selection at the very start, and unicode ahead of the caret (an emoji, a combining sequence) landing the cursor on a grapheme boundary rather than through one. `SnippetCommandEditor` already IS the `NSTextView` bridge the residual asked for (built for syntax colouring and the `{{` completion list, part 2) — the only missing piece was a way for the row's button, sitting elsewhere in the sheet, to reach that view's live selection. `SnippetCommandEditorController` is that reach: a plain `@State`-held reference type the editor's `Coordinator` registers itself with once `SnippetCommandEditor.makeCoordinator()` runs, so the button can ask the live text view to make the edit itself. Falls back to the old append behaviour (via the now-thin `snippetCommandInsertingPlaceholder`, which delegates to `SnippetBodyInsertion`'s `nil`-selection branch) only if that view has not registered yet — unreached through the UI, since the command field is always on screen before the button can be pressed. **Not touched, by design, per this entry's own framing:** a foreign `{{foo}}` from another templating language is still flagged, blocking nothing — that is a display, not a defect. Tests: `SnippetBodyInsertionTests` (Core, 6 cases), full `swift test` at 4958 tests in 420 suites, all green, zero build warnings. **Fix round 1, 2026-09-06** (review finding): the live insert path assigned `textView.string` directly, which registers no undo action and posts no change notification, so "Insert in command" could not be undone with ⌘Z while ordinary typing could. `Coordinator.insertPlaceholder` now goes through `NSTextView`'s own edit bracket — `shouldChangeText(in:replacementString:)`, `replaceCharacters(in:with:)`, `didChangeText()` — the same one Apple documents for a programmatic change that must behave like typing; `didChangeText()` posts the notification that runs `textDidChange(_:)`, so recolouring and the SwiftUI binding update happen through the exact path a keystroke already takes, and the cursor lands after the inserted placeholder by construction (`replaceCharacters(in:with:)`'s own behaviour), with nothing left to set by hand. `SnippetBodyInsertion` now backs only the no-live-view fallback. Pinned by a source guard (`SnippetCommandEditorGuardTests`, finding 11): `insertPlaceholder` must call `shouldChangeText(in:` and `didChangeText()`, and must not assign `.string =`. Full `swift test` at 4960 tests in 420 suites, all green, zero build warnings. |
| [Checksums for files](superpowers/specs/2026-08-27-backlog-file-hashes.md) | **Items 1 and 2 done 2026-08-31** (four tasks). Core did not get a generic `exec(String)`, but "compute this file's checksum": `ChecksumCommandLine` has a `fileprivate init` and two construction sites in the whole package. A result without **provenance** cannot be constructed — proved by planting the weaker construction and watching it pass with a green suite. A multipart ETag explicitly says it is **not** the file's checksum. **Item 3 done 2026-09-02** (a toggleable column, empty until the user asks through the existing action; the sheet and the batch feed the tab's ledgers — one per pane side — keyed by path+size+date; nothing computes on its own), plus no progress within a file and no dedicated case for "this algorithm does not exist here". — **"This algorithm does not exist here" done 2026-09-05:** a new `RemoteChecksumOutcome.algorithmUnavailable(ChecksumAlgorithm)` case, distinct from `.unavailableOnThisConnection` — that one is a property of the WHOLE connection (no checksum form answered the presence probe at all), this one of a single algorithm on a connection that answers fine for others, because the presence probe only ever proves the PREFERRED algorithm's tool is there (`ChecksumCommandForm.presenceProbeLine()`), not every algorithm's own. Classified by exit code alone: a new `ChecksumCommandExitFailure(exitCode:)` — thrown by `CitadelFileSystem.standardOutput(of:)` when Citadel's `SSHClient.CommandFailed` reaches it, so `RemoteChecksumProvider.swift` itself stays free of any concrete backend's error type — with exit 127 (POSIX shells' own "no such executable") the one code `RemoteChecksumRun` reads as "the tool is missing" rather than the generic per-file failure. Text was never an option: `standardOutput(of:)` discards standard error entirely, and the wording is not a signal that generalizes across shells the way the exit code does — bash writes "command not found" after the name, zsh before it, a POSIX `sh` typically omits the word "command" — three distinct wordings counted for the record, none reachable here regardless. Rendered through `CoreL10n` (`core.checksum.unavailable %@`, all four catalogs, German du-neutral since the sentence carries no address at all) at the same `RemoteBrowserViewModel.checksum(of:algorithm:)` boundary that already produces every other per-file failure message, and folded into the EXISTING `ChecksumRequestResult.failed` case rather than a new one — shown exactly where the generic failure shows today, per the residual's own framing, with no new App-layer rendering path to keep green. Tests: the classifier (exit 127 → `.algorithmUnavailable`; a different non-zero exit, and a failure carrying no exit code at all → the existing generic failure, both pinned separately); the Core→App mapping (`ChecksumRequestTests.anAlgorithmWithNoToolOnThisConnectionNamesItself`); the App-side rendering of that exact sentence through `.failed` (`ChecksumDisplayTests.anAlgorithmWithNoToolOnThisHostReadsAsAFailureNamingTheAlgorithm`); catalog parity across all four languages. Full `swift test`: 4944 tests, 419 suites, all green. Still open, unchanged by this entry: no progress within a file (a remote `sha256sum` has no progress to report). |
| [Snippet dry run](superpowers/specs/2026-08-20-backlog-snippet-dry-run.md) | **Done 2026-08-30** (four tasks). The dry run shows the resolved command, the send form, the rejection reason and the coloring — from **one** value that both entry points call (rejection on trigger, "Test" in the editor). The per-snippet marker exists in addition; it **cannot** be exported, because export got its own type before the field existed. The inserted value reaches no log, no export, and no error message — not even a test failure message. |
| [Detachable, optionally sticky tabs](superpowers/specs/2026-09-03-detachable-tabs-design.md) | **Done 2026-09-05** (`docs/superpowers/plans/2026-09-03-detachable-tabs.md`, six tasks: `84459616`/`36fb81d9` Task 1, `aee95e98`/`c9ae9e7b`/`71fdf913`/`33d508f4` Task 2, `fb612134`/`c52c83b5` Task 3, `3fd16e67` Task 4, `3b3f8e02`/`88a54ff7` Task 5 (the second commit is Task 5's own fix round 1), `8abd0761` Task 6, and a final fix round (the commit above) for the whole-plan review's own findings). **Task 1**: a process-wide `TabRegistry` lets a tab change which `TabsViewModel` holds it without touching the `SessionTab` object, its `BrowserSession`, queue or terminal — pinned by identity, not by re-dialling. **Task 2**: "Move Tab to New Window" opens a second `WindowGroup` instance keyed by a `WindowSeed`, the moved tab is parked in the registry until the new window claims it, and a window an emptied move would leave with nothing closes itself unless it is the app's last window. **Task 3**: a tab dragged onto another open window's strip moves there the same way, connection untouched; in-strip reordering is unchanged. **Task 4**: a per-window "Keep on Top" entry in the Window menu holds that window's `NSWindow.level` at `.floating`. **Task 5**: `SettingsStore.restoresWindows` (default off) describes every window still OPEN at quit — each tab's stored session and pane visibility, the window's sticky flag, whether it is the primary window — into `windows.json` from `applicationWillTerminate`, and the next launch brings those windows back with their tabs showing each session's overview, disconnected; nothing dials automatically. Corrected in Task 5's fix round 1 (`3b3f8e02` wrote a window's seed from its own `willClose` handler, which described exactly the wrong set — ⌘Q closes no windows, so the windows on screen at quit were never described while every deliberately closed window accumulated in the file — and put the restored tab's form in EDIT mode via `beginEditing`, offering "Save & connect" for a session that is already saved). The seed file describes one quit and is consumed once: read then deleted by a launch that restores, deleted unread by one that does not, so no launch can see two generations. **Task 6**: this closeout, plus two review-deferred fixes — `ContentView.performCloseOthers` used to snapshot which tabs to release before its own teardown loop's `await`s, missing a tab a concurrent cross-window drag added mid-loop (`TabsViewModel.closeOthersReportingRemoved(besides:)` now reports what was actually removed); the `window move parked` log line used to be written before the move ran, naming a seed that could turn out to park nothing. **Not in this plan, and built afterwards**: detaching by dragging a tab out and dropping it on the desktop was out of scope here and blocked by the deployment target at the time; it shipped on 2026-09-05 by moving the tab's drag source to AppKit — see the row below; sticky across Spaces or beside full-screen apps; automatic reconnect for a restored, disconnected tab. **Accepted limits**: a tab moved into a window of its own is parked in `TabRegistry` until that window claims it; if the window never appears the tab stays parked and invisible until its source window closes (ordinary teardown) or the app quits (a registry sweep) — see the row below for drop-outside detach's own limit, measured the same way. Restoration carries two more of its own: only the PRIMARY window's frame is remembered across a relaunch — AppKit's own autosave, which sits beside `.restorationBehavior(.disabled)` on the `WindowGroup` and covers only the one window SwiftUI opens by itself, not the windows `windows.json` reopens; and a crash (or a force-quit) leaves `windows.json` as the LAST clean quit wrote it — the next launch consumes that one stale generation, never more, because every launch deletes the file it read. **Sight check for the maintainer's dev build**: move a tab to a new window; drag it back to the original window; drag the last tab out of a second window and watch that window close; ⌘W with the Settings window focused (Task 2's re-review moved every menu off an app-wide bridge onto per-window focused values for exactly this); turn on Keep on Top for a window, then open Settings — the Settings window renders BEHIND a floating window (Task 4's own sight-check note); toggle "Restore windows at launch" on, open a second window, quit WITHOUT closing either (⌘Q, which is the only path that writes the file), relaunch and expect both windows back with each tab showing its session's overview and nothing connected; then turn it off, quit and relaunch and expect one window. `swift test`: 4843 tests in 408 suites green, zero build warnings on a clean `swift build --build-tests` (recounted after Task 5's fix round 1, which added 11: five for the registry's window describers, two for the seed file's consume-or-discard rule, four for the quit-path and overview wiring). |
| [Detachable tabs: drop outside is detach](superpowers/specs/2026-09-03-detachable-tabs-design.md) | **Done 2026-09-05**, the AppKit way, on a maintainer report against dev build dev-46da7909: "when I drag a tab out of the window, a new file is created on the Desktop and no new window opens". Both halves of that sentence were one root cause — the drag was `.draggable` over a `String`. **What the Finder used to do:** a `String` payload exports `public.utf8-plain-text`, the Finder accepts anything conforming to it, so a tab let go on the desktop was written out as a text clipping carrying the drag's JSON envelope. **How it is built now:** `TabDragPayload` is `Transferable` over `UTType(exportedAs: "dev.noix.macscp.tab")` — `public.data`, no text conformance, no filename extension, declared in `scripts/package-app`'s `UTExportedTypeDeclarations` — and the tab's drag SOURCE is an `NSViewRepresentable` (`TabDragSourceView`) whose `NSView` is its own `NSDraggingSource`: it begins the session with a pasteboard writer offering that one type, answers `[.move, .copy]` within the application and `[]` outside it so no other app can take the drop, and reads `draggingSession(_:endedAt:operation:)` — an empty operation at a point inside NONE of this app's own windows (`TabDropOutsidePlan`, asked with the frames `TabDropWindowFrames.ours` keeps: visible, on the active Space, not a panel) is "this landed nowhere" and calls the window's existing `moveToNewWindow(_:)`, the same path the tab context menu and the Window menu already take. SwiftUI's `.draggable` is gone from the strip (two drag sources race one mouse-down) and the drop destination reads `TabDragPayload.self`, so the in-strip reorder and the cross-window move are unchanged in behaviour and stronger in refusal: a text drag, a file, a sidebar row now reach no closure at all. **Fix round 1 (2026-09-05):** the source offers `[.move, .copy]` within the application, not `.move` alone — SwiftUI's `dropDestination` negotiates its own operation and has historically asked for `.copy`, so a move onto another of our windows could be refused, end with an empty operation at a point outside the source window, and detach into a third window instead of moving; `hitTest` now answers `nil` for a left-button event carrying `.control`, because macOS delivers control-click as `.leftMouseDown` and the overlay was swallowing the secondary click that opens the context menu; and `acceptsFirstMouse` is `true`, so a drag from a background window's tab costs one gesture rather than two (the click still raises the window either way). **Fix round 2 (2026-09-05):** "outside" now means outside EVERY window of ours, not just the one the drag started in. `TabDropOutsidePlan` is asked with the frames of all the app's visible, non-panel windows (`NSApp.windows`, filtered by `TabDropWindowFrames.ours`) and answers "landed nowhere" only when the point is inside none of them — so a drop on another of our windows' NON-strip area (its file list, its terminal) is a no-op and the tab stays where it was, where round 1 detached it into a third window. **Fix round 3 (2026-09-05):** a window on another Space no longer counts as ours. `isVisible` reports whether a window is ordered in, not whether the user can see it — it is `true` for a window on another Space — so a second macSCP window left FULL-SCREEN on another Space carried a screen-sized frame that covered every point on the desktop, and detach would have silently stopped working with nothing on screen to explain it; `isOnActiveSpace` is now a fourth fact of the filter. **Accepted limit, stated rather than implied:** a drag CANCELLED with Escape ends with an empty operation too and AppKit reports no reason, so cancelling over the desktop or over another application's window detaches; cancelling over any window of ours does not; the new window opens where SwiftUI puts it, not where the tab was let go — `openWindow(value:)` takes no frame, and threading a position hint through `WindowSeed` was scoped out; and the AppKit overlay covers the tab's label but deliberately not its ✕, because an `NSView` that answers `hitTest` takes the mouse event outright and a button underneath one would stop working. **Sight check for the maintainer's dev build:** drag a tab out onto the Desktop and expect a new window and NO file written; drag a tab onto another window's strip and expect it to move, with no third window appearing; drop a tab on another window's FILE LIST (not its strip) and expect nothing to happen — the tab stays where it was; click a tab (activates), control-click it (context menu), right-click it (the same menu), hover it (the ✕ appears and closes it); drag a tab out of a window that is NOT in front and expect the drag to start on the first gesture; reorder inside one strip by dropping a tab on another tab's title. **Accepted limit, measured 2026-09-05 (Task 2 fix round 3), unchanged:** a tab moved into a window of its own is parked in `TabRegistry` until that window claims it, and if the window never appears the tab stays parked and invisible — SwiftUI reports no failure from `openWindow(value:)`, so there is no signal that says the window will never come, and fix round 2's guess at one (a reclaim on the source window's next activation) beat the real claim and left the new window blank. Two certain points end that state instead: the source window closing, which runs its unclaimed seeds through the ordinary `teardown(_:reason:)`, and the app quitting, which records them (`app` category, `window move parked seed=…` at park, `window move torn down unclaimed seed=…` at teardown) — at quit only the record is guaranteed, because `applicationWillTerminate` is synchronous and the teardown is main-actor `async`. **"Duplicate Tab" (2026-09-05)**, on the maintainer's own follow-up report ("the tab context menu still lacks Duplicate Tab"): a new `TabMenuEntry.duplicateTab`, offered exactly when the tab has a stored session to copy (`TabContextMenu.entries`'s `hasStoredSession` fact, the same `active ?? restored` rule `WindowRestorationPlan.sessionID` states for window restoration); picking it inserts a fresh tab right after the source (`TabsViewModel.addTab(_:after:)`, admitted through `ContentView.addTabRegistering(_:after:)` — the same door every other tab uses) and `TabDuplicationPlan.plan(sourceConnected:storedSessionID:)` (Core, pure, three cases) decides what the duplicate does next: dial the same stored session at once when the source is connected (a second, independent connection — one SSH connection per TAB is the invariant, not one per session), or show that session's overview when it is not. Key `tab.duplicate` = "Duplicate Tab" in all four App catalogs. |
| Quit tears down nothing | **Done 2026-09-05** (`docs/superpowers/plans/2026-09-05-quit-teardown.md`, two tasks plus one fix round). ⌘Q still closes no window — that was never the thing to change — so the app grew a quit path of its own. `AppDelegate.applicationShouldTerminate(_:)` counts the tabs that hold a session, over every tab `TabRegistry` knows including the parked ones, and returns `.terminateNow` when that count is zero. Otherwise it writes the restoration seeds FIRST (a teardown clears `activeStoredSessionID`, so describing the windows afterwards would describe them empty), returns `.terminateLater`, and one main-actor task runs the rest: the unclaimed parked seeds, then every open window's own close sequence in registration order, then the `app quit windows=<n> tornDown=<m> forced=<bool>` line, `flushSynchronously()`, and `NSApp.reply(toApplicationShouldTerminate: true)`. The four stages left `ContentView.teardown(_:reason:)` for `TabTeardown.run(_:reason:)` — a delegate has no view, and neither has a parked tab — so there is still exactly one owner of `cancelAll` → `stopAll` → `shutdown` → `disconnect`; `QuitSequenceTests` pins that the two `TeardownStage` cases are named in exactly two files under `Sources/`. `TabRegistry` stores the per-window closures and runs none: the file contains no `await` at all. **What the audit log now shows:** one `disconnected` row per stored session that was connected at quit, written by `AuditRecorder.recordDisconnected()` inside the one owner — before this, a session the user quit out of never got one. **`QuitWatchdog.bound` is fifteen seconds and is NOT a hard ceiling**, and the distinction is the point: when it wins, the teardown chain is cancelled, and cancellation is read between items and never inside one — between parked tabs and windows in `QuitTeardownChain.run`, and between the tabs a window holds in its own two loops. So at most ONE tab's teardown is still running when the bound elapses, and it runs to its own end (two `TeardownStage` bounds plus `disconnect()`'s, on top of a `cancelAll` measured under six milliseconds against a frozen peer but carrying no bound at all). A quit can outlast fifteen seconds by roughly one tab's worth; `forced=true` in the quit line is what says the bound won. The per-tab checks came from fix round 1 — before them a cancelled window still ran every tab it held, so the overrun scaled with the tab count. **One knock-on, found and fixed in the same round:** `AppRelauncher` (the Settings → Language relaunch) composed `sleep 1; open "<bundle>"`, which raced a quit that used to be instant. With the deferred quit the old process can outlive that second, and `open` on a running app activates the dying instance instead of launching a new one — the relaunch silently did nothing. The helper now polls `kill -0 <pid>` every 0.2 s, capped at sixty seconds, and opens the app afterwards either way. **Measured:** full suite green at 4930 tests in 419 suites, zero warnings; nine mutation probes across the two commits, all caught. **Sight check** (maintainer's dev build): ⌘Q with a connected tab and a transfer in flight → the app defers, the transfer is cancelled, the session's audit log gains a `disconnected` row, and the diagnostic log's last `[app]` line reads `quit windows=1 tornDown=1 forced=false`; ⌘Q with nothing connected → immediate, and that line is the unchanged `quit`; Settings → General → Language → change it → "Relaunch macSCP" with a connected tab open → the app quits, tears the tab down, and comes back. (The brief's third check named "Check for Updates → Install"; there is no such flow — `UpdateCheckModel` opens the release page in a browser, and the language change is the only caller of `AppRelauncher.relaunch()`.) |
| Forwardings "At login": the launch flag is unverified, and "Start in the background" is not shipped | **Open, recorded 2026-09-06** by Task 7 of the port-forwarding plan (`docs/superpowers/specs/2026-09-06-port-forwarding-design.md`, "Limits, stated"). `LoginLaunchDetector` (`Sources/MacSCPAppKit/LoginItem.swift`) reads the launch Apple event — `kAEOpenApplication` whose `keyAEPropData` parameter carries `keyAELaunchedAsLogInItem` — from `AppDelegate.applicationDidFinishLaunching(_:)`, and `LaunchAutoStartPlan.moments(launchedAsLoginItem:)` runs the `.login` profiles only when it answers `true`. **The parsing is measured** (`LoginItemTests`: a hand-built event with that reason reads `true`, one with `keyAELaunchedAsServiceItem` and one with no parameter at all read `false`). **What is NOT measured** is whether macOS 15 actually sends that reason at a real login launch: that needs a signed, registered `.app` opened by loginwindow, which neither `swift test` nor the dev build performs (both launch from a shell), and Task 7 was instructed not to launch the GUI. The failure direction is the safe one — an unread flag starts nothing — so a wrong `false` costs a manual start, never an unasked-for dial. **Because of that**, the design's optional "Start in the background" toggle (ordering the main window out at a login launch) was NOT built: it is only correct if the flag is right, and a wrong `true` would hide the window of an ordinary launch. **Two hypotheses, and the test must separate them.** (a) macOS 15 no longer sends `keyAELaunchedAsLogInItem` at a login launch. (b) The event is sent but this app never sees it: under `@NSApplicationDelegateAdaptor` SwiftUI installs the delegate itself, and if that happens AFTER the `oapp` event has been dispatched, `NSAppleEventManager.shared().currentAppleEvent` in `applicationDidFinishLaunching(_:)` carries no launch event at all — a `nil` descriptor, which `LoginLaunchDetector` reads as `false` exactly like a wrong reason code. The two are indistinguishable from the outside and would be measured as one. **To close this**: package with `scripts/package-app`, sign it well enough for `SMAppService.mainApp.register()` to reach `.enabled`, add a temporary `info` line logging the DESCRIPTOR itself (`String(describing: NSAppleEventManager.shared().currentAppleEvent)` — nil vs. `'aevt'\\'oapp'{ 'prdt':… }` vs. an `oapp` with no `prdt` parameter), log out and back in, and read that line together with whether the `.login` profiles started. `nil` is hypothesis (b) and the fix is an `NSAppleEventManager` handler installed earlier (or a plain `applicationWillFinishLaunching`); an `oapp` without `prdt`, or with another reason, is hypothesis (a) and the fix is to record the flag as unusable and drop the `.login` moment to "same as At launch" with the sheet's footer saying so. Only after (b) is excluded is shipping "Start in the background" a decision that can be made at all. Related and separate: an **ad-hoc-signed dev build cannot register a login item at all** — `SMAppService` answers `.notFound` or `.requiresApproval`, which the overlay shows verbatim rather than pretending the toggle took. **2026-09-16**: the maintainer decided to measure first; a Developer-ID-signed probe build (bundle id `dev.noix.macscp.loginprobe`, logging the launch Apple event at `applicationWillFinishLaunching` and `applicationDidFinishLaunching` to `~/Library/Logs/macSCP/login-probe.log`) was handed over; the outcome is pending. |
| A forwarding's failure reason is English on four localized surfaces | **Open, recorded 2026-09-06** by the final review of the port-forwarding plan (`docs/superpowers/specs/2026-09-06-port-forwarding-design.md`, "Limits, stated"). `TunnelState.failed(reason:)` carries the sentence `DialSupport.reason(for:)` rendered for the diagnostic log — English by construction, and by design a paste artifact of the audited line — and the App shows it verbatim on four surfaces, counted 2026-09-06 with `grep -rn "TunnelProfilesSheet.stateLabel" Sources/` plus the sheet's own column: the profiles sheet's state column (`TunnelProfilesSheet.stateLabel` itself), the autostart sheet's state column (`TunnelAutostartSheet.swift`), the Dock menu's tooltip (`TunnelDockPresence.swift`) and the sidebar glyph's tooltip (`SessionSidebar.swift`) — the last three all through `stateLabel`, so one place decides the text and four show it. **Not fixable in the App as written**: the case identity is discarded when the sentence is rendered, so nothing is left for `L10n` to map — a translation here would have to guess the failure back out of its own prose. **The fix shape**: `TunnelState.failed` carries a TYPED failure (the case and its data — a port number, a host, a refusal — not its rendering), Core keeps rendering the English sentence for the log from that value, and the App maps the same value through `L10n` into the four catalogues. A Core model change, and the reason it is a row rather than a fix round: it touches the state machine, its store round trip and every test that spells a reason string. **Done 2026-09-17** (`ab4a2c4c`, `8c9289a5`, technical-backlog plan Task 6): `TunnelState.failed`/`TunnelEvent.failed` now carry a new `TunnelFailureKind` (host/path/algorithm/session/port data, no secret, no raw error text, no fingerprint) instead of a free-text reason; `DialSupport.classify(_:)` is the one switch both `DialSupport.reason(for:)` (the English log sentence, unchanged for every pre-existing case) and the new `DialSupport.failureKind(for:)` read from. The App's `TunnelProfilesSheet.stateLabel`/`failureLabel(_:)` map each kind through `L10n` in all four catalogs on all four surfaces; a guard pins that no case renders `String(describing:)` and that every `TunnelFailureKind.Name` has a resolving key. The CLI and the log keep the old English sentence byte-identical through a new `TunnelRunner.failureReason`/`ForegroundTunnel.failureReason`, alongside the state's kind. `TunnelFailure.connectFailed(reason:)`'s three carrier refusals and the deleted-session case became a typed `TunnelRefusal`. Fix round 1 added `remotePortZeroRefused`, `remoteBindRefused(needsGatewayPorts:)` and `remoteForwardUnanswered` kinds so the design's "Limits, stated" promises (the GatewayPorts hint, the port-0 refusal naming itself) hold in the App again, not only in the log/CLI. Also closed in the same commit: the opaque `KeychainError` text row below. Residual: local-bind errors other than port-in-use (e.g. `EADDRNOTAVAIL`) still lose their errno/detail text in the App — new row below. |
| A login-set session's converted PEM key applies for one retry only | **Open, recorded 2026-09-10** by the PEM private keys design (`superpowers/specs/2026-09-10-pem-private-keys-design.md`) and its Task 4 review rounds. `ContentView.convertedKeyImported` re-points a failed attempt's session at the newly-converted OpenSSH copy and retries once; for a session bound to a login set, the next submit re-applies the set (`SessionListViewModel+Submit.swift`) and the converted key is dialled only after the person switches the login to Manual or edits the set itself. Offering a login-set edit straight from the failed surface is a scope decision, not made here. **Done 2026-09-16** (`5ce949b7`; fix rounds `d8db692c`, `5680442c`, `79e161ab`, `12573d5a`): the conversion sheet now offers exactly that edit. A confirmation names the set and how many sessions use it; confirming re-points the set's own `keyPath` at the converted key, drops the set's now-redundant Keychain slot when the managed key's slot holds the passphrase and no jump hop still reads it, and retries; declining (or Escape) keeps the one-attempt behaviour this row describes. Record: `superpowers/specs/2026-09-10-pem-private-keys-design.md`'s "Decisions 2026-09-16" section. |
| Session export counts a converted session's passphrase as missing | **Open, recorded 2026-09-16** by the final review of the maintainer-decisions plan. The export reads a session's secret from its own slot (`Sources/macSCPCore/Presentation/SessionListViewModel.swift:1403`) and counts a nil as missing (`:1405`). A private-key session whose own slot was dropped for the managed key's, or never written under `SessionSecretPolicy.usesStoredManagedPassphrase`, is counted in the "N passwords missing" total although its passphrase is stored under the key. **Done 2026-09-17** (`a5c73952`, technical-backlog plan Task 5): `exportPayload` now checks `managedKeyHoldsThePassphrase(authKind:keyPath:kind:)` for the session's own login (manual or set-bound) and for the jump hop, and counts either as covered rather than missing when the managed key's slot holds the passphrase; what the export writes is unchanged. |
| The login-set question's title names the set as captured | **Open, recorded 2026-09-16** by the final review of the maintainer-decisions plan. The dialog title formats `setRepointRequest?.set.name` (`Sources/MacSCPAppKit/ContentView+Sheets.swift:191`), the copy captured with the request, while "Update login set" writes the fresh re-read (`LoginSetRepointPlan.currentSet`, `Sources/MacSCPAppKit/ContentView.swift:2713`). A set renamed in another window while the question is open is updated under its new name and asked about under its old one. **Done 2026-09-17** (`2914526a`, technical-backlog plan Task 5): the dialog title reads `LoginSetRepointPlan.currentName(of:in:)`, which re-reads the set's current name from `sessionListViewModel.loginSets` (falling back to the captured name only when the set is gone). The message still names the key and the count, not the set's name, so only the title changed. |
| The login-set question says "sessions" for jump uses too | **Open, recorded 2026-09-16** by the final review of the maintainer-decisions plan. The count is `usageCount(of:)` (`Sources/macSCPCore/Presentation/SessionListViewModel.swift:847-848`), which is `sessionsUsing(setID:)` (`:840-843`) and includes sessions that use the set only for a jump in its own mode (`jump.loginSetID == setID && jump.sessionID == nil`); the message (`Sources/MacSCPAppKit/Resources/en.lproj/Localizable.stringsdict:117-132`) says "used by %1$lld sessions". **Done 2026-09-17** (`f9ce645b`, technical-backlog plan Task 5, fix round 1): a new `sessionsDependingOn(setID:)`/`dependentSessionCount(of:)` counts direct bindings plus sessions that read the set through a session-mode jump (via a private `jumpHopReadsSet(of:_:)`, also now backing `setServesAJumpHop`), counted once for a session both bound and jumping. The wording became "…is used by %lld sessions, directly or as their jump host…" in all four catalogs (pl one/other). `usageCount(of:)` itself, and its other two callers (the row text and the delete-confirmation message in `LoginSetsSheet.swift`), are unchanged — those mean "sessions that reference the set", which a delete restores, not the repoint population. |
| An in-place reconnect drops a dangling login set's error silently | **Open, recorded 2026-09-16** by the Task 1 review of the maintainer-decisions plan (pre-existing). `ContentView.connect(in:stored:)` runs its fill in a discarded Task (`_ = Task {`, `Sources/MacSCPAppKit/ContentView.swift:2236`) whose `guard try fillForm(form, from: stored)` (`:2250`) lets a `LoginResolver` throw from `resolvedCredentials(for:)` (`:2809`) escape uncaught, so a session bound to a deleted login set shows nothing on this path. **Done 2026-09-17** (`24c7514f`, technical-backlog plan Task 5). **This row's premise was inaccurate**: a dangling login set is already caught inside `fillForm` (published as `loginSets.missingSet`, `fillForm` returns `false`), covered by `ReconnectPathTests.aDanglingLoginSetStopsTheUnattendedSchedule` — that path was never silent. What could actually escape was any *other*, non-`LoginResolveError` throw from the fill, which no callee raises today (the resolvers read the Keychain through `try?`). Fixed anyway, as a structural boundary rather than a runtime case: `connect(in:stored:paneVisibility:)` now runs a `Task<Void, Never>` whose fill is wrapped in a `do`/`catch` that shows `error.localizedDescription` through `form.showFailure` on any throw — the same text the sidebar's external-terminal path shows for the same throw — so a bare `guard try fillForm(...)` inside a non-throwing task, which would not compile, cannot come back. |
| A Keychain error in a forwarding's reason renders as an opaque system sentence | **Open, recorded 2026-09-16** by the final review of the maintainer-decisions plan. A `KeychainError` thrown by the forwarding's secret chain (`SecretResolver` in `Sources/macSCPCore/Tunnels/TunnelConnection.swift:82`) reaches `TunnelRunner.swift:288`'s `DialSupport.reason(for:)`, which has no arm for it and falls to `(error as NSError).localizedDescription` (`Sources/macSCPCore/Diagnostics/DialProbes.swift:341-342`): "The operation couldn’t be completed. (macSCPCore.KeychainError error 1.)". **Done 2026-09-17** (`ab4a2c4c`, technical-backlog plan Task 6, same commit as the typed-failure row above): `DialSupport.classify(_:)` gained a `keychainUnreadable` kind with a fixed sentence, "the keychain could not be read", replacing the opaque `NSError` text on every surface that reads through `DialSupport.reason(for:)`/`failureKind(for:)`. |
| An alternative sidebar layout like Termius (maintainer wishlist) | **Not started.** Recorded 2026-09-16 from the maintainer's wishlist (item 2): "an alternative sidebar view like Termius". Exists today: one sidebar layout, `SessionSidebar` (`Sources/MacSCPAppKit/SessionSidebar.swift`, mounted at `ContentView+Detail.swift:35`) — a tree of groups and sessions with a density setting (`SettingsStore.sidebarCompact`) and the tag filter; the session overview (New features, "Session overview") is a detail view, not a sidebar. **Ambiguous, recorded rather than resolved:** the wish names a product, not a layout; which aspect of that product's sidebar is meant is not stated and needs the maintainer, ideally with a mockup before a design. Open: a switchable alternative beside today's tree (as "alternative" suggests) or a replacement. Not started. **Decided by the maintainer 2026-09-16 (accepting the proposal in chat):** a mockup with tiles and groups to choose from, before anything is built. |
| Custom terminal themes (maintainer wishlist) | **Not started.** Recorded 2026-09-16 from the maintainer's wishlist (item 8). Exists today: fixed colours — `SSHTerminalView` sets background, foreground and caret from `DesignTokens.terminalBackground`/`terminalText` (`Sources/MacSCPAppKit/SSHTerminalView.swift:31-33`; `DesignTokens.swift:61`, `:64`). M9d left this out by decision ("No color/theme selection", `docs/superpowers/specs/2026-07-28-m9d-terminal-appearance-design.md:79`), so the wish reopens that scope line. SwiftTerm at the pinned revision `d5ee56e` exposes `installColors(_:)` for the ANSI palette (`Sources/SwiftTerm/Apple/AppleTerminalView.swift:411` in the checkout). Open questions: what "custom" covers (shipped presets, a user editor, importing another terminal's theme file); whether the choice is global or per session. Not started. |
| Highlight terminal output by regex (maintainer wishlist) | **Not started.** Recorded 2026-09-16 from the maintainer's wishlist (item 7): "Highlight terminal syntax by a regex". Exists today: nothing colours terminal output. `SnippetHighlighter` (`Sources/macSCPCore/Terminal/SnippetHighlighter.swift:30`) colours snippet COMMANDS in the editor and dry run, not the output stream. SwiftTerm at `d5ee56e` has link highlighting (`LinkHighlightMode`, `Sources/SwiftTerm/Apple/AppleTerminalView.swift:51`) and a search service (`SearchService.findAll`, `Sources/SwiftTerm/SearchService.swift:88`); whether either can carry persistent per-pattern colouring was **not checked**. Related open wish: Cmd-click on a URL in the terminal (item 11 of `superpowers/specs/2026-09-02-backlog-maintainer-notes.md`), link detection on the same surface. Open questions: rules global or per session/tag; colour only, or also weight/background; the cost of matching every output chunk against user-written patterns (a pathological pattern on a busy stream). Not started. |
| Terminal: copy on select, paste on right click (maintainer wishlist) | **Not started.** Recorded 2026-09-16 from the maintainer's wishlist (item 17): a setting — selecting text copies it, a right click pastes — default off. Exists today: no such setting (the full key list, `Sources/macSCPCore/Settings/SettingsStore.swift:58-90`). A right click on the terminal surface is already taken whenever snippets exist: it opens the snippet menu (`SSHTerminalView.attachSnippetMenu`, `Sources/MacSCPAppKit/SSHTerminalView.swift:105`; `snippetContextMenu`, `:136`), and with no snippets it does nothing. **Ambiguous, recorded rather than resolved:** one switch for both behaviours (as written) or two; and where the snippet menu goes while the setting is on. Open: whether a right-click paste takes the same bracketed-paste path as a normal paste (`bracketedPasteQuery`, `SSHTerminalView.swift:53`). Not started. **Maintainer answers 2026-09-16 (same day, in chat):** while the setting is on, a right click pastes and replaces the snippet menu there; the snippet menu stays reachable some other way, the maintainer suggests Option-right-click. Still open: one switch or two. **Decided by the maintainer 2026-09-16 (accepting the proposal in chat):** two separate switches (copy on select; paste on right click), both default off. **Done 2026-09-17** (`c5739646`, the next-build plan's Task 6): `SettingsStore.terminalCopyOnSelect`/`terminalPasteOnRightClick` (`SettingsStore.swift:70-71`), both default off, with a new Section (two toggles and a footer) in `TerminalSettingsTab` (`SettingsView.swift:1011`). A right-click paste goes through the same bracketed-paste path a normal paste uses; while the setting is on, Option-right-click opens the snippet menu instead of a plain right click, per the maintainer's suggestion. Minors deferred at review (their own row below). |
| Terminal emulation type (maintainer wishlist) | **Partly there.** Recorded 2026-09-16 from the maintainer's wishlist (item 18): choose the terminal emulation type (xterm, linux, vt100, etc.). Exists today: the plumbing takes a name — `RemoteShellProvider.openShell(terminal:cols:rows:)` (`Sources/macSCPCore/RemoteFS/RemoteShell.swift:27`) — but the only caller passes a literal, `openShell("xterm-256color", 80, 24)` (`Sources/macSCPCore/Presentation/TerminalPanelViewModel.swift:151`); no setting, no session field. Open questions: a global setting, a per-session field, or both; the name only tells the server what to send — the emulator stays SwiftTerm's, so offering a type SwiftTerm does not render faithfully would be a promise the surface cannot keep (which types are safe is unmeasured). Not started. |
| A resizable Settings window (maintainer wishlist) | **Not started.** Recorded 2026-09-16 from the maintainer's wishlist (item 24): the settings panel should be resizable. Exists today: `SettingsView` pins its size, `.frame(width: 680, height: 620)` (`Sources/MacSCPAppKit/SettingsView.swift:161`). Open: whether the size is remembered; the smallest size every pane survives in its widest localization (the diagnostics footer, "Dev-build UI follow-ups 2" in Bugs, measured French as the widest before choosing a minimum). **Done 2026-09-17** (`745d62b4`, `2a35c832`, the next-build plan's Task 4): the fixed `.frame(width: 680, height: 620)` is gone, replaced by `.windowResizability(.contentMinSize)` and a 680×620 minimum; fix round 1 (`2a35c832`) found, by measurement against a scratch SwiftUI app, that `.windowResizability(.contentMinSize)` alone does not give a Settings window the `.resizable` style mask, and inserts `.resizable` in the `WindowAccessor` closure only when missing. The size is remembered via an AppKit frame autosave name (`macSCP.settings`) through `WindowAccessor`. Sight check outstanding: edge drag + zoom button (its own row below). |
| "Open with": rules scoped globally or per protocol, presented differently (maintainer wishlist) | **Partly there.** Recorded 2026-09-16 from the maintainer's wishlist (item 25): improve the "Open with" rules editor, let a rule be global or restricted to a protocol, and present it differently — **mockup first**. Exists today: Settings → "Open with" (`Sources/MacSCPAppKit/SettingsView.swift:13`) with a default editor (`SettingsStore.defaultEditorPath`, `Sources/macSCPCore/Settings/SettingsStore.swift:188`) and per-extension rules, a flat extension → app-path map (`SettingsStore.fileAssociations`, `:209`; listed at `SettingsView.swift:744`); `EditorResolver` resolves extension rule → default editor → system association (`Sources/MacSCPAppKit/EditorResolver.swift:13-20`). No rule knows a protocol. Open questions: what "improve" covers beyond the scoping (the wish does not list it); precedence when a global and a protocol-scoped rule both match; whether "protocol" means `ConnectionKind` (`ssh`, `s3`, `webdav` today) or something finer. Not started. **Decided by the maintainer 2026-09-16 (accepting the proposal in chat):** a mockup first; the maintainer decides the presentation from it before anything is built. |
| A confirmation before "upload all / download all" (maintainer wishlist) | **Not started.** Recorded 2026-09-16 from the maintainer's wishlist (item 28): for the upload-all / download-all actions, a safety dialog asking whether the user really wants to upload or download everything. **Ambiguous, recorded rather than resolved:** no action named "upload all" or "download all" exists (no such string in `Sources/MacSCPAppKit/Resources/en.lproj/Localizable.strings`, grep 2026-09-16). What exists: the toolbar's Upload and Download buttons (`Sources/MacSCPAppKit/ContentView+Transfers.swift:66`, `:84`) and the context-menu route, both through `transferSelection` (`:93`), which enqueue whatever is selected — a directory as a whole tree (`enqueueTree`) — without asking; drag & drop is a further route. Which of these the wish means (every transfer, a multi-item or directory selection only, above a size or count) needs the maintainer. Shape precedent: the bulk "Close Other Tabs" confirmation (`Sources/MacSCPAppKit/ContentView+Sheets.swift:41-47`). Not started. **Maintainer answers 2026-09-16 (same day, in chat):** the wish means the two arrow buttons in the toolbar (one up, one down) — `uploadButton`/`downloadButton`, `Sources/MacSCPAppKit/ContentView+Transfers.swift:61` and `:79`, which transfer the current selection including whole folders without asking. Still open: whether they always ask, or only when the selection holds more than one item or a folder. **Decided by the maintainer 2026-09-16 (accepting the proposal in chat):** the two toolbar buttons ask only when the selection holds more than one item or a folder; a single file transfers without a question. **Done 2026-09-17** (`341ab06b`, `ce66e592`, the next-build plan's Task 5): a pure `ToolbarTransferPlan.plan(for:)` decides `.transferNow`/`.ask(itemCount:includesFolders:)` — symlinks are filtered out first and do not count, `.other` counts as an item, not a folder; `uploadButton`/`downloadButton` route through it into a confirmation sheet in the house shape (stringsdict, four languages) naming the count and whether folders are included. The context menu's transfer to the other pane, `transferToSession`, and drag and drop are unchanged and do not ask, stated in the plan type's own doc comment. Fix round 1 (`ce66e592`) pinned the session-identity guard in `confirmToolbarTransfer` (unpinned at review) and passed the captured destination path into `transferSelection` on both toolbar paths. Sight check outstanding: dialog wording in four languages (its own row below). |
| Tabs inside the terminal panel (maintainer wishlist) | **Not started.** Recorded 2026-09-16 from the maintainer's wishlist (item 29): tabs in the terminal as well. Exists today: one terminal per connection tab — `SessionTab.terminal` is a single `TerminalPanelViewModel` (`Sources/MacSCPAppKit/SessionTab.swift:14`) opening one shell. **Reading, recorded as a reading:** several shells within one connection tab's terminal panel. That fits the invariant as written (one SSH connection per tab, the shell a child channel over it — CLAUDE.md "Architecture invariants"), but it changes a stage the teardown and quit paths name: the terminal `shutdown` in `TabTeardown.run(_:reason:)` would own several shells, not one. Open: whether a terminal tab can move to another connection tab or window; which terminal a snippet run targets when several are open. Not started. **Decided by the maintainer 2026-09-16 (accepting the proposal in chat):** yes — several shells inside one connection tab, all over that tab's one SSH connection. |
| The main window's size across launches (maintainer wishlist) | **Partly there.** Recorded 2026-09-16 from the maintainer's wishlist (item 32): remember the main window's size so it is the same after opening. Exists today, read from the code and **not reproduced**: the primary window carries an AppKit frame autosave name (`ContentView.applyFrameAutosave(to:)`, `Sources/MacSCPAppKit/ContentView+Lifecycle.swift:990-993`; the name `macSCP.primary` at `ContentView.swift:914`), introduced in `71fdf913`. Two things in the same code can make the remembered size not the one expected: a window left with a single unconnected tab shrinks to 700×460 (`shrinkIfPristine`, `ContentView+Lifecycle.swift:2040-2046`, called at `:1586`, `:1810` and `ContentView.swift:3274`), so a quit after a disconnect saves the form size; and the size a connect grows back to is `lastBrowserSize`, in-memory `@State` (`ContentView.swift:308`), so after a relaunch a connect grows to at least 930×620 (`ContentView.swift:1847-1849`), not to the last browser size. Which of these the maintainer sees is unknown — reproduce first. Found along the way: `resizeWindow(toWidth:height:)`'s doc comment still says the app "sets no `frameAutosaveName`" (`ContentView+Lifecycle.swift:2020`), which `applyFrameAutosave` contradicts — a stale comment in the sense of CLAUDE.md "Comments that describe other code". **Done 2026-09-17** (`1199a847`, `7cb822bd`, the next-build plan's Task 3): "keep the autosave on the browser frame only", plus a persisted size (`MainWindowSizePlan`, `SettingsStore.mainWindowBrowserSize`). `shrinkIfPristine` now persists the pre-shrink size, suspends the autosave (re-applies name `""`) before resizing, so the form size is never written as the frame; a connect grows to `max(remembered, 930×620)`; a live-resize drag persists the size too, but only for the primary window, with its active tab connected, not in full screen, and with the autosave not suspended (`MainWindowSizePlan.persistsLiveResize`). The stale `resizeWindow(toWidth:height:)` comment is fixed. Fix round 1 (`7cb822bd`) found, and fixed, that resuming the autosave can land the window on `NSScreen.main` rather than its own display, and pinned `isPrimaryWindow` arguments that had been unpinned at review. Residual gaps, their own rows below: the launch-time name set (not the resume path this fix covers) likely also lands on `NSScreen.main`; a few smaller review deferrals. |
| Notifications on errors and disconnects (maintainer wishlist) | **Not started.** Recorded 2026-09-16 from the maintainer's wishlist (item 22): notifications on errors, disconnects, etc. Exists today: no system notifications — no `UserNotifications`, `UNUserNotificationCenter` or `NSUserNotification` in `Sources/` (grep, 2026-09-16); in-app surfaces exist (the menu-bar status item, `Sources/MacSCPAppKit/MenuBarController.swift:22`; error captions and dialogs; the reconnect offer, `SettingsStore.reconnectBehaviour`). **Ambiguous:** "etc." leaves the event list open (candidates: a dropped connection, a failed transfer, a failed forwarding, a finished queue), and macOS notifications versus in-app banners is not stated. Open questions: `UNUserNotificationCenter` asks the user for authorization and is tied to the bundle identifier (`dev.noix.macscp`, `scripts/package-app`) — how an unbundled `swift run` build behaves is unmeasured; a notification's text must not carry what a diagnosis row must not (Bugs, "Diagnostics: error text as a leak route"); per-event switches. Not started. **Decided by the maintainer 2026-09-16 (accepting the proposal in chat):** macOS notifications (not in-app banners) for a lost connection, a failed transfer and a failed forwarding. **Done 2026-09-17** (`4dc5483b`, `8984d29c`, the next-build plan's Task 7): a pure `ErrorNotificationPlan` decides whether to post; `ErrorNotifier` holds a `UserNotificationPosting` seam and posts through `UNUserNotificationCenter` only when `Bundle.main.bundleIdentifier` is non-nil (an unbundled `swift run` posts nothing, measured by reading, not by running unbundled). A `SettingsStore.notificationsEnabled` toggle (default on) in Settings → General gates all three events; authorization is requested lazily, at the first post, not at launch. Coordinator ruling recorded here: at most one "transfer failed" notification per tab until that tab's window next becomes key (the user has seen it); a connection-lost notification fires once per transition; the default is silent, and the live notifier is reached only by wiring it in — nothing reaches it by omission — with the live notifier injected only from `MacSCPApp.init`. Fix round 1 (`8984d29c`) added the `willPresent` delegate so a foreground post is shown as a banner/list entry (round 0 left it unshown while the app was frontmost), fixed the live notifier being reached by existing tests through no seam at all, and added the per-tab latch. Minors deferred at review, and sight checks, each their own row below. |
| Unobtrusive easter eggs (maintainer wishlist) | **Not started.** Recorded 2026-09-16 from the maintainer's wishlist (item 26): build in easter eggs, unobtrusively. Exists today: none (grep for "easter", 2026-09-16). Rules already in force: every user-visible string lives in all four catalogs (`en`, `de`, `fr`, `pl`), the German one in the du form (`Tests/macSCPCoreTests/GermanAddressFormTests.swift`); public-facing text names no tech stack. Open: what, where, and what counts as unobtrusive — the wish names no content. Not started. |
| The login-set repoint question's session/jump count is captured when the dialog opens | **Open, recorded 2026-09-17** by Task 5's review of the technical-backlog plan (deferred M3). The dialog's title now re-reads the set's current name (Done above), but its plural count (`connection.convertKey.repoint.message %lld %@`) is still the count captured when the question was first asked — a session added or removed as a dependent of the set while the dialog is open is not reflected before the user answers. Not started. |
| A forwarding's local-bind failure message loses its errno/detail text on non-loopback and non-port-in-use failures | **Open, recorded 2026-09-17**, residual of "A forwarding's failure reason is English on four localized surfaces" above (now Done). The App's four surfaces now show a translated fixed sentence per `TunnelFailureKind`, and Task 6's fix round 1 restored the GatewayPorts hint and the port-0 refusal for remote forwards specifically — but a LOCAL bind failure other than port-in-use (e.g. `EADDRNOTAVAIL`, a permission error) still collapses to the plain "could not start listening" sentence in the App; the log and the CLI keep the full errno/detail text through `TunnelRunner.failureReason`. Separately: the `de`/`fr`/`pl` tunnel-failure catalog strings added across Tasks 5-7 are the implementers' own translations and have not been reviewed by a native speaker (`GermanAddressFormTests` and the parity/du-form suites pass, which checks form, not fluency). Not started. |
| The tab (and its preview) still say "New Connection" while a session is being edited | **Not started.** Recorded 2026-09-18 from the maintainer's report: while a session is being edited, the tab still says "New connection"; the same happens in the session preview. Exists today: `SessionTab.displayTitle` (`Sources/MacSCPAppKit/SessionTab.swift:419-420`) reads `titleName ?? L10n.string("tabs.newConnection", "New Connection")`, and `titleName` is set only on a successful connect — the label knows only "connected or not", not the form's `.edit(sessionID:)` mode or the overview a tab is showing. In German the label is "Neue Verbindung" (`Sources/MacSCPAppKit/Resources/de.lproj/Localizable.strings:410`), which is most likely what the maintainer saw; that reading is an assumption and worth a confirming question. Not started. |
| "New group" is missing from a (sub)folder's context menu | **Not started.** Recorded 2026-09-18 from the maintainer's report: "New group" is missing from the context menu on subfolders. Exists today: the folder-row menu (`Sources/MacSCPAppKit/SessionSidebar.swift:1318-1344`) offers Rename, Export Group, Sort by Name, Move to and Dissolve, with no "New group" entry; "New group…" exists only in the list background menu (`:1000-1002`, which creates a top-level group) and a session's "Move to" submenu (`:1697`). Core cannot create a nested group either: `createGroup(named:)` (`Sources/macSCPCore/Presentation/SessionListViewModel.swift:747-762`) always builds a `StoredGroup` with `parentID: nil`, although `StoredGroup.init(parentID:position:)` (`Sources/macSCPCore/Sessions/StoredGroup.swift:25`) already takes one. Not started. |
| "New group" is missing from the group picker in the session editor | **Not started.** Recorded 2026-09-18 from the maintainer's report: "New group" is also missing from the group picker in the session editor. Exists today: `ConnectionFormView.swift:478-489` offers "No group" plus `ForEach(groups)` and nothing else; the view already holds `sessionList` (the `SessionListViewModel`) and could create a group through it. Not started. |
| Group pickers should show the tree, everywhere a group is chosen | **Not started.** Recorded 2026-09-18 from the maintainer's report: the group picker in the session editor, and every other place a group is chosen, should show the tree so the user sees where a group sits; every place should be able to show it. Exists today, all flat: the session editor's picker (`ConnectionFormView.swift:478-489`, fed at `ContentView+Detail.swift:878`) is a flat `ForEach(groups)` in store order, names only; a session row's "Move to" (`SessionSidebar.swift:1690-1698`) and a folder row's "Move to" (`:1334-1343`) both go through `moveToMenuItems`, flat names in `SidebarOrdering.moveTargets`'s depth-first order (`Sources/macSCPCore/Sessions/SidebarOrdering.swift:230-256`); "Import from Cyberduck"'s group choice (`ImportFromSourceSheet.swift:226-236`) is flat names too. Already tree-shaped: drag and drop onto a folder, and the CLI's `--group` (`SessionFieldOptions.swift:89`), which already prints a path such as "Work / Prod". Not started. |

## New features

| Entry | Core |
|---|---|
| [Maintainer notes 2026-09-02](superpowers/specs/2026-09-02-backlog-maintainer-notes.md) | 22 items dictated in chat: one bug (the terminal does not resize with the window), eleven small explorer/sidebar/transfer items (duplicate opens the edit form, folder drop to root, session drag & drop, "Move to…", compact sidebar, cancel transfers, path-bar click area, full transfer paths, a status line, Cmd-click URLs, Space preview), and ten features wanting a brainstorm each (session preview on single click, a wastebasket with 30-day purge, folder colours and icons, detachable sticky tabs, S3 access-level probe, what's-new dialog, `..` row, Docker volumes, rsync for SSH). Not scheduled; recorded as given. **Decided:** first polish plan = terminal resize (bug) + transfers (cancel, full paths); first brainstorm = detachable/sticky tabs. **Items 1, 7, 9 done 2026-09-03** (`docs/superpowers/plans/2026-09-03-polish-terminal-and-transfers.md`): the terminal-resize cause was measured as (b) — the surface's mount-time `sizeChanged` dropped by `TerminalPanelViewModel.resize`'s `guard let shell` while the shell opened at a hardcoded 80×24 before layout — and fixed (`6a27ad64`/`63bf5242` measurement, `2d0fa18f`/`1cc7f07a`/`273c6786` fix); the split layout's cols-but-not-rows behaviour was ruled intended, not a defect. Transfers gained "Cancel all" and a per-row cancel (`66bea864`/`278c4361`) and full source/destination paths on hover plus "Copy paths" (`4d0b04f9`/`3f77a605`). Still owed: the maintainer's live `stty size` check. **Maintainer wishlist 2026-09-16** (Recorded 2026-09-16 from the maintainer's wishlist, items 19 and 27). (19) "rsync GUI possible? e.g. syncing a whole folder via rsync" — the same wish as item 22 here (rsync for SSH sessions), raised again; still nothing in the tree (`rsync` appears in `Sources/` only as an example in `Sources/macSCPCore/Terminal/SnippetCommandSurvey.swift:264`). Item 22's constraints stand (SSH only; the remote may lack `rsync`; a capability probe first), plus one: if `rsync` runs as a local subprocess it dials with its own transport (normally the system `ssh`), not the tab's connection, so its host-key and secret paths have to be stated — the external-terminal command line (`SSHCommandBuilder`) is the existing precedent for handing a session to the system `ssh`. (27) "Docker support: show active containers and open them" — related to item 21 here (Docker volumes), but a different scope: containers, not volumes; nothing in the tree (`docker` appears in `Sources/` only in comments — about the test rig, and as an example snippet tag in `SnippetMenuModel.swift:67` and `SnippetTagSuggestions.swift:4`). **Ambiguous:** what opening a container means (a shell inside it, its file system in the browser, its logs), and whether the daemon is local or reached over the SSH session — item 21's own scoping question. Neither started. **Maintainer answers 2026-09-16 (same day, in chat):** for item 27 (Docker): local Docker AND Docker on the SSH server; "open a container" means managing the files inside it and using a terminal in it. Read as "both" from the answer "lokal, SSH …"; confirm if only one was meant. |
| [Cyberduck import](superpowers/specs/2026-09-03-cyberduck-import-design.md) | **Done 2026-09-03** (commits `167cbfbe` Task 1 provenance, `d146ce5d` Task 2 `CyberduckBookmarkSource`, `2c354476`/`e8a61bd5`/`61eeb24e` Task 3 `ImportPreviewPlanner`, `43c4b4d2` Task 4 `CyberduckSecretReader` gated `MACSCP_KEYCHAIN=1`, `b612ebe1` + `70188e09` (a three-state keychain answer: only a query that ran and found nothing counts as "not read"; the folder is read off the main actor; 4148 tests green) Task 5 menu entry/sheet/view model). Sessions menu → "From Cyberduck…" reads Cyberduck's bookmark folder and shows every bookmark in a preview with a checkbox and a status (new / known unchanged / known changed with the field diff / not supported), importing or updating the selected ones through the existing import planner; `StoredSession` and `ExportedSession` carry `importSource`/`importID`/`importedAt` so a later run recognises what it imported. sftp and s3 bookmarks import; secrets are read from Cyberduck's own keychain items only with the switch on (default off); one switch governs group and labels together; 4148 tests green after the last commit (`70188e09`). Decided along the way: a record moved by id onto a connection another record holds asks the user nothing (documented at the branch, no resolution vocabulary could say "update anyway"); `secretsNotRead` counts only bookmarks the reader queried and found nothing for (`70188e09`). Plan: [2026-09-03-cyberduck-import.md](superpowers/plans/2026-09-03-cyberduck-import.md). Open follow-ups: WebDAV bookmarks after a measurement (Cyberduck's `dav`/`davs` carry path-shaped URLs, unmeasured); FTP/FTPS once that backend exists; FileZilla (`sitemanager.xml`) and Transmit sources through the same `BookmarkSource` protocol; the dev-build-only verification that the sheet actually renders (no GUI was launched by any implementer). |
| [FTP and SMB/AFP](superpowers/specs/2026-08-25-backlog-further-protocols.md) | Two very different halves under one word. macOS already speaks SMB/AFP — it would be about integration, and the usual TOFU guarantees have no counterpart there. FTP would first need a decision on library and variant, since bare FTP transmits credentials in plaintext. **Do not take on together.** **Revised 2026-09-02 (same evening): both, and standalone** — FTP and FTPS must both work (plain FTP behind the plaintext gate), SMB as a native SMB2/3 client with no mount. **Measured 2026-09-02:** SMB → `SMBClient` (MIT, pure Swift, builds under `.v6`; no SMB3 encryption, an open thread-safety issue) over `libsmb2`/`AMSMB2` (LGPL-2.1 product); FTP/FTPS → own NIO code (nothing maintained exists), NIOSSL is a new dependency. Designs next, FTP first. **Maintainer wishlist 2026-09-16** (Recorded 2026-09-16 from the maintainer's wishlist, items 3 "smb" and 4 "ftp"): both raised again as single words, with no requirement beyond this row's 2026-09-02 revision. Still nothing in the tree: `ConnectionKind` has three cases, `ssh`, `s3`, `webdav` (`Sources/macSCPCore/Connection/ConnectionKind.swift:4-8`). Not started past the library measurement. |
| [Tools for inspecting a connection](superpowers/specs/2026-08-25-backlog-connection-tools.md) | **Done 2026-09-03** (`docs/superpowers/plans/2026-09-03-connection-tools.md`, commits `8997c955`…`b6e181f3`, listed in the entry). Shipped: resolve, TCP ping and the dial as one report behind the descriptor seam; ICMP echo, matched on a payload marker plus a per-socket nonce because an unprivileged ICMP socket is not demultiplexed — measured, another process's `ping` lands its replies on this probe's socket too; an IPv4 network trace with its own 20 s budget and honest endings (a budget-cut hop is marked, never a silent `*`); the panel behind three doors (tab, session menu, error dialog) with rows appearing incrementally and a diagnosis bound to the tab that opened it; S3 access level (`HeadBucket`/`ListObjectsV2`/`ListBuckets`) and WebDAV server claims (`OPTIONS`/`PROPFIND`) as the seam's first two contributions. Open: SSH's negotiation contribution needs the swift-nio-ssh fork's own observer; the IPv6 trace is unmeasured (no route on the measuring machine); the 20 s trace budget is a judgement, not a measurement; `traceHopUnreachable` has never gone red from a real network message; the twelve cancellation sites in `ConnectionDiagnostics.run()` are a convention, not a structural guarantee; the panel's detail-renderer guard has a known blind spot that wants a structural row type. **Selective runs and the hop table done 2026-09-03** (`docs/superpowers/plans/2026-09-03-diagnostics-selective-runs.md`, commits `0176da62`, `5ea7f2ba`, `7a81a455`, `36edabd4`): a scope menu beside Run starts just one probe (ping, trace, dial, or the protocol probes) instead of the whole diagnosis, and the trace's hops render as a table (hop, address, RTT, outcome) rather than one packed detail line. |
| [Session overview](superpowers/specs/2026-09-04-session-overview-design.md) | **Done 2026-09-04** (`docs/superpowers/plans/2026-09-04-session-overview.md`, commits `7b2d5eeb` Task 1 model, `c3e45c10` Task 1 fix round, `df58a6a0` Task 2 view and wiring, `9d4e4a4f` Task 2 fix round, `fefdab39` Task 3 Run wiring, `066afd2e` Task 3 fix round). A single click on a stored session now shows a read-only overview in the detail area instead of the empty "New connection" form: facts drawn from the stored record and the descriptor's field vocabulary, never a secret value; the three actions Connect / Edit / Diagnose resolving to the sidebar's existing effects (`connectFromSidebar`, `editStored`, `showDiagnostics(for: .stored)`) so no fourth way to do any of the three exists; the last 10 recent connections paired out of the session's own audit log (`connected` … `disconnected` with duration and transfer counts, plus the new `AuditEvent.Kind.connectFailed` row carrying its own "Open diagnosis"); and every stored snippet with a Run that connects, waits for the terminal, then sends. Responsive: the head (name, kind badge, endpoint, actions) sits pinned outside a `ScrollView` that carries the rest; three `ViewThatFits(` in `SessionOverviewView.swift` (the actions row, the two-column facts `Grid`, the recent-connections table) plus a `LazyVGrid(.adaptive(minimum: 260))` for the snippet cards; the history table's transfers column is what drops when narrow, not the whole row. **Critical, caught by the Task 3 review**: `deliverPendingSnippetRun` called `terminal.openIfNeeded()` unconditionally, and `TerminalPanelViewModel.openIfNeeded()` reopens from `.ended` — every reopen attempt is a state change, which re-wakes the `PendingSnippetRunner` watching it, so a shell that would not open looped forever. Pinned in `066afd2e` by a `switch` on the terminal's own state: only `.closed` opens one; `.opening` waits; `.ended` clears the pending run and says so out loud (`overview.snippets.shellDidNotOpen`, all four catalogs) since the panel was never revealed to show its own "did not open" state itself. Counted at HEAD: 84 new tests across the five suites this plan added (`ConnectionHistoryTests` 15, `SessionOverviewModelTests` 29, `SessionOverviewNamesTests` 3, `SessionOverviewWiringGuardTests` 30, `SnippetAfterConnectSequenceTests` 7), 33 `overview.*` keys per App catalog and 9 `core.overview.*` keys per Core catalog, all four languages; sensitivity measured by planting and reverting 4 + 7 + 13 + 17 = 41 violations across the three tasks' guard rounds, 40 caught red — the one exception (Task 3's first round, the pending-run clear moved to after the send instead of before) stayed green and is recorded as such rather than hidden, because nothing downstream re-enters synchronously today. Open: no view was rendered by any test in any task — every claim about the responsive split, the alert appearing and the runner's `.onChange` firing is a source-text claim; the dev build is the maintainer's own sight check. The snippets section is hidden entirely for S3 and WebDAV (`supportsShell` gates the whole section, not the card's button) — a design decision the maintainer may still revisit rather than a defect. `.ended(nil)`, a shell the user exited normally, would share `.ended`'s "did not open" sentence if a snippet were still pending at that moment — unreachable on the current path (the pending run clears at the send, and the send happens on the first `.running`), but the sentence is written for the case that actually occurs, not for both. The secret-field scan in `SessionOverviewWiringGuardTests` is a substring match over three derived ids (`password`, `passphrase`, `secretAccessKey`); a fourth secret field would need the scan re-derived rather than edited, and the check is about spellings, not about capabilities — what actually keeps a value off the view is the model's `Bool?`, which is structural. |
| Diagnostics: a running row names its step | **Done 2026-09-04** (this commit; `docs/superpowers/plans/2026-09-04-diagnostics-running-step.md`, one task). Maintainer feedback from the dev build dev-a61df6bd: while a step runs the line should name it, not read the bare "Messung läuft…". Core grew a start event — `DiagnosticRunObserver` carries `onStepStarted(id:titleKey:)` beside the existing `onStep`, `run(scope:observer:)` is the primary and `run(scope:onStep:)` a one-line wrapper (still what `macscp-cli diagnose` calls). Six kinds of step announce themselves from **two** code sites: `ConnectionDiagnostics.starting(_:announcedTo:)`, which the four universal steps (resolve, tcp, icmp, trace) build their timers through, and `bounded(_:_:)`, which the dial and every contribution run through. The one row that announces nothing is the endpointless `noHost` row — it is a finished row, and nothing measured it. The panel's key is **`diagnostics.running.step`** (`en` "%@ — measuring…", `de` "%@ · Prüfung läuft…", plus `fr`/`pl`), filled with the step's own `titleKey` — the same key its finished row is titled with, so no step is named twice — and the unnamed `diagnostics.running` stays for the gap between two steps. Guarded by `theMeasuringLineNamesTheStepInFlight`, which derives both the view model property (the one `String?` it declares) and the line (the one HStack drawing a ProgressView), and holds the two keys to their shape rather than their spelling; sensitivity measured by planting six violations across the guard, the view model and Core (a panel that reads no running key, a second `Text` drawing the key raw, a key that does not extend the plain one, the row-clear removed, the end-of-run clear removed, `bounded`'s announcement removed) — six caught red. Open: no view was rendered — the panel claim is a source-text claim, and the dev build is the sight check. |
| Sidebar: compact mode, drag & drop, folder to root | **Done 2026-09-04** (`docs/superpowers/plans/2026-09-04-sidebar-polish.md`, three tasks: Task 1 `6be9d8bc`, Task 2 `dc9ce11c` with fix rounds `0ce0ca69`/`fbd31774`/`30b61e70`, Task 3 `f798788e`, fix round `9ca44657`, this row's hash correction `docs(backlog)` on top), re-raising items 3–6 of the dictated notes of 2026-09-02 (`superpowers/specs/2026-09-02-backlog-maintainer-notes.md`). **Task 1** (measurement, no behaviour change): drag & drop and folder-to-root already existed at HEAD `38757773` — three `dropDestination` sites (the sidebar title, a group row, a session row) behind two `draggable` sources, both funnelling through `SessionListViewModel.move(_:before:)` / `move(_:intoGroup:)`; pinned in `SidebarOrderingTests.swift` and `SessionListViewModelTests.swift` (reorder within a group, between two named groups, a group to the root with its placement checked, a cycle refused, a self-drop a no-op, a stale drop named). **Task 2**: `SettingsStore.sidebarCompact` (default `false`), an Appearance toggle, and `SessionSidebar`'s row builder reading it — compact changes vertical padding only (5 → 2); the kind badge stays visible in both densities and the default row is byte-for-byte what it was before the setting existed, both fix rounds this plan's own SDD ledger and BACKLOG's commit history hold down. **Task 3**: the group context menu gained the "Move to…" submenu the session row already had (Task 1's own measurement is what found the asymmetry). Both submenus now read one Core function, `SidebarOrdering.moveTargets(for:currentParentID:in:) -> [UUID?]` (`Sources/macSCPCore/Sessions/SidebarOrdering.swift`) — the top level first (omitted when the item is already there), then every eligible group in sidebar (depth-first, position) order; "eligible" excludes the item's current parent and, for a folder, the folder itself and every descendant (`GroupTree.wouldCycle` asked once per candidate). The session submenu migrated onto it too: its ordering changed from store order to the same depth-first sidebar order the group submenu uses, and it retired the old disabled-with-checkmark row for the current group in favour of omitting it outright — the same refusal, stated once. `SessionSidebar.swift` gained one shared `@MainActor @ViewBuilder` free function, `moveToMenuItems`, that both `SessionRow` and `SidebarGroupRow`'s context menus call; no new catalogue key at Task 3 — the existing `sidebar.moveTo` and `sidebar.noGroup` (already in all four catalogs) are reused for the root entry in both menus. Five new Core tests in `SidebarOrderingTests.swift` (a session excludes only its current group; a session already at the top level is not offered it again; a group excludes itself, its descendants and its current parent; a top-level group is not offered the top level again; the listing is in sidebar order, not store order) plus a new `SidebarMoveToWiringGuardTests.swift` (both submenus call the shared builder — two call sites; the builder is the one place calling `SidebarOrdering.moveTargets`; the root entry passes `nil`; each row's `onMove:` closure calls `move(` with `intoGroup:` and nothing else that moves), sensitivity measured by planting a second effect (`dissolveGroup(`) inside the group row's `onMove:` closure — caught red, reverted. `IconTooltipLintTests`' `decorativeIcons` entry for the retired checkmark icon was removed in the same commit, since the icon it named no longer exists. **Fix round** (`9ca44657`, final whole-plan review): the group submenu's root entry moved off the reused `sidebar.noGroup` key onto its own `sidebar.moveTo.root` = "Top level" (all four catalogs; `sidebar.noGroup` stays — `ConnectionFormView`'s group picker still reads it); a lone top-level folder's "Move to…" now renders nothing instead of an empty submenu (`SidebarGroupRow` gates the `Menu` on `SidebarOrdering.moveTargets(...).isEmpty`, pinned by `SidebarOrderingTests.aLoneTopLevelGroupHasNoMoveTargets`); and `SidebarMoveToWiringGuardTests`'s call-site count moved from file-wide to per-row — `SidebarGroupRow`'s and `SessionRow`'s own bodies are each anchored separately now, closing the gap where two calls sitting inside one row would have passed a file-wide count of two. **Dev build**: right-click a folder shows a "Move to…" submenu (top level plus every other eligible folder) unless it is a lone top-level folder, in which case no submenu appears at all; picking an entry moves it there immediately, the same placement a drag onto that folder would produce; a refusal (a cycle) surfaces through the sidebar's existing error caption, same as a drag. `swift test`: 4505 tests green, zero build warnings. **Update 2026-09-05: folders too** — maintainer report on dev build dev-46da7909 ("the compact sidebar does not reduce the spacing between the folders"): `SidebarGroupRow` now reads `isCompact` too and tightens its own vertical padding (3pt → 1pt), the same density-only rule `SessionRow` already followed; `SessionSidebarCompactRowGuardTests` extended with the same positive/negative pair on the folder row. **Maintainer wishlist 2026-09-16** (Recorded 2026-09-16 from the maintainer's wishlist, item 30): "the sidebar's compact mode is still not compact enough" — reopens this row. What compact changes today is vertical padding only: session rows 5 → 2 (`Sources/MacSCPAppKit/SessionSidebar.swift:1614`), folder rows 3 → 1 (`:1305`), with the protocol badge and the forwarding glyph kept in both densities by ruling (`:1582-1586`, `:1591-1593`). Open: what else should shrink (font size, row content such as the badge, icon size, indentation) is not stated. Not started. **Decided by the maintainer 2026-09-16 (accepting the proposal in chat):** (item 30) single-line rows without a subtitle, and smaller icons. |
| [Diagnostic log, and the home-folder listing that never finishes](superpowers/plans/2026-09-04-diagnostic-log.md) | **The log half done 2026-09-05** (`docs/superpowers/plans/2026-09-04-diagnostic-log.md`, commits `3a788fa9` docs, Task 1 `51f9c49d` + `41831e48` + `1cfbd350` the sink, Task 2 `b1ce3491` + `7a3716e7` the setting and launch wiring, Task 3 this commit the instrumentation). A tester's report against release 1.3.0, relayed by the maintainer: the local pane no longer loads the user's home folder — sometimes five minutes, sometimes never — with every permission granted, nothing knowingly changed. Not reproducible from the code: `git log v1.3.0..HEAD -- Sources/macSCPCore/RemoteFS/LocalFileSystem.swift` is empty, and the owner/group gate (`0b5ca533`, M18a) shipped in 1.2.0. **Task 3** wired the sink into the places the design named: `LocalFileSystem.list` (`info` start/done/failed, `debug` `entry slow` per entry ≥ `slowEntryThreshold` = 500 ms); `RemoteBrowserViewModel.load()` (`info` done/failed, one layer above the FS line — the VM's carries the hidden-file filter and sort, the FS's the raw read — via a `logCategory` the App sets per pane, `"browser.local"`/`"browser.remote"`); `ConnectionViewModel.connect()` (`info` start/auth method/hostkey unknown-accepted-rejected-mismatch/done/failed, `disconnect` in `clearRetainedSecrets()`); `CitadelFileSystem` (`debug` `sftp <op> path=… ms=… ok`/`failed reason=…` through one `measured(_:path:_:)` helper wrapping all 11 `RemoteFileSystem` protocol methods it implements — 11 measured directly, not 12: counted 2026-09-05 by listing the protocol's own required methods, correcting the plan's original estimate); `CitadelShell` (`debug` shell open/close); `TransferEngine.copyFile` (`info` start/done/failed, a new optional `direction` parameter default-`nil` so the CLI and existing tests stay source-compatible); `BrowserPane.swift` (`error`, the App-layer line for the same failure the VM's `info` line already named, fired once per transition via `onChange`, never from inside `body`). 27 `DiagnosticLog.shared.log(` call sites under `Sources/` at HEAD (`grep -rc "DiagnosticLog.shared.log(" Sources`, summed) — `DiagnosticLogSecrecyGuardTests.swift` (new) scans all of them: no interpolation names a password/passphrase/secret/token/privateKey/presigned/fingerprint/hostKey identifier (negative), every category literal — or, for `RemoteBrowserViewModel`'s dynamic `logCategory`, every literal it can structurally hold — is one of the fixed eight, and the call-site count holds a `>= 20` floor (positives). `DiagnosticLogTests.swift` gained three more tests exercising `LocalFileSystem.list`, `TransferEngine.copyFile` and `ConnectionViewModel.connect()` against fakes/mocks — placed in that already-`.serialized` suite rather than in `LocalFileSystemTests`/`TransferEngineTests`/`ConnectionViewModelTests`, since `DiagnosticLog.shared` is a process-wide singleton and those three suites carry no serialization of their own. `MACSCP_ITEST=1 swift test --filter Citadel` green (87 tests) against the Docker rig — the SFTP lines change no behaviour. `swift test`: 4608 tests green (the guard's own two self-tests included), zero build warnings, no gated suite run outside the Citadel one above. **Fix round 1 (the commit above)**, from review: the browser message and the log had carried a mapped `RemoteFSError`'s raw `reason` verbatim since the S3 backend landed — that field is exactly where `S3FileSystem`/`WebDAVFileSystem` embed the endpoint the user typed, which may carry `scheme://KEY:SECRET@host` — measured in this review, on `RemoteBrowserViewModel.message(for:path:)`'s `.protocolError`/`.connectionFailed` arms (Critical) and on six of the seven `reason=` lines Task 3 itself had hand-written (Important: `CitadelFileSystem.mapSFTPError`'s `String(describing:)` on the raw Citadel/NIO error, replaced with `.localizedDescription`). Both arms of `message(for:path:)` now read `DialSupport.reason(for: error)` instead; `DiagnosticLog` gained a `log(_:_:_:reason: any Error)` overload (Structural) that computes that same mapping itself, so a category-spelling regex can no longer be satisfied by an unsafe `reason=\(error)`/`reason=\(message)` — every call site that used to hand-format `reason=` (six, plus `BrowserPane.swift`'s App-layer line, once `RemoteBrowserViewModel` grew a `lastLoadError` property beside `state` for it to read — seven in all) now passes the ORIGINAL error to that overload instead. `DiagnosticLogSecrecyGuardTests` gained a negative (no call's arguments contain the literal `reason=`) beside a positive (≥ 3 call sites use the `reason:` overload — 7 measured). Both new checks were run red first against the pre-fix shape (commit `40445d0d`): the negative failed listing all seven old lines, the positive failed at 0; a new `DiagnosticLogTests` case (a `RemoteBrowserViewModel` fed a planted `.connectionFailed(reason: "Invalid S3 endpoint: https://AKIA:hunter2@example")`) was also run red first, confirming the secret reached both the banner and the log file before the fix. One pre-existing test (`ChecksumRequestTests.anErrorAboutOneFileIsAFailureAndNotAnAbsence`) asserted the old, leaky text and was updated to expect `DialSupport.reason(for:)`'s mapped sentence instead. `swift test`: 4612 tests green, zero warnings; `MACSCP_ITEST=1 swift test --filter Citadel` green again (87 tests) against the Docker rig. Not fixed, flagged instead: `CitadelFileSystem.mapConnectError`/`mapStageAware` (pre-existing, outside the three review items) and `CitadelShell.open`'s thrown `.protocolError(reason: "failed to open shell: \(error)")` still build a `RemoteFSError`'s free text from `String(describing:)`/raw interpolation on an underlying error — safe in practice today (both consumption paths this round fixed already discard `.protocolError`'s reason unconditionally) but not hygienic at the construction site, the same class of gap this round closed elsewhere. **Those five sites done 2026-09-05 (the commit above)**: `mapConnectError`'s two `default:` arms, `mapStageAware`'s two `jump host: …` texts and `CitadelShell.open`'s thrown reason now read `localizedDescription`, never `String(describing:)`/a bare `\(error)` — the connect-time four through one `CitadelFileSystem.connectFailureText(for:)` helper, which carries ONE typed narrowing: a `NIOSSHError` is rendered as its `type` alone (a closed enumeration whose description is the case name, the `diagnostics` string with the server's own text dropped), because `ConnectionViewModel.failedState` recognises a jump host refusing the tunnel by `channelSetupRejected` in exactly this text and `localizedDescription` on a `NIOSSHError` is Foundation's generic sentence (the type adopts neither `LocalizedError` nor a description Foundation reads) — a plain `localizedDescription` there would have switched `core.connect.jumpTunnelRejected` off in silence, with no test to say so. NIO's own `IOError`, the refused-dial shape a person actually reads in the connect form, is unchanged: its `description` IS its `localizedDescription`. Citadel's `SSHClientError` cases other than the auth refusal (`channelCreationFailed`, the three `unsupported…` ones) now read as that generic sentence instead of their case name — accepted, the same trade `mapSFTPError` made. `mapConnectError`/`mapStageAware`/`JumpStageError` became internal for `CitadelConnectErrorMappingTests` (five tests in `CitadelErrorMappingTests.swift`: the two secrecy checks were run red first against the `String(describing:)` spelling, a positive pins that the fixture's description WOULD leak, one pins the jump attribution for a non-auth `SSHClientError`, one pins `NIOSSHError.ErrorType.channelSetupRejected.description == "channelSetupRejected"` — the rendering the narrowing relies on, since `NIOSSHError` has no public initializer and the mapper's own arm can only be read, not driven). `CitadelShell.open`'s change has no unit test: the shell cannot be opened without a channel, and the fixture-less one-token change is read, not measured. **Open, unstarted**: the hang itself — hypothesis 1 (a metadata call waiting on a privacy prompt that never appears: owner or group column visible → `attributesOfItem` on Desktop/Documents/Downloads) vs. hypothesis 2 (a dead mount or cloud placeholder waiting on a network timeout) vs. hypothesis 3 (something the log has yet to show). Its own draft plan: [2026-09-04-local-listing-never-blocks-design.md](superpowers/specs/2026-09-04-local-listing-never-blocks-design.md). What the dev build should show once this ships: Settings › General → Diagnostic log = Debug, open the local pane's home folder, then Show in Finder → `~/Library/Logs/macSCP/macSCP-<date>.log` carries the `browser.local list start`/`list done` pair for that path, and an `entry slow` line by name for any entry whose metadata call took ≥ 500 ms — the tester's own report, reproduced or ruled out by what that file says. Three questions to relay to the tester meanwhile: owner or group column visible? only the home folder, or its subfolders too? any network share or cloud folder in the home folder? **The hang itself: done 2026-09-05** (`docs/superpowers/specs/2026-09-04-local-listing-never-blocks-design.md`, commits `9d8354cc` Task 1 — the directory read returns names and kinds alone, no per-entry `stat` in `LocalFileSystem.list`'s own loop; `3f9e3cf8` Task 2 — `LocalMetadataSource.metadata(for:)`, one child task per entry, yielding each filled-in item as its own call returns and abandoning (never awaiting) a child whose call never returns; and this commit, Task 3 — `RemoteBrowserViewModel.load()` merges that stream into `displayedAll` by path as it arrives, re-sorting only when `sortKey` is one the stream feeds). What the user sees now: the local pane's folder appears in the time the directory read itself takes, every row's size/modified/owner/group filling in as each entry's own metadata call returns, independently — a row whose entry is genuinely stuck (a dead mount, an unanswered privacy prompt) shows `-` forever instead of blocking the rest of the listing, and the diagnostic log names it — though at this commit only an entry that eventually RETURNED got a line (`entry slow`, written after the probe came back); a permanently stuck entry, the exact case this design exists to name, got none at all until the final fix round below added a supervisor for it. Not verified, carried over from Task 1's own review: the bulk directory read's behaviour for a dead-mount child is unverified (Foundation may fall back to a per-key syscall inside the bulk read), so a dead mount could in principle still cost something inside phase one itself rather than only in phase two. `swift test`: 4626 tests green (7 new: 5 merge tests in `RemoteBrowserViewModelMetadataMergeTests.swift`, a positive/negative wiring-guard pair in `LocalListingMergeWiringGuardTests.swift`), zero warnings. **Final fix round (the commit above)**, from the final whole-plan review (`3a788fa9..1f1f3f3d`, opus): Critical — `ConnectionViewModel.connectLogFields` interpolated `s3.endpoint`/`webdav.baseURL` verbatim into the `connect start host=…` line, so a stored S3/WebDAV session whose endpoint carried `scheme://KEY:SECRET@host` wrote that secret straight to disk; both fields now route through `URLText.withoutUserinfo` (the same helper `SessionOverviewModel`'s summary rows already use for them), confirmed red first against the pre-fix code with a planted `AKIAEXAMPLE:hunter2@` endpoint driven end to end through `ConnectionViewModel.connect()`. Important — `DiagnosticLog.writeRun` read the open file handle under `state`'s lock and wrote to it OUTSIDE that lock, so a concurrent `configure(.off)` closing the same handle could raise an uncaught `NSFileHandleOperationException`, aborting the process — confirmed against the exact API by a standalone probe (`FileHandle.write(_:)` on a closed handle: uncaught exception, exit 134; `write(contentsOf:)` on the same handle: a catchable Swift error) — fixed by moving the write inside the same locked block and switching to the throwing `write(contentsOf:)` with `try?`. Important — `DiagnosticLogTests`' `.serialized` trait only serializes within that one suite; `ConnectionViewModelTests`/`LocalFileSystemTests`/`TransferEngineTests` run in parallel and their own production code logs into the same process-wide `DiagnosticLog.shared` while this suite holds `.info`/`.debug`, flaking six `lines.count`/`.first`-based assertions (`linesPreserveCallOrder`, both `flushSynchronously` tests, the midnight-batch test, both line-format regex tests) — each now filters the file by its own category and a per-test UUID marker before asserting. Important — three writer-gated tests called `#require` before their own `gate.signal()`, so a failed `#require` could park the writer forever and hang every later `flush()` in the process; each gained a `defer { gate.signal() }` right after installing the gate (`AsyncSignal.signal()` is idempotent). Minor — the design's "Not logged" paragraph is amended, not the code: a host-key mismatch's two fingerprints are public values of public keys and reach the log only through the audited `DialSupport.reason(for:)` mapper; the design's SFTP operation count corrected 12 → 11 (Task 3's own measured number). Minor — `connectionFailedReasonNeverReachesMessageOrLog`'s vacuous `else { message = "" }` gained a positive `#expect` that the state IS `.failed`, beside the existing negative. Not fixed, named instead: the same filtered command (`swift test --filter "DiagnosticLog|ConnectionViewModel|LocalFileSystem"`, run 20 times) still shows an unrelated, pre-existing flake unchanged by this round — a `fileContents(url)` read returning `""` right after `flush()` reports the line written, reproducing on the pre-fix HEAD too and not reproducing at all under a full `swift test` — see `.superpowers/sdd/2026-09-04-diagnostic-log/final-fix-report.md`. `swift test`: 4614 tests green (two new), zero warnings on a clean `swift build --build-tests`. **Final fix round 2** (the flake the round above could not explain): traced to `DiagnosticLog.shared.currentFileURL` reading the LIVE process-wide `directory`/`fileDayKey` at call time, which any suite's concurrent `configure(...)` on that same singleton could overwrite between one test's `await flush()` and its follow-up read — `flush()` itself was never the bug. Fixed by giving `DiagnosticLog` an `internal init()` and moving every diagnostic-log test that does not need the live singleton onto its own private instance (`DiagnosticLogTests.swift`, `.serialized` dropped — nothing left to race), while the five tests whose production code calls `DiagnosticLog.shared` directly (`LocalFileSystem`, `TransferEngine`, `ConnectionViewModel` x2, `RemoteBrowserViewModel`) moved to a new `.serialized` suite, `DiagnosticLogSharedSinkTests.swift`, where each computes the file its own lines went to (a fixed `now` plus the sink's own `yyyy-MM-dd` naming) rather than asking the singleton. A new guard, `DiagnosticLogSharedSinkIsolationGuardTests.swift`, holds the split in place (no other test file may mention `DiagnosticLog.shared` as code); its own raw-string-aware source stripper is a local copy adapted from `TabContextMenuWiringGuardTests`, not a change to the shared `SwiftSource` module, which deliberately still fails closed on a raw string. `swift test --filter "DiagnosticLog|ConnectionViewModel|LocalFileSystem"` run 20 times straight, all green (previously 4/20 failed on the empty-read flake); full `swift test`: 4615 tests green (one new), zero warnings. **Final fix round (the commit above)**, from the final whole-plan review: Important — a permanently stuck entry never got its `entry slow` line, because the per-entry timing ran only AFTER `metadataProbe` returned; `LocalMetadataSource.metadata(for:)` now also runs a supervisor `Task` per call that sleeps `slowEntryThreshold` once and writes a `browser.local entry slow name=… ms=… (still pending)` line for every item its tally has not yet accounted for, kept textually distinct from the on-return line. Important — `RemoteBrowserViewModel`'s merge loop looked up each arrival's row with `firstIndex(where:)`, O(n) per arrival (roughly 12.5 million compares for one 5000-entry merge); replaced with a `[String: Int]` path→index map built once per merge and rebuilt inside `publishMergedMetadata` whenever its own resort reorders `displayedAll`. Important — `LocalListingMergeWiringGuardTests`'s negative scanned `list`'s own body for a `resourceValues(` call, but Task 1 had already moved the bulk-prefetch call into `listNamesAndKinds`, so the span could no longer hold the violation; the guard now also scans `listNamesAndKinds` and pins its key set to exactly `[.isDirectoryKey, .isSymbolicLinkKey, .nameKey]` — mutation-probed by hand (`.fileSizeKey` planted, red observed, reverted). Ruled in — a session-scoped `StuckPaths` (`Mutex<Set<String>>`), shared by both `LocalFileSystem` instances one browser session builds: the supervisor above marks a still-pending path stuck, and a later `metadata(for:)` call skips probing it entirely (logging `entry skipped name=…` once instead), so the tester's own home folder — exactly the folder one keeps returning to — does not spend a fresh thread rediscovering the same stuck entry on every visit. Minors closed in the same round: `selectedItems` is re-derived by path after every merge publish, so Get Info reads the filled row; `.permissions` dropped from `metadataOrderedSortKeys` (phase two never fills it); a `.other` row resolving to `.directory` now forces a resort under every sort key, not only the ones phase two feeds, since directories-first grouping applies regardless of key; `refreshQuietly()` gained a doc-comment paragraph on the risk of wiping phase-two metadata if it were ever wired to the local pane; the German fixture names `eins.txt`/`zwei.txt`/`drei.txt` renamed to `one.txt`/`two.txt`/`three.txt`. `swift test --filter "LocalFileSystem|RemoteBrowserViewModel|LocalListing|DiagnosticLog"`: 196 tests green; full `swift test`: 4639 tests green, zero warnings on a clean `swift build --build-tests`. **Round 2 (the commit above)**, from a follow-up review of the round above: Important — the supervisor snapshotted still-pending items and then marked each into `StuckPaths` with no re-check in between, so a child that finished in that gap was blacklisted for the whole session anyway; fixed with two separate deadlines — `slowEntryThreshold` (500 ms, unchanged) now only ever writes the `entry slow … (still pending)` log line, and a new `stuckEntryDeadline` (5 s) is the sole trigger for a mark, read and applied in ONE `MetadataTally` actor turn (`markStillPending`) so a child's own `childFinished` cannot land in the gap. A marked path that eventually answers now clears itself (`StuckPaths.clear`), so a one-off six-second cloud file is probed again on the next listing rather than staying blacklisted forever. Both deadlines are now `MetadataDeadlines`, an injectable value on `LocalFileSystem.init` (default 500 ms / 5 s), so tests drive the whole sequence in milliseconds. Tests run red first: the existing supervisor test gained an assertion that the path is NOT in `StuckPaths` right after the 500 ms line appears (red against the pre-round-2 code, which marked unconditionally at that point), and the self-healing clear test was run red with the clear call removed by hand (a 60 s `.timeLimit` failure), then green with it restored. `swift test --filter "LocalFileSystem|RemoteBrowserViewModel|LocalListing|DiagnosticLog"`: 199 tests green; full `swift test`: 4642 tests green, zero warnings. |
| [What's new: a changelog dialog after an update, and in Settings](superpowers/plans/2026-09-04-whats-new.md) | **Done 2026-09-04** (Task 1 `88229bfb` + fix `5eb201f9` — `ChangelogParser`, total by construction, parses `CHANGELOG.md` into releases/sections/entries and exposes a newest-first `releases(newerThan:in:)` over a dotted, component-wise version comparison; Task 2 `487a0712` + fix round `232661e1` — `ChangelogResource.load()` reads the file the packaging script now copies into `Contents/Resources/`, `WhatsNewModel.releasesToShow(current:lastSeen:in:)` is the pure decision, `WhatsNewSheet`/`WhatsNewList` render it, `SettingsStore.lastSeenVersion` remembers what was shown; Task 3 `b18ea209` + fix round `e6dbc83e` — a "What's new" entry in the Settings sidebar, after "Shortcuts"; final fix round (the commit above this row's last edit) — the parser tests select the pinned real-file release by version instead of `releases.first` (a release keeps that release's numbers off the release commit's own regeneration), a comparator-derived non-numeric `lastSeen` replaced one that passed with or without its own guard, the Settings-pane guard's negative now names the extracted `loadReleases()` load, and the sheet's hardcoded-`Text` scan widened to `Text(verbatim:` and to the Settings pane). **Decision rule**: the launch sheet shows only when a last-seen version is on record and the running version is newer than it, listing the changelog releases newer than last-seen and no newer than the running version (a fresh install, an unchanged version and a downgrade all show nothing), and the version is recorded the moment the decision is made, not when the user dismisses the sheet. **Non-numeric version rule**: a dev build's `current` never shows the sheet (though it is still recorded as `lastSeenVersion`), and a non-numeric `lastSeen` is treated as a fresh install, both decided explicitly rather than by the accident of how a `"dev-<hash>"` string happens to compare lexicographically against a numeric one. **Dev build**: Settings → "What's new" lists every release the bundled changelog carries, newest first, with the same "No changelog shipped with this build." message the launch sheet's empty state uses when none is bundled — always reachable, unlike the launch sheet, which needs a real version bump to fire. **Tests**: 4552 green (full `swift test` run, final fix round). **v2**: the launch sheet's `showWhatsNew` is App-scope `@State` attached inside the single `WindowGroup`, so a second window would present the same sheet again rather than each window deciding independently — fine while the app is single-window, worth a per-window home if that changes. |
| File browser: a bucket icon, and a Type column | **Maintainer feedback 2026-09-04**: in the S3 bucket-list mode a bucket row shows a bucket symbol beside its name so it reads as a bucket, not a folder; and a new "Type" column (pdf, png, … from the extension; folder / bucket / symlink for the others) with sorting, in every backend. Open: `RemoteBrowserViewModel`'s columns and sort keys, the column's persistence beside the existing ones, the catalogs. Last of the four feedback items. **Done 2026-09-04** (`docs/superpowers/plans/2026-09-04-browser-type-column.md`, two tasks: Task 1 `4e6fd242`, Task 2 this commit). **Task 1** (Core): `RemoteFileItem.isBucket: Bool` (default `false`, the init's last parameter), set `true` at the one bucket-row construction site (`S3ListParser.parseBuckets`, inside `S3FileSystem.listBuckets()`); `FileTypeLabel.label(for:)`/`.sortKey(for:)` (`Sources/macSCPCore/RemoteFS/FileTypeLabel.swift`), precedence `isBucket` → `.directory` → `.symlink` → last extension uppercased → "File", through four new `core.fileType.*` catalog keys; `RemoteBrowserViewModel`'s existing `SortKey.type` (found already shipped from M11m, ranking by raw `kind` — a coarser, differently-tested ordering) re-based onto the new label. **Task 2** (App): the ALSO-already-shipped Type column's cell (`RemoteFileTableView.typeText(for: item.kind)`, kind-only text) now reads `FileTypeLabel.label(for: item)` — `typeText(for:)` deleted along with the four `filetable.type.*` keys it alone used (checked with `grep` first: no other reader in any of the four catalogs). The row's kind marker (previously fixed at build time, symlink-arrow only) now varies per row: `item.isBucket` shows `archivebox` (no existing bucket SF Symbol was found under `Sources/MacSCPAppKit` by grep), a symlink still shows `arrow.up.forward`, anything else shows nothing; both the marker's accessibility description and the name cell's hover tooltip read a new `filetable.bucketTooltip` = "Bucket" key (all four App catalogs). `Tests/macSCPAppKitTests/RemoteFileTableTypeColumnGuardTests.swift` (new, 6 source-scanning tests: the Type cell reads `FileTypeLabel.label(for:` beside `typeText(` no longer existing in the file at all; the icon branch reads `item.isBucket`; `archivebox` appears exactly once; every `FileColumn.allCases` case has a `filetable.column.<rawValue>` key, derived rather than hand-typed; the new tooltip key is declared) plus two behavioural additions to existing suites (`ChecksumColumnTests.aSymlinkOrABucketNameCellCarriesATooltipAPlainFileDoesNot`; `FileTypeLabelTests` gained the two edge cases Task 1's review flagged — a trailing-dot name and a directory named like it carries an extension, both Core). **Sight check** (to run, not performed — this plan's own Global Constraint is "Do NOT launch the GUI", so nothing here was launched): open an S3 session's bucket-list root — every row should show the archivebox glyph beside its name and "Bucket" in the Type column; in any listing, the Type column should show PDF/PNG/Folder/Link/… and sort by it. `swift test`: 4560 tests in 386 suites green (one pre-existing, unrelated skip), zero build warnings. **Final fix round** (the commit above this row's last edit, final review of Task 2): `FileColumn.type.defaultVisible` flipped `false` → `true` — the plan's own Global Constraint says the column "defaults to visible," so the plan governs over M11m's opt-in-by-default rule for a new column, a deviation disclosed here — a fresh install, and any existing install that has never itself persisted a `visibleColumns` set, now shows the Type column by default; an install that HAS persisted one (even for an unrelated column) keeps exactly what it stored, `.type` included only once the user opts it in. Also: a behavioural cell-reuse test for the bucket/symlink marker (`RemoteFileTableTypeColumnGuardTests.theMarkerVariesPerRowBucketThenSymlinkThenPlainFile`), added because the prior scan-only guard for the icon branch stayed green even with that branch deleted (satisfied by the tooltip's own `isBucket` read instead); `FileTypeLabelTests` and the `.type` sort test now compare against `CoreL10n.string(...)` instead of English literals, since the sort test's hardcoded order broke under the Polish catalog ("PDF" sorts before "Plik", the opposite of English's "File" before "PDF"); a `ChecksumColumnTests` structural check for the `.type` cell; two doc corrections to the plan itself (`RemoteFileKind`'s case count, the kind-icon measurement); and the two marker `NSImage`s cached instead of rebuilt on every row/reuse. |
| A speed test in diagnostics (maintainer wishlist) | **Not started.** Recorded 2026-09-16 from the maintainer's wishlist (item 5): "speedtest diagnostics tool". Exists today: the diagnostics panel and `macscp-cli diagnose` run resolve, TCP ping, ICMP echo, the backend's dial, a network trace and per-protocol contributions (`DiagnosticScope`, `Sources/macSCPCore/Diagnostics/ConnectionDiagnostics.swift:15-30`); none measures throughput. Transfers show a live rate (`Sources/MacSCPAppKit/TransferQueueBar.swift:235`). **Ambiguous:** throughput to this server over the session's own protocol (a test payload up and down) or a general internet speed test. Constraints: a payload on the user's server has to be cleaned up, cancel included; the configured bandwidth limits (`Sources/macSCPCore/RemoteFS/BandwidthLimiter.swift`) cap what would be measured; its tests fall under CLAUDE.md "A wall-clock ceiling in a test measures the runner". Not started. **Maintainer answers 2026-09-16 (same day, in chat):** both — one test of throughput to the server over the session's own protocol, and one general internet speed test, as two separate tests. The ambiguity above is resolved; which service or method measures the general internet speed is open. |
| A widget (maintainer wishlist) | **Not started.** Recorded 2026-09-16 from the maintainer's wishlist (item 6): "widget". Exists today: none (no `WidgetKit` in `Sources/`). **Ambiguous:** what the widget shows or does is not stated (candidates: sessions to open, forwarding states, transfer progress). Constraints: a WidgetKit widget ships as a sandboxed app extension inside the bundle and shares data with the app through an App Group; the app today is a SwiftPM build whose bundle `scripts/package-app` assembles by hand, with no extension target and no entitlements at all (`scripts/release:48`) — an extension is a second build product, the same "second build system" question the XCUITest decision struck for now (Security and testability, "How far can the UI be tested?"). A widget reads no secrets. Not started. **Maintainer answers 2026-09-16 (same day, in chat):** several widgets for several cases — active transfers (downloads), recent servers, snippets, and similar. The "what does it show" ambiguity is resolved to that list; the extension-target constraint above stands. |
| SSH compression as a setting and a session flag (maintainer wishlist) | **Not started.** Recorded 2026-09-16 from the maintainer's wishlist (item 9): "SSH can use gzip compression on any connection (Settings, session flag)". Exists today: none, and the transport cannot negotiate any — the `NoiXdev/swift-nio-ssh` fork at 0.3.10 offers only `"none"` in both directions (`Sources/NIOSSH/Key Exchange/SSHKeyExchangeStateMachine.swift:114-115` in the checkout). SSH's compression methods are `zlib` and `zlib@openssh.com`; the wish's "gzip" is read as those — a reading, recorded. Constraints: a fork change under CLAUDE.md "Forks are a debt with a review date" (upstream check first, red first, the fork's suite green) and an upstream PR candidate; which compression implementation fits (the SDK's zlib, Apple's `Compression` framework) is unmeasured. Open: a global default with a per-session override; whether forwardings and the CLI follow it. Not started. |
| Offer to resolve an entered host name to its IP (maintainer wishlist) | **Not started.** Recorded 2026-09-16 from the maintainer's wishlist (item 11): when a host is entered as a domain, offer to resolve it to an IP and take that over — as an action. Exists today: no such action in the connection form; a resolver exists inside diagnostics, module-internal (`HostResolver`: `getaddrinfo` at `Sources/macSCPCore/Diagnostics/HostResolver.swift:122`, numeric form at `:136`). Open questions: a name with several addresses (IPv4 and IPv6, round robin) — which one, or a choice; consequences a design has to state: known hosts are looked up by host and port (`knownHosts.find(host:port:)`, `Sources/macSCPCore/SSH/HostKeyValidation.swift:127`), so swapping a name for an IP means a new TOFU consent for the IP; a TLS endpoint (S3, WebDAV over https) validates its certificate against the name, which an IP would not match; whether the name is kept anywhere after the swap. Not started. |
| Diagnostics: reverse DNS and host name check (maintainer wishlist) | **Partly there.** Recorded 2026-09-16 from the maintainer's wishlist (item 12): "diagnose DNS reverse hostname check". Exists today: the resolve step does forward resolution only (`getaddrinfo`, `Sources/macSCPCore/Diagnostics/HostResolver.swift:122`) and renders addresses numerically (`getnameinfo` with `NI_NUMERICHOST`, `:136`; likewise `ICMPEcho.swift:481` and `NetworkTrace.swift:721`) — no PTR lookup anywhere. **Reading, recorded as a reading:** a PTR lookup per resolved address and a check that the returned name resolves back to that address. Open: whether trace hops get names too; a missing or non-matching PTR is common and no fault by itself, so the row reports rather than judges (the S3 access probe's precedent, item 18 of `superpowers/specs/2026-09-02-backlog-maintainer-notes.md`); the lookup's time counts against the step's budget. Not started. |
| ProxyCommand (maintainer wishlist) | **Partly there.** Recorded 2026-09-16 from the maintainer's wishlist (item 13): "ProxyCommand (for firewalls etc.)". Exists today: ProxyJump through exactly one hop (`SSHConnectionConfig`, `Sources/macSCPCore/SSH/SSHConnectionConfig.swift:17`; the form toggle, `ConnectionFormView.swift:857`; `-J` in the external-terminal command, `SSHCommandBuilder.swift:52`); no arbitrary ProxyCommand, and the ssh-config importer reads only `HostName`, `User`, `Port` and `IdentityFile` (`Sources/macSCPCore/Sessions/SSHConfigParser.swift:59-65`). Constraints: a ProxyCommand is a local shell command run on every connect, so a session file, an import or `macscp-cli sessions add` could carry command execution — the reason the jump host and jump user are held to strict whitelists today, since `ssh -J` becomes an implicit `ProxyCommand` (`SSHConnectionConfig.swift:50-110`); the connection would run over a subprocess's stdin/stdout instead of a socket, which Citadel's client path may not accept as-is (unmeasured). Open: whether something narrower covers the need (a SOCKS/HTTP proxy setting, or a multi-hop jump chain). Not started. |
| X11 forwarding, or GUI app support (maintainer wishlist) | **Not started.** Recorded 2026-09-16 from the maintainer's wishlist (item 14): X11 forwarding, "maybe bundle/link [an X server]" or support for GUI apps (the middle part reads a typo, recorded as a reading). Exists today: nothing, and the transport cannot open the channel — swift-nio-ssh's own channel-type documentation says X11 channels are "not supported by swift-nio-ssh" (`Sources/NIOSSH/Child Channels/SSHChannelType.swift:23`, fork 0.3.10), so it is a fork change under CLAUDE.md's fork rules. Constraints: macOS ships no X server — XQuartz is a separate install, and bundling one is a licence and size decision; `xauth` cookie handling; X11 forwarding lets the remote side open windows on, and read input from, the user's display (why `ssh` separates `-X` from `-Y`). The external-terminal hand-off (`SSHCommandBuilder`) runs the system `ssh`, which has `-X`; whether that route suffices is open. Not started. **Decided by the maintainer 2026-09-16 (accepting the proposal in chat):** forwarding to an installed XQuartz only; no bundled X server. |
| Custom actions in the file context menu (maintainer wishlist) | **Not started.** Recorded 2026-09-16 from the maintainer's wishlist (item 15): custom actions in the context menu for files, folders and mass selections — like unzip, zip, gzip — some elements by default plus a custom builder. Exists today: built-in entries and backend-contributed ones (`FileActionContribution`, `Sources/macSCPCore/Capabilities/BackendContributions.swift:5`, e.g. S3's "Share Link…"); no archive action, no user-defined action. Constraints: a remote action is a command on the server, and Core deliberately has no generic `exec(String)` — the checksum work built a fixed command line instead (Interface, "Checksums for files"); a builder is user-authored remote command execution with file names substituted in, the quoting problem the snippet variables and dry run already handle (`SnippetCommandSurvey`; Interface, "Snippet dry run"); only backends with a shell can run it (`ProtocolCapabilities.supportsShell`, `Sources/macSCPCore/Capabilities/ProtocolCapabilities.swift:12`; S3 and WebDAV have none); a mass action must never include a synthetic `..` row (item 20 of the 2026-09-02 notes). Open: remote side, local side, or both; which defaults ship. Not started. |
| Serial ports in the terminal (maintainer wishlist) | **Not started.** Recorded 2026-09-16 from the maintainer's wishlist (item 16): serial support in the terminal — local COM ports. Exists today: nothing; every terminal opens an SSH shell through `RemoteShellProvider` (`Sources/macSCPCore/RemoteFS/RemoteShell.swift:26-27`), and a tab is built around a connection to a server. Constraints: a local serial device (`/dev/cu.*`) has no file system to browse, so it does not fit `BackendDescriptor` as another backend without deciding what the tab shows; the app is not sandboxed (`scripts/release:48`), so device access needs no entitlement today but would under a sandbox; USB-serial adapters may need vendor drivers outside the app; no serial library is in the dependency graph. Open: baud rate, parity and flow control per session; whether a serial session lives in the same sidebar. Not started. |
| Send a file from macOS to macSCP to upload it (maintainer wishlist) | **Not started.** Recorded 2026-09-16 from the maintainer's wishlist (item 20): a "Send to / Open with macSCP" entry in the macOS context menu, so a file can be uploaded straight away. Exists today: nothing — the bundle's `Info.plist` (`scripts/package-app:68`) exports types only; no `CFBundleDocumentTypes`, no `NSServices`. **Ambiguous:** which macOS mechanism is meant — the Share menu (a Share extension: a second target and bundle, sandboxed), Finder's "Open With" (a document-type declaration in `Info.plist`), or a Services entry. Open questions: where the file lands — which session, which tab, which remote directory; what happens with no connected tab (connecting, with any TOFU consent given in the app, never silently). Not started. **Decided by the maintainer 2026-09-16 (accepting the proposal in chat):** Finder "Open With" macSCP, followed by a choice of session and destination folder. |
| S3: the tools of s3Manager (maintainer wishlist) | **Partly there.** Recorded 2026-09-16 from the maintainer's wishlist (item 21): extend S3 with the tools of `NoiXdev/s3Manager` (https://github.com/NoiXdev/s3Manager). Read 2026-09-16 with `gh repo view` and its README, read-only: a separate desktop app (Electron, React, TypeScript; MIT; public; last updated 2026-07-23) whose README lists multiple accounts and providers (Amazon S3, Hetzner, custom endpoints); browsing with breadcrumbs; upload, download, delete, copy a presigned URL, move, rename, create folders; presigned upload links (time-limited PUT URLs); a metadata editor (`Content-Type`, `Cache-Control`, `Content-Disposition`, custom `x-amz-meta-*`); a CORS editor (bucket CORS as JSON); Object Lock (bucket default retention, per-object retention and legal hold); visibility and ACLs (public/private toggle, per-grantee ACLs); create bucket (with Object Lock and versioning options); sync (bucket ↔ bucket and local ↔ bucket, with a global status indicator); a scan-free dashboard of accounts and buckets. Already in macSCP: S3 sessions and login sets, Hetzner presets (Bugs, "Dev-build UI follow-ups 2"); browsing, including a bucket-list mode (B-3); upload, download, delete; create folder (`S3FileSystem.createDirectory`, `Sources/macSCPCore/S3/S3FileSystem.swift:521`); rename within one bucket (cross-bucket refused, `:552`); presigned GET and PUT links (`PresignedMethod`, `Sources/macSCPCore/RemoteFS/PresignedURLProvider.swift:5-8`); an access-level probe. Missing (grep for `cors`, `acl`, `ObjectLock`/`retention`, `x-amz-meta`, `CreateBucket`, `versioning` in `Sources/`, 2026-09-16 — no feature code): metadata editor, CORS editor, Object Lock, visibility/ACLs, create bucket, sync, dashboard, cross-bucket move. **Ambiguous:** re-implementing these tools in macSCP (s3Manager's code is TypeScript and does not carry over) or integrating with the separate app; and which of the listed tools are wanted. Constraints: ACL and Object Lock behaviour differs between providers (the rig's MinIO already disagrees with AWS about `ListBuckets`, item 18 of the 2026-09-02 notes); retention and legal hold are hard to undo by design and need a confirmation; sync overlaps rsync (item 22 of the 2026-09-02 notes). Not started. **Maintainer answers 2026-09-16 (same day, in chat):** rebuild the tools inside macSCP, properly — not an integration of the separate app. The "rebuild or integrate" ambiguity above is resolved; which tools come first is still open. **Decided by the maintainer 2026-09-16 (accepting the proposal in chat):** order — create bucket, metadata editor, visibility/ACLs first; then CORS, Object Lock and sync. |
| A diff view for files and folders (maintainer wishlist) | **Not started.** Recorded 2026-09-16 from the maintainer's wishlist (item 31): a kind of diff view to compare two files, or whole folders. Exists today: no diff in `Sources/`; the checksum column and action (Interface, "Checksums for files") can already tell two files apart without showing how. **Ambiguous:** which pairs — local against remote, two remote files in one session, or across sessions and backends — and whether a folder comparison means names/sizes/dates, content hashes, or a recursive content diff. Constraints: comparing remote content means downloading it to a temporary place (the question the Space-preview wish also raises, item 12 of the 2026-09-02 notes); a remote folder compare by hash costs one checksum per file; binary files. Open: handing the pair to an external diff tool (through the "Open with" rules) instead of a built-in view. Not started. **Maintainer answers 2026-09-16 (same day, in chat):** a folder comparison means both — the counts (files only on one side, files that differ) AND a content diff of the files inside, like `git diff`. Which pairs (local↔remote, remote↔remote, across sessions) is still open. **Decided by the maintainer 2026-09-16 (accepting the proposal in chat):** first local↔remote and two files of the same session; across sessions later. |
| Diagnostics ignore the jump host | **Not started.** Recorded 2026-09-18 from the maintainer's report: diagnostics does not handle jump hosts yet; every step must resolve through the jump, and the jump itself must be checked first — ping, trace and so on against the jump, then from the jump to the target. Exists today: the steps (`DiagnosticStepID`, `Sources/macSCPCore/Diagnostics/DiagnosticStep.swift:86-91`) run resolve, tcp, icmp, dial, trace (`Sources/macSCPCore/Diagnostics/ConnectionDiagnostics.swift:283-400`) straight against the target, from the Mac; the jump is ignored completely. `SSHFieldSchema.makeConfig` deliberately skips `.jump` (`Sources/macSCPCore/SSH/SSHFieldSchema.swift:234-235`), so the dial step connects to the target directly with `hostKey: .refusing`, and `SSHFieldSchema.values(from:)` (`:319-327`) carries no jump fields for a stored session either; for a tab the form's jump fields (`Sources/MacSCPAppKit/ContentView+Diagnostics.swift:42-75`) are dropped the same way. A target reachable only through a bastion therefore reads as "does not resolve" or "refused", and the jump itself is never examined. Not started. |

## Tooling and maintenance

| Entry | Core |
|---|---|
| [CLI: completion, help, host list](superpowers/specs/2026-08-20-backlog-cli-completion-hosts.md) | **Item 1 done 2026-09-02.** `macscp-cli sessions` lists sessions via `SessionCatalog`, filterable by `--group`/`--kind`/`--name`/`--tag` (`--name` is a case-insensitive substring, not a pattern), `--json` included; a source-scanning guard pins that the command touches no secret or connection API. **Open: item 2** (autocompletion — session names only vs. remote paths is still an open design question) **and item 3** (a root-command `discussion` explaining `name:/path`). **Items 2 and 3 done 2026-09-02:** generated zsh/bash/fish scripts plus session-name completion for every `name:/path` target from the store (Core completer, CLI wrapper; secrets untouched, nothing dialled), and the root help explains `name:/path`. Left open: `--group`/`--tag` value completion. — **`--group`/`--tag` value completion done 2026-09-05:** same completer family, split into its own pair of files (`GroupTagCompleter` in Core, `GroupTagCompletion` the CLI wrapper) rather than folded into the session-name completer, because it completes an OPTION's value against a different slice of the catalog — two new `SessionCatalog` accessors, `groupNames`/`tagNames`, deduplicated and sorted, deliberately not derived from `Row.groupPath` (splitting that `" / "`-joined ancestry string back apart would misread a group whose own name contains that separator). Same constraints as session-name completion: no secret, no keychain, no connection, silent (`CLISessionsCommandGuardTests`' forbidden-symbol scan now covers both new files, with its own exists/positive-anchor/negative-scan tests per file, same shape as the existing pair); a new binary-level test confirms the generated zsh script wires `.custom` completion (not the generator's static fallback) onto the verbs that declare `--group` and `--tag` (`sessions list` and the `add`/`edit` field options since 2026-09-06 — the test reads the verbs and their options off the binary and compares the two sets, no longer a literal command path). Full `swift test`: 4938 tests, 419 suites, all green. — **Fix round 1, 2026-09-06:** `completeGroups`/`completeTags` now filter out any name containing whitespace or a newline — bash's generated script hands a `.custom` completion's answers to unquoted `compgen -W`, which word-splits on whitespace, so a group like "Work / Prod" or a tag like "needs review" would otherwise complete as two or more words (zsh is unaffected: its completion function receives the list as an array). Session-name completion (`SessionNameCompleter.complete(prefix:in:)`, 2026-09-02) carries the identical limit and was NOT revisited in this pass — noted here, not fixed, since it is a pre-existing gap outside this row's own change. Pinned: a name with whitespace or a newline is absent from completion, one without either is present beside it. — **Shell completion in Settings, 2026-09-10** (`52627742`, `eb0c387f`; design `superpowers/specs/2026-09-08-shell-completion-settings-design.md`): item 2's shipping-and-setup question is answered — the app documents the command rather than an install script generating one, and the line always asks the installed tool for its script at shell start (`source <(macscp-cli --generate-completion-script zsh)`, bash the same form, fish `… | source`), so an update never leaves a stale script behind. Settings → Command-Line Tool → Shell Completion: a picker over the three shells, the line for the selected one in monospace, a Copy Command button, the file to paste it into, and the zsh `compinit` clause, all following the install state (the bundled tool's quoted path before the shortcut is installed, `macscp-cli` after, no line at all while the app runs translocated). Still open: remote-path completion after `name:` (session names only, as decided above) and `--session` completion for the app-side verbs, both deferred to the CLI plan. |
| [Dependencies](superpowers/specs/2026-08-20-backlog-dependencies.md) | **Rewritten 2026-09-02.** Two forks under NoiXdev, both `exact:` overrides by package identity: `swift-nio-ssh` (0.3.9 — pre-line fix, the upstream security patches, `hostKeyAlgorithmNames`) and `Citadel` (`0.12.1-noix.2` — RSA-SHA2 signing, ECDSA file parsing). Each carries a record with the measurement behind every change; the rule for checking and retiring them is in CLAUDE.md. Also new: `_CryptoExtras` (swift-crypto) linked directly for RSA host-key verification; SwiftTerm still hangs off a bare revision. |
| [Import planner](superpowers/specs/2026-08-19-backlog-import-planner.md) | **Done 2026-09-02.** A bag the backend's own schema cannot dial (half bag, jump-only twin) is rejected and counted, judged over the schema's defaults with a blank value read as absent, so a port-less entry still imports and an S3 export with an empty region still imports for repair. Left as is: the pre-M23 column-less S3/WebDAV entry, a documented store exception. |
| [Polish: terminal resize, transfer cancel and paths](superpowers/plans/2026-09-03-polish-terminal-and-transfers.md) | **Open, from the closeout of the 2026-09-03 polish plan.** `Tests/macSCPAppKitTests` now carries four separate comment/string strippers; `SwiftSourceStripping.swift` (added for the cancel guard) is the convergence point, unconverged — and it is now the one of the two `SwiftSource` types that carries `blankingCommentsAndStrings`/`blankingComments`, the other being `Tests/macSCPCoreTests/SwiftSourceStripping.swift` (`stripCommentsAndStrings`, older); a convergence has to name which. **Recounted 2026-09-03:** `IconTooltipLintTests`' proximity window in `TransferQueueBar` did NOT widen — the covered lines grew 50 → 52, but the percentage FELL, 22.4% → 18.5%, because the file grew 223 → 281 lines. All five figures the lint's comment states were recounted with the lint's own method and written there with that date; four of the five were stale, the worst by 10.5 points. **Closed 2026-09-03:** the shared stripper's mis-parse of a nested string literal inside an interpolation (it now walks `\(…)` with paren depth and recurses into literals; red proved first). `TransferQueueViewModelTests` has one pre-existing unbounded `await` on a waiter, unrelated to this plan's tests. Two universal claims recorded from the same review, both prose-only, no behavioural hole, **corrected 2026-09-17** (`d89a10f2`, technical-backlog plan Task 4): `TerminalPanelViewModel.swift:326-328` said the shell-identity check is "the same discipline every other post-`await` write in this file follows" — two writes are not (the read loop's per-chunk `bufferForReplay`, which has no generation check, and `shutdown()`, which bumps the generation instead) — the comment now names the four checked writes and the two exceptions instead of claiming universality; `TerminalPanelViewModelTests.swift:473-474` said "every step is forced rather than waited for" in a sequence that ends on a justified 100 ms `Task.sleep` — the comment now says each step is gated then observed with `pollUntil`, with that one sleep named as the exception. The lint percentages of 2026-09-03 were measured at `8997c955` (five lines before HEAD in the bar). The older Core-target `SwiftSource` (`Tests/macSCPCoreTests/SwiftSourceStripping.swift`) still reads `\(` as an escape pair and lets a literal inside an interpolation pose as code — reachable through `PKCS12ImportIsolationGuardTests`, which scans every test file; fail-loud, not a hole, but the convergence on the AppKit copy is where it gets fixed. — **Converged 2026-09-05:** one stripper, `SwiftSource` in the plain `Tests/MacSCPTestSupport/SwiftSourceStripping.swift` target both test targets depend on (public; the AppKit ancestor's interpolation-safe algorithm, plus `TabContextMenuWiringGuardTests`' raw-string/extended-regex-literal parsing merged in, plus a fix `TabContextMenuWiringGuardTests` did not have: `#"""#` — a one-character-content single-quote raw string — was misread as opening a triple-quote literal by both the old private implementation and the first merged draft; Swift's own rule, that a multiline literal's `"""` must be followed by a line break, is what the merged stripper now checks). Two files deleted outright (`Tests/macSCPAppKitTests/SwiftSourceStripping.swift`, `Tests/macSCPCoreTests/SwiftSourceStripping.swift`); three more had their private copy removed and calls repointed at the shared one (`ReconnectWiringGuardTests`, `TabContextMenuWiringGuardTests`, `ConnectingAttemptWiringGuardTests`) — the four the AppKit target carried, gone; the fix round (`93d145bd`) took the last two private copies in the Core target (`PollingGuardTests`, `DiagnosticLogSharedSinkIsolationGuardTests`) with it — six in all, counted at that commit. The Core original's real callers — six, not the five its own doc comment claimed (recounted against the tree rather than trusted): `LocalListingMergeWiringGuardTests`, `BackendDescriptorHostKeyWiringGuardTests`, `PKCS12ImportIsolationGuardTests`, `DiagnosticsNoDescribingGuardTests`, `ConnectionViewModelSourceGuardTests`, `DiagnosticLogSecrecyGuardTests` — now call the shared type through compatibility names (`stripCommentsAndStrings`/`stripComments`, forwarding to `blankingCommentsAndStrings`/`blankingComments`) — safe because each reads only `contains`/`split` or a newline-count line lookup, never an absolute length, and blanking is a strict superset of the old non-length-preserving guarantee. 33 files gained `import MacSCPTestSupport`. Both old self-test suites moved to one deduplicated `Tests/macSCPCoreTests/SwiftSourceStrippingTests.swift`, plus new cases for what only one of the four pinned before (raw strings, extended regex literals, a bare `#` before a macro). `swift build --build-tests`: zero warnings. Full `swift test` after the convergence: 4922 tests, 418 suites, all green. |
| Xcode 27 / Swift 6.4: the default build system needs Metal, and `--build-system native` is deprecated | **Open, recorded 2026-09-16** at the pre-flight of the maintainer-decisions plan. Xcode 27 / Swift 6.4 changed `swift build`'s default from SwiftPM's own build system to Swift Build, which compiles SwiftTerm's `Shaders.metal` — the Metal toolchain component is now required for every local build and test run, not only for packaging — and, observed the same day, wrote both release slices to one directory instead of one each. `scripts/package-app` now pins `--build-system native` (`3522be3c`) to keep the release build on the old, known-good path; SwiftPM marks that flag deprecated, so it is a stopgap, not a fix. The exposure on CI, read from the workflows 2026-09-16: only `scripts/package-app` is pinned. `ci.yml`'s `swift build` (`:44`) and `swift test` (`:78`), and `release.yml`'s "Run tests" step (`swift test`, `:91-93`), pass no `--build-system`, so on a runner whose default Xcode is 27 they build with Swift Build and need the Metal toolchain component regardless of `package-app`. `macos-latest` (`release.yml:83`) is the exposed runner; `ci.yml` runs on `macos-15` (`ci.yml:10`). Whether the runner's Swift accepts `--build-system native` is unmeasured. **Done 2026-09-17** (`acd2fe40`, `712c23ea`, technical-backlog plan Task 1): one step, `Metal toolchain (Xcode 26+)`, added identically to both `.github/workflows/ci.yml` (`test` job, before the first `swift build`) and `.github/workflows/release.yml` (`build` job, before `swift test`) — prints `xcodebuild -version`; if `xcrun metal --version` already succeeds it prints that and stops; otherwise, if `xcodebuild -help` lists `-downloadComponent` (captured to a variable and glob-matched, not piped through `grep -q`, to avoid a SIGPIPE race that could silently take the wrong branch), it runs `xcodebuild -downloadComponent MetalToolchain` and requires a second `xcrun metal --version` to succeed, else fails the step naming the missing component; an older Xcode without the flag prints that and stops. `scripts/package-app` pins `--build-system native` and does not invoke `metal`, so the release job needs the step only ahead of "Run tests", not ahead of the later build/sign/notarize steps — `--build-system native`'s own runner acceptance stays unmeasured, and is no longer load-bearing for CI either way. Verified locally: `bash -n` on the extracted step, a real run on Metal-present hardware (prints the version, stops), and the download and flag-absent branches reproduced 3/3 and once respectively against PATH shims (this machine has Metal installed and could not exercise them for real). **The runner's own first measurement is still pending**: no CI run has yet executed this step on a runner whose Xcode needs the download, since these commits were not pushed as part of this plan. |
| `NetworkTraceTests.aBrokenReceivingSocketIsAFailureAndNeverASilentHop` failed once | **Open, recorded 2026-09-16** by Task 2 of the maintainer-decisions plan: red once under a full `swift test` run (`Tests/macSCPCoreTests/NetworkTraceTests.swift:311`), not reproduced on re-run. **Done 2026-09-17** (technical-backlog plan Task 4, no code change): `swift test --skip-build --filter NetworkTraceTests` run 20 times sequentially after `d89a10f2` — 20/20 green, 1.39-1.44 s each. Not reproduced; nothing changed. The limit stated with the measurement: these were filtered, single-suite runs, and the original red was under a full `swift test`'s load, which these 20 runs do not exercise. |

---
| `SOCKS5Handshake.swift`'s throw-site/helper comment cites stale line numbers for the forwarding connect path | **Open, recorded 2026-09-17**, checked as part of Task 9's closeout of the technical-backlog plan. `SOCKS5Handshake.swift:65-90`'s reply-code table comment was recounted by Task 6's fix round 1 (2026-09-17) and its counts are still correct (EIGHT throw sites, FIVE helpers, THREE inline constructions, verified again here with the same `grep`s), but three of its LINE CITATIONS for `SSHForwardingConnection.swift` drifted after that recount, from an edit later the same day: it cites `:119` and `:228` for the two `throw TunnelFailure.` sites and `:256` for `remoteBindFailure`, where they now sit at `:131`, `:240` and `:268` respectively (a roughly 12-line shift, all in one file). Fix shape: retake the three line numbers; per CLAUDE.md "Comments that describe other code", a number in a comment is a claim to verify at the moment it is written, and this one drifted the same day it was last verified. **Done 2026-09-17** by the docs commit of the technical-backlog plan's final fix: the three citations now read `:131`, `:240` and `:268`, and every other line number and count in that paragraph was checked against the same `grep`s and was already right. |

## Done (pointers stay so the justifications stay findable)

| Entry | |
|---|---|
| [Swift 6 warnings](superpowers/specs/2026-08-19-backlog-swift6-warnings.md) | done 2026-08-26: all six targets on `.v6`, warning-free, CI gate proved red and green |
| [Keep-alive as two settings](superpowers/specs/2026-08-25-backlog-keepalive-two-settings.md) | done 2026-09-02: `keepAliveEnabled` + a sentinel-free 15…600 interval, read-side migration for old `0`-interval files, the probe loop keyed on the switch, and the sentinel-mapping enum (`KeepAliveControlPlan`) plus the view-local memory (`SSHSettingsSection.lastKnownKeepAliveInterval`) deleted rather than kept |
| [Snippet editor part 3: declared variables](superpowers/specs/2026-08-19-backlog-snippet-part-3.md) | implemented, ten review rounds |
| [M6a polish backlog](superpowers/specs/2026-07-26-m6a-polish-backlog-design.md) | milestone completed |
| [M11e backlog sweep](superpowers/specs/2026-07-29-m11e-backlog-sweep-design.md) | milestone completed |
| [`macscp-cli diagnose`](superpowers/specs/2026-09-04-cli-diagnose-design.md) | done 2026-09-04 (`6e16b677` the rendering, `8565c546` the subcommand, this row's own commit the matrix cases and the docs). The seventh subcommand runs the same walk the app's diagnostics panel runs, as rows or as one JSON object per line, exiting 16 as soon as a step failed or timed out. Driven against every rig backend: 2 cases per backend (`--scope ping`, `--scope dial`) plus 2 that are not per backend (the `--host` trace, the two usage refusals) — 8 new cases, and the gated matrix went 55 tests in 6 suites to 63 in 7. **Two reds first**, both under `MACSCP_ITEST=1` at `8565c546`: `everySubcommandTheBinaryOffersIsDrivenByACase` (the binary offered `diagnose`, no case drove it) and `theConnectionFlagsAreAskedForPerCommand` (`without` came back `["sessions", "diagnose"]` against an anchor naming only `sessions`). **Two measurements the cases are written around.** The ICMP cell belongs to the runner, not the code: on this machine (macOS 15, Darwin 25.6.0) the echo on loopback answers `3/3 replies` and every `--scope ping` run exits 0, while a sandbox that refuses the socket would report `unavailable` (still 0) and one that hears nothing `failed` (16) — so the case derives the expected exit from Core's own rule over the rows it got and prints the row it measured, instead of pinning a number. And the diagnosis never accepts a host key: the SSH dial answers with `HostKeyDecider.refusing`, so against a fresh store the rig's key is a `failed` row with exit 16 and nothing is written to `known_hosts.json`; after one `ls --accept-new`, the same command exits 0. The design doc said the key was "decided by the same `--accept-new` / `--non-interactive` policy as a connect" — that wiring never existed, and the sentence was corrected in the same commit. **Note 2026-09-04** (the round after `36ec918f`): the line-buffering case `rowsArriveInMoreThanOneChunk` measures the reader's wakeups between six flushes about a millisecond apart (1 chunk without `setvbuf`, 6 with it, on an idle machine); on a starved reader the flushes coalesce and the count reads 1 with the fix present, so the case sits behind `MACSCP_PIPE_TIMING=1` rather than beside the rig suite. |
| [CLI: sessions and tunnels from the command line](superpowers/specs/2026-09-06-cli-store-and-tunnels-design.md) | done 2026-09-07 (`docs/superpowers/plans/2026-09-06-cli-store-and-tunnels.md`, seven tasks; docs commit `77ba8994` design and plan, feature commits from `31654efc`, final task commit `935286e1`, plus this row's own commit `28776211`. **30 task commits**: `git log --oneline 00093ccc..28776211` returns 32 lines, and the two the count leaves out are named — this row's own commit `28776211` and the base docs commit `a383bd20`, which belongs to the PREVIOUS plan's review and sits at the bottom of the range because that is where the branch was cut. **13 of the 32 are `docs(` commits** (counted 2026-09-07 with `git log --oneline 00093ccc..28776211 | grep -c '^[0-9a-f]* docs('`), which is what a record-heavy plan looks like from the outside. The final review's own fix commits land after `28776211` and are outside both numbers, the way the port-forwarding row below says of its own). Per task, commits counted against `.superpowers/sdd/2026-09-06-cli-store-and-tunnels/progress.md`: **Task 1** (Core rules) `31654efc`, `36e53bda`, `0d121012` (the session-name-authority row below), `bb26a673`, `9aef3823` — `TunnelSpec.parse`/`render` (OpenSSH `[bind:]port:host:hostport` / `[bind:]port`), `TunnelCarriers.carries(_:)`/`refusal(for:)` (an exhaustive switch: only `.ssh` carries a forwarding), and `SessionNameRule.conflict(_:among:excluding:matching:)` — one rule now read by both the app's save warning (`.exact`) and the CLI's refusal (`.caseInsensitive`, trimmed), where before there were two copies. **Task 2** (`sessions add/edit/rm`) `df037792`, `ff1c19c9`, `f4af5237`, `c68748c2` — writes the session store, never a secret (no `--password`/`--passphrase`/`--secret-key`, no stdin read, no keychain call, held by a source-scanning guard); `rm` deletes a session's tunnel profiles first, through `TunnelStore.deleteAll(for:)`. **Task 3** (`tunnels list/add/edit/rm`) `f47475ea`, `eccfa855`, `a70f64c5`, `d6fad148`, `d4bf0c15` — `TunnelRow: Codable` for `--json`, plus the applicability matrix case `tunnelsAddIsAllowedExactlyWhereCoreSaysSo`, which runs `tunnels add` against Core's own `TunnelCarriers.carries(_:)` on every rig backend rather than a second hand-written list. **Task 4** (`tunnels start`) `d171b744`, `6b9c8305` (the SFTP-channel row below), `2157cd81`, `411319e9`, `60831bb5`, `22a7829f` — holds one forwarding open in the CLI process, foreground, until Ctrl-C or failure; the drive loop lives in Core as `TunnelForegroundRun.drive` (`Sources/macSCPCore/CLI/TunnelForegroundRun.swift`) over a `ForegroundTunnel` protocol, so it is testable without the rig, and the rendering it drives moved with it as `TunnelStartRendering` (`Sources/macSCPCore/CLI/TunnelStartRendering.swift`) — the plan had placed both `TunnelStateLine.render(_:json:)` and `TunnelExit.code(for:)` "in the CLI target", corrected in the plan and recorded in the design's new "Deviations from this design, as built" section on 2026-09-07. **Task 5** (activation reload) `bfa0cd0a`, `8ffac56b`, `1f6e89d8`, `ccf4dfd6`, `6064ce9b` (the TunnelStore-writes row below), `7c1a1969`, `d6b6ad9b` — the app re-reads both stores on `NSApplication.didBecomeActiveNotification`; a profile the CLI deletes while the app is running it is reconciled (stopped and its state dropped), a case the Task 5 review found and folded into the design. **Task 6** (matrix) `38e39b6f`, `e5b76997`, `935286e1` — nested verbs and per-backend applicability read off the binary and off Core rather than listed in the tests, plus the matrix end-to-end over SSH: a local forward carrying the sshd's own banner, a dynamic forward answering a hand-rolled SOCKS5 CONNECT, a remote forward reached from inside the container, Ctrl-C ending with exit 0 and a `stopped` line, an unknown host key refused without `--accept-new` and a changed one a hard stop that `--accept-new` does not open. **Limits** (design's Limits section, unchanged by this row): a CLI-started tunnel is invisible to the app's glyph and badge — the app's menu shows the profile as stopped; a tunnel edited while the app is running it keeps its old mapping until stopped and started again in the app; a CLI write between an app read and an app write of the SAME session is lost, the app's write wins, same as two windows; no secret is ever written by the CLI, ever; groups are created along a path but never deleted by the CLI; no `apply`, no import/export; name completion for `sessions edit/rm` and `tunnels … --session` is deferred (the existing completer appends a trailing `:` for `name:/path` targets, deferred item found in Task 2); the CLI's name rule is stricter than the app's (case-insensitive and trimmed, versus the app's exact match) — see the session-name-authority row below for the one measured gap this plan found rather than closed, the TunnelStore-writes row below for a second, and the SFTP-channel row below for a third. The design's `--auth basic|digest` flag for WebDAV was not implemented — `StoredWebDAVConfig` has no auth-scheme field to hold it — `--nextcloud` stands in its place. **Deferred minors** (full text in `.superpowers/sdd/2026-09-06-cli-store-and-tunnels/progress.md`'s "minor (deferred)" lines): Task 1 — `render`/`parse` not inverse for a stored host beginning with `[`, no bind/host content validation. Task 2 — `edit` cannot turn a `@Flag` off, a declined `rm` prompt exits 0, a same-group re-file leaves position (deliberate, untested). Task 3 — `tunnels list --session X` decodes `sessions-v2.json` twice (validate, then run); a host or a name containing a NEWLINE splits the TEXT listing into rows that are not rows (nothing escapes it on that path, and no verb refuses such a name), where `--json` escapes it correctly — so a script that parses the text output can be fed a fake row by whoever chose the name. Found by the final review's triage, 2026-09-07; deferred because the fix is a decision (refuse the name on write, or escape on render) rather than a repair. Task 4 — the decider guard's scan covers only the composing body (run/validate/interrupts unscanned, not reachable today); `CLISourceWalk` keys slices by bare function name across files (order-dependent, stable under 60 shuffles); exit 12 is not driven through the drive loop. `TunnelLatch.wait` was listed here as "ignores cancellation (a leak on a red path)" and is now FIXED rather than deferred (2026-09-07, final review): ignoring cancellation is the contract — the only path it is ever waited on from is a cancelled one, and a wait that answered cancellation failed `aStartThatLandsWhileStopIsSuspendedWaitsForIt` in 3 of 3 measured runs — but it ignored it by SPINNING, `try?` discarding the throw of a `Task.sleep` that stops suspending once cancelled, which burns a cooperative-pool thread for as long as the gate is held. It now parks on an `AsyncSignal` awaited on a detached task, and `PollingGuardTests.noWhileLoopSwallowsItsSleepsCancellation` forbids the shape across `Tests/`. Task 5 — no test drives three chained reconcile callers; the handle retains one completed task; the observer is proved only by source guards; cross-process last-write-wins is stated, not measured beyond that. Task 6 — the pid window between reap and the run task's `defer` is narrowed, not closed (`SubprocessRunner` would need its own exit latch); `jsonStateIs` treats an undecodable line as "not this line" by design; two checkouts racing the 40000–60000 remote-port draw is unmeasured. `swift test` at the final task commit (`e5b76997`, unchanged by `935286e1`'s comment-only fix): **5351 tests in 456 suites, green** (ungated); `MACSCP_ITEST=1 swift test --filter CLIMatrix`: **89 tests in 8 suites, green**. After the final review's fix commits (2026-09-07, outside the 32-line range above): **5358 tests in 457 suites, green** (ungated), `swift build --build-tests` **0 warnings** — the seven new cases are `TunnelLatchTests` (2), `PollingGuardTests`' swallowed-sleep pair (2), the `tunnels start` secrecy guard's positive and negative (2), and `tunnels start`'s own no-secret-flag case (1). **Sight check for the maintainer, NOT yet performed**: run `tunnels start` from a terminal while the app is open, and confirm the app's menu shows the profile as stopped (the recorded limit above); and that a `sessions add` run from a terminal appears in the sidebar after ⌘-Tab (the activation reload, Task 5). |
| [SSH port forwarding](superpowers/specs/2026-09-06-port-forwarding-design.md) | done 2026-09-06 (`docs/superpowers/plans/2026-09-06-port-forwarding.md`, eight tasks; **34 commits**, plus this row's own commit `900642df` — `git log --oneline b73abd41..900642df` returns 35 lines, and the 35th is the docs commit that wrote this row, which the count deliberately leaves out and which the sentence originally failed to say (corrected 2026-09-06 at the final review, whose own fix commits land after `900642df` and are outside both numbers). Not the plan's own placeholder "eight commits", which named one Step-3 commit per task before the fix rounds happened). Per task: **Task 1** (model, store, state planner) `7c19a301`, `f48fa73c` — `TunnelProfile`/`Kind`/`AutoStart`/`TunnelState`, `TunnelStore` (`tunnels.json` beside the session store), the pure `TunnelStatePlan` + `BackoffPlan`, and a `SessionDeletionObserver` seam so deleting a session removes its profiles. **Task 2** (local forward) `bf9f16c6`, `4f673653`, `e853066c`, `385f262b` (+ the coordinator's stale-comment fix `4be53e72`) — `BytePump` (a backpressure-aware duplex byte pump), `LocalForwardListener`, `TunnelConnection` (builds the SSH connection for a stored session the CLI's own way), `CitadelFileSystem.openDirectTCPIP`. **Task 3** (dynamic forward) `d0cbf657`, `04ee1885` — a hand-written `SOCKS5Handshake` decoder and `SOCKS5Listener` speaking SOCKS5 with no authentication, CONNECT only. **Task 4** (remote forward) `8ce2535f`, `a037a2ba`, `b35f4e3b` — `CitadelFileSystem.withRemotePortForward` and `RemoteForward`, a long-lived task per inbound channel, `stop()` bounded and cancellable via `BoundedClose`. **Task 5** (`TunnelRunner`) `ab5f62dc`, `75d333d6`, `94ade42b`, `94baa44f`, `7e773cd3`, `e7955c89` — an actor per profile driving `TunnelStatePlan`, reconnect on 2 s-doubling-to-60 s backoff, the `tunnel` diagnostic-log category with `reason:`-mapped lines. **Task 6** (`TunnelManager`, context menu, profile overlay) `ea797a62`, `2eb6f8e6`, `4600f4e6`, `817bbee3`, `9b704e34`, `1100ff32`, `09641bb9`, `7fb2ac6a`, `8e7460f9`, `7046da25` (+ the coordinator's docs commit `ae4bc912`) — `TunnelManager`, the session row's "Port forwarding" submenu, `TunnelProfilesSheet`, `stopAll()` wired into the quit chain before the windows close. **Task 7** (autostart overlay, login item, sidebar glyph, Dock) `b1d2c713`, `1a4b94b1` — `TunnelAutostartSheet`, `LoginItem` (`SMAppService`), the sidebar forwarding glyph, the Dock badge and menu block, `startAutoStart` wired in at launch. Two commits inside the range are not port-forwarding work and are this file's own rows elsewhere: `b4b4a81f` and `7af90e40` are the wall-clock-claim and fingerprint rows below; `017adf76` is an unrelated maintainer-reported dev-build fix (the file browser's name-column marker gap) bundled into the same push. `swift test` at the final commit (`1a4b94b1`): 5173 tests, 441 suites, green. **Limits, recorded rather than fixed** (the rest are already their own rows and are pointed at, not repeated): a remote forward naming port `0` ("let the server choose") is refused — the pinned Citadel registers its inbound handler under the REQUESTED `(host, port)` and dispatches on the BOUND one, so a server-chosen port would bind and then swallow every connection; the refusal is a fork debt, recorded in `superpowers/specs/2026-08-20-backlog-dependencies.md` under "Open 2026-09-06". No fork change has been made and **no tag carries a fix** — `Package.swift` pins Citadel at `exact: "0.12.1-noix.3"`, and this row named a `0.12.1-noix.4` that exists nowhere (corrected 2026-09-06 at the final review; `grep -rn "noix.4"` over the tree found this row and nothing else). What the record actually states is a review date: "at the next release and before the next Citadel fork change, with the rest of the fork check this document prescribes". It also states the fix shape and that the guard and its test (`TunnelRigITests.aRemoteForwardOnPortZeroIsRefused`) must be removed together when the fork carries it, because the test pins OUR refusal and would stay green against a fixed Citadel. The same key could in principle mismatch on the HOST half too, for a `0.0.0.0` bind whose server echoes a different `listeningHost` — **unverified**, the rig's `GatewayPorts` is off. The dynamic forward's SOCKS5 failure replies `05 connectionRefused` and `04 hostUnreachable` are both unreachable in practice today (NIOSSH drops the open-failure reason, and the listener wraps every error as `.channelOpenFailed`); only `01` general failure is ever produced. A `reconnects` profile retries forever on the same backoff, no ceiling and no give-up state — `stop()` is the only end. The autostart sheet's "Last active" column is not recorded (`TunnelState` carries no timestamp, `TunnelStore` writes no history) and is deferred until the runner keeps one, per the design doc's autostart section. Pointed at rather than repeated, five rows in three sections of this file: **Interface** — `Forwardings "At login": the launch flag is unverified, and "Start in the background" is not shipped` (also covers the ad-hoc-signed dev build never reaching `SMAppService` `.enabled`) and `A forwarding's failure reason is English on four localized surfaces`; **Security and testability** — `Host-key fingerprints in the diagnostic log (maintainer decision)` and `The SOCKS5 handshake has no timeout`; **Bugs** — `A local or dynamic forward's per-connection failures are never reported`. The last three were opened on 2026-09-06 by the final review; the section names were corrected in the same pass, this row having called all of them Bugs rows. **Deferred minors** (full text in `.superpowers/sdd/2026-09-06-port-forwarding/progress.md`'s "minor (deferred)" lines): Task 2 — the listener's negotiate-then-open gate wiring is pinned only at 1-of-10 sensitivity; `channelWritabilityChanged` resumes the peer without consulting that gate (unreachable today); the SOCKS5 accumulator is unbounded while `.connecting`, and the window is one event-loop turn wide: `BytePump.install` puts the local handler on the accepted channel — which turns that channel's `autoRead` back on and issues a read — and only the `confirm` that follows sets the handshake handler to `.handedOver`, so bytes a client pipelines behind its CONNECT in between are appended to `accumulator` with nothing reading them and no cap on the size (`LocalForwardListener`'s accept path, `SOCKS5HandshakeHandler.channelRead`). Task 5 — the six-row reconnect table's names/seeds/digits were only spot-checked; a `Citadel.onDisconnect` registration window, a swallowed non-cancellation throw in the sleeper, no event epoch, no test for `stop()` mid-dial or for `port=-`, and a second copy of two names in the secrecy guard's `dynamicOnly`. Task 6 — `appKitFiles()` one level deep, `runningCount` read right after start on ⌘Q's `.now` branch, a hardcoded `.red`, "Connect this session once by hand" surviving a cancelled prompt, duplicate profile names allowed, every window re-registering the store. Task 7 — `SessionRow` computes `tunnelStates` twice per redraw, `profiles(for:)` filters every profile instead of indexing, badge-clearing at quit unproven. One of these was promoted rather than kept here: Task 5's unforwarded `onFailure`/`onConnectionFailure` is a user-visible silence, not a deferred minor, and is now the Bugs row named above. **Sight check for the maintainer, NOT yet performed** (Tasks 6 and 7 both record "no view rendered by any test" — the dev build is the only witness): a local forward to a database; a dynamic forward with a browser's SOCKS setting; a remote forward; ⌘Q with a tunnel running; autostart across a relaunch and a login. |
| [Shell completion from Settings](superpowers/specs/2026-09-08-shell-completion-settings-design.md) | done 2026-09-10 (`docs/superpowers/plans/2026-09-08-shell-completion-settings.md`, two tasks; feature commits `52627742` — the recipe (`ShellCompletionRecipe` in Core: `line(for:tool:)`, `shell(fromLoginShellPath:)`, `quotedForShell(_:)`), the Settings section (a picker over the three shells, the line, a Copy Command button, the per-shell "where" sentence, the zsh `compinit` clause, a footer keyed on install state) and their tests — and `eb0c387f` — the review's fixes: the forbidden command fragments are derived from `line(for:tool:)` with a sentinel rather than spelled as literals in the guard; `.translocated` gets its own `CLICompletionPresentation` branch (no line, no copy button, a move-the-app-first sentence) instead of showing a dead path; the fish interpreter lookup (`fishCandidates(inPATH:)`) is tested against a planted executable rather than left unmeasured; six private `locateCLIBinary()` copies folded into `CLIMatrix.binaryURL()`. `swift test` at `eb0c387f`: 5390 tests in 461 suites, green. **Deferred minors** (from `.superpowers/sdd/2026-09-08-shell-completion-settings/progress.md`'s "minor (deferred)" line): the section is source-verified only — never rendered, `.translocated` unreachable without a disk image; fish is unrun as an interpreter on this machine; the property that the guard follows the recipe's own spelling cannot be mutation-probed, because `ShellCompletionRecipeTests` already pins the three lines verbatim. **Limits** (design's Limits section): the line completes `macscp-cli` invoked by that name, or by the quoted full path shown — a user who installs the tool under another name edits the line by hand; zsh needs `compinit` initialised, which the section states but does not detect; remote paths after `name:` are still not completed (session names only, per the CLI-completion backlog's item 2 decision), and that completer is the one deferred for the `sessions`/`tunnels` verbs' `--session`. **Sight check for the maintainer, NOT yet performed**: open Settings → Command-Line Tool, pick each shell, copy the line, paste it into that shell's rc file, restart the shell, and type `macscp-cli <Tab>`. |
| [PEM private keys](superpowers/specs/2026-09-10-pem-private-keys-design.md) | done 2026-09-10 (`docs/superpowers/plans/2026-09-10-pem-private-keys.md`, five tasks; nine feature commits plus this row's own commit `985afbcd` (Task 5, the closeout) — Task 1 (decoder) `79589a00`, `ce366821`; Task 2 (converter) `7f3e02b8`, `d22f09d4`; Task 3 (loader and message) `5634c012`, `71fdf3f2`; Task 4 (key manager import and failed surface) `6dba7444`, `6e89ee9b`, `38e1e062`) (+ rig and CLI coverage 2026-09-11: `d9c4ff54`, `2465d684`, `d94ece2c` (coverage record), `706d12b9` (final-review fix dispatch)). A PEM-format private key (PKCS#1, SEC1, PKCS#8, plain or passphrase-protected) reads directly through a pure Core decoder (`PEMPrivateKeyDecoder`, swift-asn1 + swift-crypto primitives); what it cannot read is named by feature and offered `ssh-keygen -p` as a copyable command or a one-click conversion of a copy (`SSHKeyConverter`); the key manager's import converts on the way in so every stored key stays OpenSSH-format and exportable. Open: a stored session bound to a login set converts its key for one retry only — see the Interface row below. |

---

## If you don't know where to start

Three candidates, for different reasons:

1. **Single click no longer connects** — one line, felt on every use, and the context menu already has the path.
2. **Known-hosts column sorting** — almost free, because the sheet is already a `Table`.
3. **The capability boundary** — the only entry that ends a recurring bug class instead of handling its next instance.
